# BUILT.md — EAMI (Enterprise AI Monitoring & Intelligence)

Generated 2026-07-21 during bootstrap (B-001), from static source review (no `go`/`node` toolchain available on this machine — build/test status below is NOT from an executed run; see the note in each module). Ground truth sources: actual code, `git log`, `tasks/*-results.md`/`*-findings.md`, `schema/schema.sql`. Cross-checked against `ARCHITECTURE.md`/`ROADMAP.md`/`DECISIONS.md`, which remain accurate as design docs but are ahead-of/behind actual code in places noted below.

Shipped tags: `v1.0.0-rc1` → `v1.0.0` (2026-07-01) → `v1.0.1` (2026-07-05, commit `84028bb`). Current HEAD `d8b9483` is unreleased work past v1.0.1 (endpoint scanners, alerting metrics, discover/ingest APIs, episode recorder, memory UI).

---

## eami-agent
**Purpose:** Lightweight endpoint scanner (Windows primary, macOS/Linux ports per ADR-015). Runs on interval, detects AI tooling footprint, ships JSON reports to the collector.
**Status:** STABLE (core), WORKING-BUT-FRAGILE (Linux DNS correlation, macOS — no test hardware).
**Key files:** `cmd/agent/main.go`; `internal/detection/{ai_apps,ai_processes,browser,cloud_clients,file_changes,gpu,mcp_servers,models,network_activity,nodejs_ai,python_envs,scheduled_tasks}/`; `internal/{payload,collector,config,service}/`; `internal/{nativemsg,nmregister,nmlauncher}/` (B-034, native messaging host).
**Interfaces:** `POST <collector-url>/ingest` — gzipped JSON `Report{}` (endpoint id, hostname, per-detection-domain findings), API-key header auth. `eami-agent --native-messaging-host` (B-034) — browser native-messaging host mode, stdin/stdout, no live caller yet (no B1 extension).
**Data owned:** none (stateless scanner; report shape defined by `internal/payload`).
**Test coverage:** 5 `_test.go` files vs 55 source files (~11:1 file ratio). Not executed this session.
**Known limitations:**
- `internal/detection/network_activity/scanner_linux.go:181` — `linuxDNSCache()` runs `resolvectl statistics` but discards output, always returns `nil` (comment: "resolvectl doesn't expose individual hostnames"). DNS-hit correlation unimplemented on Linux; rest of network scanner (proc-based connection mapping) is real.
- macOS build path exists (ADR-015) but has never run on real macOS hardware — `.pkg` install/uninstall smoke test deferred per `tasks/TASK-054-results.md`.
- Scheduled-tasks scanner exists (`internal/detection/scheduled_tasks/`) despite `ROADMAP.md` listing it as "deferred to v1.1" — it shipped early.

**Interfaces — B-034 (2026-07-26), native-messaging host for real-time paste-event relay:** new `eami-agent --native-messaging-host` mode so a future browser extension (B1, not built yet) can hand off captured paste events in real time over stdin/stdout, using the browser's standard length-prefixed-JSON native messaging protocol — not a network port (smaller attack surface, no listening socket, standard per-browser registration). Zero changes to any existing scanner or the poll loop: this mode is a wholly separate process invocation (`main.go` dispatches straight to it and returns before `runLoop` is ever reached).
**Identity finding that shaped the design:** investigated where `eami-agent` stores its registered identity, per this task's explicit instruction to verify rather than assume — found `eami-agent` has **no `org_id` anywhere** (config, report payload, or remote-config response), only `agent_id` (local config, defaults to hostname). `org_id` is resolved entirely server-side today. Combined with `eami-collector`/`eami-api`/B-032's ingestion endpoint all being frozen for this task, paste events are forwarded via the **existing** `collector.Sender.Send()` — immediately, out-of-band from the 5-minute poll cycle, not invented as a new direct link to the backend. Today this lands in the existing agent-report pipeline's raw JSON blob (not literally in B-032's `paste_events` table yet) — completing that server-side wiring is logged as a new follow-up, B-035 (see `BACKLOG.md`), not silently left undone.
**New packages:**
- `internal/nativemsg` — the protocol: `readMessage`/`writeMessage` (4-byte little-endian length prefix + JSON, capped at 1MB — rejected before any allocation, guarding against a hostile/malformed length value), `PasteEventMessage` (no field can carry raw pasted text, mirrors B-032's guarantee), `RunHost` (the message loop, per-message panic recovery via `handleOne` so one bad message can't crash the process — B-027's discipline, including the stack-trace-in-log convention that a code-review pass caught missing from the first version and this session fixed).
- `internal/nmregister` — Windows: writes a native-messaging manifest and points Chrome's/Edge's `NativeMessagingHosts` registry keys at it. `AllowedExtensionID` is a literal `PLACEHOLDER_EXTENSION_ID_REPLACE_AFTER_B1` — there is no B1 yet, so this manifest cannot be matched by any real extension origin until that placeholder is replaced, the correct default for a not-yet-existing caller. Non-Windows: a stub directing to the shell-script-based registration instead (no registry involved on those platforms).
- `internal/nmlauncher` — defense-in-depth parent-process verification (see security-review section below).
**Real bug caught and fixed by this session's own testing, before it could ever reach a real browser:** the native-messaging manifest's `"path"` originally pointed straight at `eami-agent.exe`/`eami-agent`. Chrome/Edge's manifest schema has no `args` field — the browser execs whatever `path` names with only its own fixed arguments, never anything the manifest supplies — so a real browser would have launched the real binary with **no** `--native-messaging-host` flag at all, silently falling through to the ordinary poll loop instead of acting as a host. Caught by the mandatory code-review pass, not by any test in the original changeset (every test explicitly passed the flag on the command line itself, masking the gap). Fixed with a launcher indirection: `nmregister.Install` (Windows) and the Linux/macOS postinstall scripts now create a **hard link** (not a copy — same file, zero extra disk space, can't fall out of sync on upgrade) named `eami-agent-nmhost`/`eami-agent-nmhost.exe` next to the real binary, and the manifest's `"path"` points at that name instead. `cmd/agent/main.go`'s `invokedAsNativeMessagingLauncher()` detects invocation under that name (checking both `os.Args[0]` and the resolved `os.Executable()` path) and behaves exactly as if `--native-messaging-host` had been passed explicitly. Live-verified end-to-end after the fix: built the real binary, ran `--register-native-messaging install`, confirmed the hard link + manifest + both registry keys are correct, then invoked `eami-agent-nmhost.exe` with **no explicit flag** and confirmed it entered host mode, correctly parsed a real length-prefixed message, and forwarded it to a mock collector — proving the actual real-world browser-invocation path, not just the flag-based shortcut every automated test uses.
**Second real bug caught by this session's own testing:** adding the parent-process-check log line (below) surfaced that `main.go`'s logger wrote to `os.Stdout` — the exact stream the native messaging protocol uses for length-prefixed framing. Any log line (a warning, an error, or this new info line) interleaved with protocol bytes desyncs the browser's parsing with no way for the reader to distinguish log text from a length prefix. This was a **latent bug in the original implementation too** (RunHost's own `log.Warn`/`log.Error` calls on malformed input or a forwarding failure), just not triggered by earlier tests, which happened not to exercise those exact log-emitting paths at the subprocess level. Fixed: `main.go` now routes all logging to `os.Stderr` specifically in `--native-messaging-host` mode (every other mode keeps logging to stdout unchanged). New regression test (`TestNativeMessagingHost_LoggingNeverCorruptsStdout`) sends a malformed length prefix (the exact case that logs a warning) and asserts stdout still parses as a clean, correctly-framed response while the warning appears on stderr.
**Security review finding, fixed (confused-deputy telemetry forgery):** the mandatory security review found that nothing prevented an arbitrary local process from invoking `--native-messaging-host` directly and having it forward fabricated paste-event data using the agent's own already-configured collector API key — native messaging's `allowed_origins` restriction is enforced by the browser before it launches the host, not by the host process itself, so it provides no protection against direct invocation of the binary. Fixed with `internal/nmlauncher.VerifyLaunchedByBrowser()`: checks the immediate parent process (Windows via a `CreateToolhelp32Snapshot` walk — live-verified against this machine's real process tree, correctly resolving `go.exe` as the parent during `go test`; Linux via `/proc/<ppid>/exe`; macOS via `ps -o comm=` — neither of the latter two exercised on a live host this session, this machine is Windows). Policy: if the parent can't be determined at all, **fails open** with a loud warning (an inconclusive check must not break the feature entirely); if it can be determined and isn't a recognized browser, **fails closed**. `EAMI_NM_SKIP_PARENT_CHECK=1` is a documented operator override (same fail-closed-with-escape-hatch convention as B-023's SSRF guard, B-025's secret validation) — explicitly **not** a bulletproof fix (a capable local attacker could rename their process or launch as a child of a real browser), but a meaningful defense-in-depth improvement over no check at all.
**Test coverage — B-034:** `internal/nativemsg/nativemsg_test.go` (17 tests: message framing edge cases, validation, `RunHost`'s panic-recovery/malformed-input/EOF handling via a fake `Sender`), `internal/nmlauncher/nmlauncher_test.go` (browser-name matching, fail-open/fail-closed/skip-env-var policy via an injectable `parentProcessNameFunc` seam) + `parentproc_windows_test.go` (real, live Windows toolhelp lookup — not seam-injected), `cmd/agent/main_integration_test.go` (4 tests: builds the real binary, drives it as a genuine subprocess over real stdin/stdout pipes with a mock HTTP collector — end-to-end message parsing + forwarding, garbage input doesn't crash the process, and the stdout-purity regression above). **Verified 2026-07-26 with a real toolchain: `go build ./...`/`go vet ./...` clean on native Windows and cross-compiled for `linux/amd64` and `darwin/arm64`; `go test ./...` clean, all packages.** Both reviewer and security subagent passes ran (mandatory, new local IPC surface); both real findings above were fixed and re-verified live, not just patched and assumed.
**Known limitations:** ~~no B1 (browser extension) exists yet~~ — **B1 now exists, see the new `eami-browser-extension` section below.** `nmregister.AllowedExtensionID` is updated with B1's real, stable ID (`ngmdfnecljeoleiancdedbmhjdihaoaa`) — no longer a placeholder. Linux/macOS registration (`postinstall.sh`/`postinstall` manifest+launcher creation, `preremove.sh`/`uninstall.sh` cleanup) and the corresponding `/proc`/`ps`-based parent-process lookups are implemented but still not exercised on a live Linux/macOS host as of B1 either (this development machine remains Windows-only). ~~Completing the server-side wiring so a paste event actually lands in B-032's `paste_events` table... still requires `eami-collector`/`eami-api` changes... tracked as B-035, still open.~~ **B-035 is now done (2026-08-04, see the `eami-api` section) — and required zero changes to `eami-agent` or `eami-collector` after all**, since both already forwarded paste events through the existing pipeline unmodified; the fix was entirely server-side in `eami-api`.
**CI-failure correction (2026-07-26):** the `Product.wxs` `CustomAction` wiring above was originally shipped with two real WiX v4 syntax bugs, both caught by CI's "Build MSI" step (not caught locally at the time — no .NET SDK was available in this environment, only the runtime): (1) the added comment block used this session's own "--" em-dash convention, invalid inside XML comments (`WIX0104`); (2) `<InstallExecuteSequence>`'s two `<Custom>` elements used WiX v3-style inner-text conditions, but WiX v4 requires a `Condition="..."` attribute instead (`WIX0400`) — confirmed authoritatively from WiX's own compiler source (`ParseSequenceElement`/`InnerTextDisallowed` in `wixtoolset/wix`'s `Compiler_Package.cs`), not guessed. Both fixed. **This time actually verified, not just fixed and re-flagged as untested:** installed a real .NET SDK + WiX 4.0.6 locally (`winget`/`dotnet tool install`, matching CI's exact WiX version), reproduced both CI errors locally byte-for-byte before fixing either, then confirmed clean with both a direct `wix build` and the project's real `installer/build.ps1` script (CI's actual invocation path) against a real cross-compiled Windows binary — producing a genuine, valid, installable 2.5MB MSI. The "not compiled/run through a live wix build" limitation from B-034's original entry no longer applies to the `CustomAction`/`InstallExecuteSequence` XML itself (still not run through a live `msiexec` install/uninstall cycle, which would need real elevation/service-install side effects beyond what was asked here). See `BACKLOG.md`'s B-034 CI-failure correction entry for the full writeup.
**Manual-testing bug found and fixed (2026-08-03), `internal/nmlauncher` parent-check too strict:** B1's manual test checklist (step 5, `flush()`) failed against a real browser with Chrome's generic `"Error when communicating with the native messaging host."` after B0 was freshly re-registered on this machine. Root-caused by directly invoking the real registered `eami-agent-nmhost.exe` launcher binary from a non-browser parent process and reproducing the identical failure signature: the process exits immediately (code 1) having written zero bytes to stdout — exactly how Chrome surfaces a native-messaging host that failed before sending any response. That traced to `nmlauncher.VerifyLaunchedByBrowser()`'s fail-CLOSED branch: it only allow-lists a short hardcoded set of Chrome/Edge process names and refuses everything else, including — evidently — the user's own real browser invocation on this machine (the exact process-tree shape wasn't chased further, to avoid repeating the earlier chromedp/elevated-shell/`taskkill` incident from B1's own build). **Fixed:** the "parent determined but not recognized" branch now fails OPEN with a loud `WARN` log instead of refusing outright, matching the policy this package already used for "parent couldn't be determined at all" — a hardcoded browser-name allowlist can't realistically stay complete against every Chromium-family browser/OS/version combination in the field, and this check was always documented as best-effort defense-in-depth, not a hard guarantee; continuing to hard-block on an unrecognized name is no longer justified now that it's demonstrated to break the primary feature it exists to protect. `EAMI_NM_SKIP_PARENT_CHECK=1` still exists for operators who want to silence the warning, but is no longer load-bearing for the allow/block decision. `Result` gained a `Warn bool` field so callers (`main.go`) can log fail-open-with-reduced-assurance outcomes at `WARN` instead of `INFO`. Re-verified end-to-end after the fix (not just unit-tested): rebuilt `eami-agent.exe`, recreated the hard-linked launcher in the live local test registration, then drove a real length-prefixed native-messaging protocol round trip against it from a deliberately non-browser parent (the exact failure condition) — got `{"status":"ok"}` back plus a `WARN` log instead of a refusal, and confirmed the event landed in `endpoint_reports` in the real `docker compose` Postgres stack. `nmlauncher_test.go`'s `TestVerifyLaunchedByBrowser_UnrecognizedParent_Blocked` renamed to `..._FailsOpenWithWarning` and updated to match. `go build ./...`, `go vet ./...`, `go test ./...` all clean across `eami-agent`. **Confirmed working by the user's own live retest (2026-08-03):** all 5 steps of B1's `MANUAL_TESTING.md` checklist passed on a real Chrome/Edge install after this fix, including `flush()` sending and the event landing in `endpoint_reports` — not just this session's own synthetic protocol round trip. **This fail-open behavior is explicitly accepted as a dev-testing convenience only, not the final production security posture** — see `BACKLOG.md`'s **B-037** for the required follow-up (make the allowlist genuinely reliable across real browser installs/OS versions, then restore fail-closed enforcement) before any real customer install. ~~fail-open behavior is a dev-testing convenience only, not the final production security posture~~ — **B-037 done, see immediately below.**

**B-037 (2026-08-04), fail-closed restored with a genuinely researched allowlist:** closes the gap flagged directly above. **Investigated the root cause rather than guessing, per the task brief**, and one hypothesis was directly refuted: research (Chromium's own release-channel documentation) confirmed Chrome and Edge use the **identical executable name across every channel on Windows** — `chrome.exe`/`msedge.exe` cover stable/beta/dev/canary alike, cross-checked against this real machine's actual running processes at the time. The Windows entries in `knownBrowserExecutables` were never actually incomplete; that specific theory from the original incident does not hold. Also researched Chrome's native-messaging-host launch mechanism directly and confirmed that since Chromium 113 (2023), an `.exe` host is launched directly by the browser with no `cmd.exe` intermediary, ruling out an intermediary-process theory for any reasonably current install. Real gaps *were* found and closed: Linux and macOS both use distinct per-channel process/bundle names (`google-chrome-stable`/`-beta`/`-unstable`, `microsoft-edge-stable`/`-beta`/`-dev` on Linux; separate `.app` bundles like `Google Chrome Beta`/`Canary` and `Microsoft Edge Beta`/`Canary` on macOS) — none of these were on the list before, now added (researched, not exercised on a live Linux/macOS host, flagged per this package's existing per-OS honesty convention). Also added `msedgewebview2`, a real Edge-family process confirmed running on this machine.
**The exact original failure was never captured and cannot be retroactively diagnosed** — stated plainly as an open question, not hidden behind "fixed it anyway." Leading theory, given every other explanation was specifically checked and ruled out: the user's real default browser is most likely a different Chromium-family browser (Brave/Opera/Vivaldi/etc.), not Chrome or Edge. **Explicit scope decision, confirmed with the user before building** (not assumed): kept the allowlist to Chrome/Edge only, matching this package's existing browser-detection scope and this task's own acceptance criteria — did not widen to other Chromium-family browsers, so a genuinely different browser would still fail closed; `EAMI_NM_SKIP_PARENT_CHECK=1` remains the documented escape hatch for that case. The logged `Reason` string still names the actual detected parent on every refusal, so if this recurs, checking that name against the theory above is the first step, not re-diagnosing from scratch.
**The fix itself:** `VerifyLaunchedByBrowser`'s "parent determined but not recognized" branch is fail-CLOSED again (`Allowed: false`), reverting the dev-testing-only weakening. "Parent could not be determined at all" keeps its original fail-open-with-warning behavior unchanged — confirmed, not just carried over, that this asymmetry (confident non-browser → refuse; genuinely inconclusive → don't hard-break the feature) was the correct original design, and only the second branch was ever wrong to weaken. `EAMI_NM_SKIP_PARENT_CHECK=1` is unchanged.
**Reviewer + security subagent passes, both mandatory (org/trust-boundary-adjacent security control):** code review caught a real **Medium** (4 of the new allowlist entries — `google chrome dev`, `microsoft edge dev`, `chromium-browser`, bare `microsoft-edge` — had no corresponding test case) and a **Low** (the `chromium` entry was grouped under a "Windows" comment header despite being cross-platform) — both fixed. Security review found no High/Medium issues (confirmed the restored fail-closed logic has no bypass, confirmed `isKnownBrowser`'s exact-match normalization can't be tricked by whitespace/path/homoglyph games, confirmed the Chrome/Edge-only scope decision is consistently documented with no overclaiming elsewhere) and one **Low** (the `msedgewebview2` entry's doc comment overclaimed verification of WebView2's own origin-enforcement behavior) — fixed by rewording the comment to state plainly that this entry carries the same name-recognition-only trust level as every other list entry, no stronger.
**Test coverage:** `nmlauncher_test.go`'s `TestVerifyLaunchedByBrowser_UnrecognizedParent_FailsOpenWithWarning` reverted to `..._Blocked`, now asserting `Allowed: false`/`Warn: false`. `TestIsKnownBrowser`'s table extended to cover every new allowlist entry across all three platforms, plus an explicit `brave.exe → false` case documenting the scope decision as a tested contract, not just a comment. `TestVerifyLaunchedByBrowser_InconclusiveCheck_FailsOpen` and `TestVerifyLaunchedByBrowser_SkipEnvVar_AlwaysAllowed` unchanged (still correct). **Verified 2026-08-04 with a real toolchain: `go build ./...`, `go vet ./...` clean; `go test ./...` clean across the full `eami-agent` module.** Re-verified live end-to-end, not just via `go test`: rebuilt `eami-agent.exe`, refreshed the hard-linked launcher in the live local test registration, confirmed a non-browser parent is now refused (exit 1, same as the original pre-weakening behavior), confirmed `EAMI_NM_SKIP_PARENT_CHECK=1` still bypasses correctly, and ran a full native-messaging protocol round trip through the real `docker compose` stack with the override set — got `{"status":"ok"}` back and confirmed the event landed correctly in `paste_events` (B-035's table, not the old `endpoint_reports` blob path), proving the whole B0→B1→B-035 pipeline still works with this fix in place. Also pointed the live test registration's `CollectorUrl` at `127.0.0.1` instead of `localhost` (a test-environment tweak, not a code change) to route around this machine's known IPv6-first-localhost networking quirk (see B-035's completion notes) during verification.
**Known limitations:** the exact original failure remains formally undiagnosed (see above) — this is a real, acknowledged gap in certainty, not swept under the rug. The allowlist is deliberately scoped to Chrome/Edge only; any other Chromium-family browser still requires `EAMI_NM_SKIP_PARENT_CHECK=1` to work with this native-messaging host. macOS/Linux allowlist entries remain researched-but-unverified on a live host of those platforms.

---

## eami-browser-extension
**Purpose:** Manifest V3 Chrome/Edge extension (B1) — the browser-side half of the paste-detection epic (B-032 backend, B-034/B0 native-messaging host, this task). Detects paste events on 6 known AI-tool web UIs and relays destination-domain/timestamp/content-length/content-hash to B0 via native messaging. Known-destination detection only, no content classification/DLP.
**Status:** STABLE, first version, **fully manually verified** (2026-08-03) — all 5 steps of `MANUAL_TESTING.md` passed live, by the user personally, on a real Chrome/Edge install (extension ID confirmed correct, scoped permissions confirmed, paste capture + no-raw-content confirmed on an allowlisted domain, non-allowlisted domain confirmed silent, service-worker-restart durability confirmed, and `flush()` confirmed sending and landing in `endpoint_reports`). No live production install yet in the deployment sense — this is still local unpacked-loading only; enterprise force-install policy configuration is documented but not configured (out of this task's scope).
**Key files:** `manifest.json` (MV3, `key`-pinned stable extension ID), `domains.js` (bundled allowlist, mirrors `paste_domains.go`'s `KnownPasteDestinations` exactly), `content-script.js` (paste capture + client-side hashing), `background.js` (durable buffer + native-messaging flush), `scripts/generate-key.sh`, `README.md`, `MANUAL_TESTING.md`.
**Interfaces:** native messaging only, to B0's `com.eami.agent` host — never a network call, never directly to `eami-api`. Message shape matches `eami-agent/internal/nativemsg.PasteEventMessage` exactly: `{type:"paste_event", destination_domain, occurred_at, content_length?, content_hash?}`.
**Data owned:** none server-side (stateless relay); locally, `chrome.storage.local`'s `eami_paste_buffer` key holds the durable send buffer (never the raw pasted text).
**Extension ID:** `ngmdfnecljeoleiancdedbmhjdihaoaa` — deterministic, derived from an RSA public key embedded in `manifest.json`'s `key` field (SHA-256 of the DER-encoded key, first 16 bytes, hex nibbles mapped a–p; this is Chrome's own documented algorithm, not a guess). Computed two independent ways (a shell/`openssl` script and an independent Go re-implementation) that agreed exactly. Stable regardless of install path or eventual Chrome Web Store publication (which assigns its own separate ID and doesn't read `key` at all). This same ID now replaces the `PLACEHOLDER_EXTENSION_ID_REPLACE_AFTER_B1` value in `eami-agent/internal/nmregister/nmregister_windows.go`'s `AllowedExtensionID` constant and the Linux/macOS postinstall scripts' embedded manifests.
**Design decisions:**
- **Bundled domain allowlist, not fetched** — no backend endpoint exists to serve it (`eami-api` frozen for this task, and no such endpoint exists regardless), and this matches the established convention of every other domain/extension allowlist in this codebase (`network_activity.KnownAIHosts`, `browser.knownExtensions`, `paste_domains.go` itself) — updated by hand with releases, documented as a manual-sync requirement (no automated enforcement that the three lists stay identical — a real, acknowledged gap, not hidden).
- **`chrome.alarms`, not `setInterval`**, for the periodic flush (1-minute period — Chrome's documented minimum for both packed and unpacked extensions since ~M120) — `setInterval` does not survive Manifest V3 service-worker suspension.
- **One native-messaging `Port` connection per flush, not one per event** — `chrome.runtime.sendNativeMessage`'s one-shot connect+send+disconnect would spawn a new `eami-agent-nmhost.exe` process per paste; batching through one `Port` avoids that.
- **Raw pasted text never leaves `content-script.js`**: `event.clipboardData.getData('text/plain')` is read into a local variable used only to compute `.length` and a `crypto.subtle.digest('SHA-256', ...)` hash, then goes out of scope. Every message-passing boundary (content script → background, background → native host) reconstructs a closed object literal with exactly the allowed fields — even a compromised/buggy upstream sender couldn't smuggle an extra field through, mirroring B-022/B-032's "closed field set, not a pass-through" convention. Verified in the actual shipped code by a mandatory security review, not just asserted by design (matching B-022's own standard).
**Verification — the native-messaging→backend leg, fully live and real:** with B0 re-registered using the real extension ID above, a message with the exact JSON shape `background.js` produces was sent through the **real, registered** `eami-agent-nmhost.exe` — invoked with **no `--config` flag**, relying entirely on the registry-based config fallback (`HKLM\SOFTWARE\EAMI\Agent`, ADR-014) exactly as a real MSI install would populate it, not a test-only YAML file — into a **real** `docker compose` Postgres/`eami-collector`/`eami-api` stack, and confirmed landing correctly in `endpoint_reports.report`'s `paste_events` array (`destination_domain`, `occurred_at`, `content_length`, `content_hash` all present and correct). This is the single riskiest, most novel integration point in this task and it is proven end-to-end with real code, not simulated.
**Verification — the in-browser leg (paste capture, injection scoping, storage durability):** **no browser-automation tool was available in this environment** (checked, confirmed absent). A first attempt to drive a real Chrome/Edge instance via `chromedp` (a genuine Go CDP library, fetched and initially working) was abandoned after it surfaced that this session's shell runs elevated, causing Edge/Chrome to silently de-elevate-relaunch themselves in a way that broke reliable flag/profile passthrough for automation — and, while investigating that, a blanket `taskkill /F /IM msedge.exe` was run that very likely killed the user's own real, unrelated Edge session, not just test instances (confirmed after the fact that none of the machine's running `chrome.exe` processes were test-created; Edge should restore its session on next launch via normal crash-recovery, but this was a real, avoidable mistake, disclosed to the user directly rather than glossed over). All further live-browser automation was stopped at that point. Instead: the extension's JS was syntax-validated with a real (pure-Go, zero-browser-risk) JS parser (`goja`), and every file was carefully read line-by-line against Chrome's documented MV3 API contracts (isolated-world execution, `chrome.storage.local` persistence semantics, `chrome.alarms` scheduling, native-messaging `Port` ordering guarantees cross-checked against B0's actual `RunHost` implementation) rather than assumed. A `MANUAL_TESTING.md` checklist is provided for the parts that genuinely need a human clicking through a real browser, honestly scoped to only what wasn't otherwise verifiable here.
**Reviewer + security subagent pass (mandatory, new browser-facing attack surface):** both ran independently. **Security review:** no-raw-content guarantee holds in the actual shipped code (traced, not assumed); permission scope (`storage`/`alarms`/`nativeMessaging` + 6 exact `host_permissions`, no `<all_urls>`) matches what the code does; one LOW-severity note (a compromised/XSS'd allowlisted page could dispatch a synthetic `paste` DOM event to forge fake telemetry — requires an attacker to already control JS execution on a trusted third-party AI-tool domain, a precondition far more severe than anything this extension introduces; not fixed, judged out of proportion given the precondition). **General code review** found one real, fixed issue: `background.js`'s original `chrome.storage.local` read-modify-write pattern (`bufferEvent`/`flush`) had a genuine lost-update race — two concurrent paste events (or a paste racing an in-flight flush) could both read the same pre-update buffer and the second `set()` would silently clobber the first's append, contradicting the file's own "never lose a buffered event" goal. Fixed with `runExclusive`, a promise-chain-based serialization queue around every storage critical section (the slow native-messaging round trip itself stays outside the lock, so it doesn't block new paste events from being durably buffered while a flush is in flight). Also confirmed correct, not just plausible: the RFC3339 timestamp compatibility between JS's `toISOString()` (always includes a milliseconds fraction) and Go's `time.Parse(time.RFC3339, ...)` (verified via direct Go execution that it accepts the fractional component despite `time.RFC3339`'s layout constant not declaring one), and the strict FIFO response-ordering assumption in `background.js`'s batch-ack logic (verified against `nativemsg.go`'s actual single-goroutine, fully synchronous `RunHost` loop).
**Test coverage:** JS syntax-validated with a real parser (`goja`, zero browser risk); no unit-test framework wired up for this extension (matches this codebase's existing convention — `eami-ui` has none either, per `CLAUDE.md`) — explicitly a nice-to-have per this task's own brief, not blocking. The native-messaging→backend leg has the strongest verification of anything in this task (real, live, traced into a real database).
**Manual verification, completed (2026-08-03):** the `MANUAL_TESTING.md` checklist handed off with this task's original completion report was run to completion by the user personally, in a real Chrome/Edge install — not simulated, not delegated. All 5 steps passed, including step 5 (`flush()`) confirmed sending and the event confirmed landing in `endpoint_reports` in the real Docker Postgres stack, closing the one leg (`goja`-syntax-checked/code-reviewed only, per the note above) that couldn't be automated when this extension was originally built. **Two real bugs were found and fixed during this pass, neither caught by the original build's reviewer/security passes because both required a genuine browser + genuine OS-level native-messaging registration to surface:**
1. Step 5 first failed *silently* — `flush()` ran with no error, but the buffer stayed unsent. Root cause was environmental, not a code defect in this extension: `eami-agent`'s native-messaging host wasn't currently registered on the test machine at all (unregistered as part of this task's own prior end-to-end verification cleanup, never re-registered). While diagnosing, a real code gap was also found and fixed: `background.js`'s `sendBatch` (`port.onDisconnect`) previously discarded `chrome.runtime.lastError` entirely with no logging, so a failed native-messaging connection attempt (host not registered, or any other connection failure) was indistinguishable from a clean no-op flush of an empty buffer — pure silence either way. Fixed by logging the error via `console.warn` when present; retry/buffering semantics are unchanged, this only adds visibility for anyone actually watching the console.
2. After re-registering B0 and reloading, step 5 failed a second time with a *different* error — Chrome's generic `"Error when communicating with the native messaging host."` This was a real bug in `eami-agent`'s `internal/nmlauncher` (B0's own parent-process defense-in-depth check, not this extension's code): its hardcoded Chrome/Edge process-name allowlist didn't recognize the parent process of the user's real, legitimate browser invocation, so the check refused to start the host (exit 1, zero response bytes ever written) — exactly the failure signature Chrome surfaces as that generic error. Fixed in `eami-agent` (not this extension) by changing that branch from fail-closed to fail-open-with-a-warning; see `BUILT.md`'s `eami-agent` section and `BACKLOG.md`'s **B-037** for the full writeup and the explicit follow-up this fix still owes (the current fail-open behavior is an accepted dev-testing convenience, not the final production security posture).
Both fixes were re-verified — first by this session's own direct protocol-level round trip, then by the user's own full live retest of all 5 steps, which is what finally confirmed step 5 end-to-end.
**Known limitations:** no live Chrome Web Store listing (local unpacked-loading / enterprise force-install only, both documented in `README.md`); Firefox/Safari noted, not built (different manifest key names and, for Safari, a wholly different packaging model); enterprise `ExtensionInstallForcelist`/`ExtensionSettings` policy configuration documented, not configured/tested; the `domains.js`/`manifest.json`/`paste_domains.go` triple-list-sync has no automated enforcement (a `NOTES.md`-worthy follow-up, not filed as its own B-ID given its low severity); `ARCHITECTURE.md` (owned by Architect-EAMI per `BOUNDARIES.md`, not edited here) does not yet mention this new top-level component — flagged for Architect-EAMI, not a Code-EAMI change.

---

## eami-collector
**Purpose:** On-prem HTTP server between endpoint agents and the SaaS API. Validates + buffers reports in SQLite (survives outages), forwards batches.
**Status:** WORKING-BUT-FRAGILE — thinnest test coverage in the repo.
**Key files:** `cmd/collector/main.go`; `internal/{api,db,forwarder,models}/`.
**Interfaces:** receives agent `POST /ingest`; forwards to `eami-api` `POST /v1/ingest/batch` and `POST /v1/reports` (service-key auth).
**Data owned:** local SQLite WAL buffer (`report_buffer` table + dead-letter table per ADR-003), not in Postgres `schema.sql`.
**Test coverage:** 1 `_test.go` file (`forwarder_test.go`) vs 8 source files. `ingest.go`, `db`, and `models` have **no tests**. Not executed this session.
**Known limitations:** API-key validation and ingest handler (`internal/api/ingest.go`) are implemented and read as correct, but are unverified by tests — this is the collector's security boundary (agent → collector auth) and has zero test coverage.
- **CRLF crash-loop fixed 2026-07-22 (B-020):** `docker-entrypoint.sh` had Windows CRLF line endings — `#!/bin/sh\r` made Docker's exec look for an interpreter literally named `/bin/sh\r`, crash-looping with `exec /app/docker-entrypoint.sh: no such file or directory`. Stripped to LF; not a missing `COPY`/`chmod +x` issue (Dockerfile was already correct). Verified: `docker compose build eami-collector` clean, container starts and stays up (`eami-collector listening addr=:8888`, forwarder started), no Dockerfile changes needed. **Every other `.sh` file in the repo has the same CRLF issue** (`scripts/*.sh`, `eami-agent/installer/**/*.sh`) — not fixed here (out of scope for this task), see `BACKLOG.md` B-021.

---

## eami-gateway
**Purpose:** MCP control plane. Proxies AI-agent tool calls, enforces policy (via `eami-policy`), routes approvals, writes the audit log, records episodes, issues/validates AI-token JWTs.
**Status:** STABLE (proxy/policy/audit/approval/identity), PARTIAL (episode embeddings — intentional placeholder).
**Key files:** `cmd/gateway/main.go` (orchestration — dispatch, token-usage write, episode-record calls all fire from here, not from `internal/proxy/`); `internal/{mcp,identity,proxy,approval,audit,episode,policyloader}/`. (`internal/node` gossip-mesh package described in `ARCHITECTURE.md` §7 was not confirmed present this session — verify before relying on multi-node/Serf claims.)
**Interfaces:**
- MCP endpoint (stdio + SSE) for AI agents.
- `internal/episode.Recorder.Record(...)` — writes to Postgres `episodes` table (JSONB steps + pgvector embedding column), called via `go episodeRecorder.Record(...)` from `main.go` at all 4 decision outcomes (deny / hold-escalate / forward-error / success).
- `internal/identity` — JWT issuance/validation, revocation list persisted to DB (survives restart, closes JWT-002/TASK-062) with issuer validation (closes JWT-001/TASK-063).
- `internal/audit` — hash-chained writer; DB error on init now propagates instead of silently seeding (closes AUDIT-001/TASK-064).
- pprof endpoint, opt-in via `GATEWAY_PPROF_ADDR` (TASK-061).
- **`internal/episode` read endpoint (added 2026-07-21, branch `b-002-gateway-episode-endpoint`, B-002 Brief 1):** `GET /v1/gateway/episodes` (paginated list, `limit`/`offset`/`outcome` params), `GET /v1/gateway/episodes/search?q=` (ILIKE text search, parity with eami-api's current approach — no vector search yet), `GET /v1/gateway/episodes/{id}` (full episode incl. `steps`). Dual auth via `episode.Handler.authenticateCaller`: (a) `X-Service-Key` against a dedicated `GATEWAY_EPISODE_READ_SERVICE_KEY` (required config, gateway won't start without it) — org-scoped by a **client-supplied** `org_id` query param, trusting the caller (intended: eami-api's Brief 2 proxy) to have already authorized it; (b) `Authorization: Bearer <AI token>` — org resolved server-side via `internal/registry.LookupByName`, never from a client param. `internal/episode/store.go`'s `episodeStore` interface + `pgxEpisodeStore` mirrors the `audit.WriterDB`/fake-seam test pattern. Full unit test coverage in `reader_test.go`/`http_test.go` (18 tests), including the security-critical forged-org_id-ignored and cross-org-404-not-403 cases.
**Data owned:** `episodes`, `audit_log` (+ monthly partitions `audit_log_2026_08`…`_12`), `revoked_ai_tokens`, `gateway_nodes`/`gateway_node_metrics` (schema-level; write path not confirmed this session).
**Test coverage:** 5 `_test.go` files vs 17 source files (post Brief 1). Includes `TestManager_Validate_RevokedToken_SurvivesRestart`, `TestManager_Validate_WrongIssuer_ReturnsError`, `TestWriter_DBErrorOnInit_PropagatesError`, plus the new `episode` package's 18 tests. **Verified 2026-07-22: `go build ./...` and `go test ./... -v` both clean, 0 failures — 10/10 `audit`, 18/18 `episode` (incl. the two security-critical cases: forged `org_id` ignored on the bearer path, cross-org `GetByID` → 404 not 403), 12/12 `identity`, 10/10 `proxy`. Go is installed at `C:\Program Files\Go\bin\go.exe` on this machine but wasn't on `PATH` for the shell sessions used earlier in bootstrap — see `CLAUDE.md`'s toolchain note.**
**Known limitations / landmines:**
- **Episode embeddings are a deterministic SHA-256-derived placeholder** (`internal/episode/recorder.go`, `placeholderEmbedding()`), explicitly commented as pending ADR-009 (LLM endpoint choice, still open). Not semantically meaningful — vector similarity search over episodes will not return relevant results until this is swapped.
- **ADR-010/ADR-019 conflict — fully resolved 2026-07-22 (B-002, all 3 briefs merged to master: `3eab113`, `adcd3e9`, `292d6a4`).** Full episode content stays on-prem in this package; `eami-api` no longer stores or queries it directly (`memory.go`/`store/episodes.go` deleted in Brief 3) — the only path to full episode content is `eami-api`'s org-isolated proxy calling this endpoint.
- **This endpoint's service-key path still trusts a client-supplied `org_id` with no independent check of its own** — by design, that check now lives entirely in `eami-api`'s proxy (`gateway_episodes.go`'s `checkOrgID`), which is built, merged, and verified. Residual risk is narrower than originally scoped: not "no enforcement exists" (it does, in `eami-api`) but "this endpoint has none of its own if reached directly, bypassing `eami-api`." See `BACKLOG.md` B-015 (reframed as a standalone network-hardening item, not a B-002 blocker) — do not provision `GATEWAY_EPISODE_READ_SERVICE_KEY` anywhere a caller other than `eami-api`'s proxy could use it.

**Interfaces — B-025 (2026-07-24), config validation closes a live auth-bypass:** `internal/config/config.go`'s existing `validate()` (added for B-002 Brief 1's `API.EpisodeReadServiceKey` check) never checked `API.ServiceKey` (env `GATEWAY_API_SERVICE_KEY`) at all — the value the gateway sends outbound as `X-Service-Key` on its calls to `eami-api`. An unset env var meant this stayed whatever `eami-gateway.yaml` shipped (`service_key: "changeme"`), which — since `eami-api`'s own `requireServiceKey` had the exact same unvalidated-default bug (see the `eami-api` section above) — could mean both sides of the internal call were quietly using the literal string `"changeme"`. `validate()` now also rejects `cfg.API.ServiceKey` (empty or a known placeholder) and strengthens the pre-existing `cfg.PostgresDSN == ""` check to also reject an empty/placeholder-password DSN, via the same `isPlaceholderSecret`/`dsnHasPlaceholderPassword`/`dsnPassword` helpers added to `eami-api`'s config.go (duplicated, not shared — separate Go modules under `go.work`). `cfg.API.EpisodeReadServiceKey`'s existing required-check is untouched, per this task's explicit scope boundary. See the `eami-api` section above for the full writeup (security-review-caught CRLF/whitespace bug in the first version of `dsnHasPlaceholderPassword`, test coverage, and the live `docker compose` verification, all of which covered both modules together in one pass) — not duplicated here.
**Test coverage — B-025:** new `eami-gateway/internal/config/config_test.go` (10 tests, package had none before) — same structure as `eami-api`'s: `validate()`-level placeholder/empty/real-value cases for `API.ServiceKey` and `PostgresDSN` (including the CRLF/leading-space regression cases and a `TestValidate_AppliesDefaults` regression guard for the pre-existing default-application logic), plus `Load()`-level integration tests (`TestLoad_RejectsUnsetAPIServiceKey`, `TestLoad_RejectsPlaceholderDBPassword`, `TestLoad_AcceptsRealSecrets`). **Verified 2026-07-24 with a real toolchain: `go build ./...`, `go vet ./...`, `go test ./...` all clean, 0 failures** (full module, including `audit`/`episode`/`identity`/`proxy` — unaffected by this change).

**Interfaces — B-027 (2026-07-25), panic recovery for 3 background goroutines:** none of `internal/approval.Router.Run` (the LISTEN/NOTIFY loop), the fire-and-forget token-usage writer, or `internal/episode.Recorder.Record` had panic recovery — an unrecovered panic in any of them would crash the whole `eami-gateway` process, since none of them run inside an HTTP request path (the only place with existing recovery, via Go's stdlib per-connection handling). New `internal/safego` package (`safego.go`): `Guard(name string, fn func())` recovers a panic, logs it (`slog.Error`, with goroutine name, panic value, and a `runtime/debug.Stack()` trace), and returns normally — never re-panics; `GuardErr(name string, fn func() error) error` is the same thing for callers whose loop body already returns an error (converts a recovered panic into an error instead, so existing retry/reconnect logic can treat "panicked" the same as any other failure). Used at 4 points across 3 packages:
- `internal/approval/router.go`: the per-notification body inside `listenLoop`'s `for { WaitForNotification → resolve } ` loop was extracted into `Router.handleNotification(ctx, payload)`, wrapped in `safego.Guard` — a panic resolving one approval (e.g. `resolve`'s `v.(*pendingEntry)` type assertion, if a future bug ever stored the wrong type) is logged and the loop keeps waiting for the next notification, instead of tearing down the LISTEN connection. `Router.Run`'s outer reconnect loop now calls `listenLoop` via `safego.GuardErr` — a panic anywhere else in `listenLoop` (outside the per-notification loop, e.g. during `LISTEN` setup) converts to an error and is handled by the exact same reconnect-with-5s-backoff logic that already handles a returned error, no new code path.
- `cmd/gateway/main.go`: the token-usage writer isn't a persistent loop — it's a `go func(){...}()` fired fresh from the dispatch closure on every successful tool call. Extracted into `safeWriteTokenUsage(apiBaseURL, apiServiceKey, tu)`, called via `go safeWriteTokenUsage(...)`, wrapping the write in `safego.Guard`. The actual HTTP POST logic in `writeTokenUsage` is completely unchanged; it's now called through a package-level `tokenUsageWriteFunc = writeTokenUsage` var so tests can substitute a panicking stand-in (production always uses the real function — this mirrors the existing `toolDialOverride`/`toolStoreOverride` test-seam convention already established in `eami-api`, not a new pattern).
- `internal/episode/recorder.go`: also not a persistent loop — `go episodeRecorder.Record(...)` is fired from 4 separate call sites in `main.go`'s dispatch closure (deny / escalate / proxy-error / success outcomes). Rather than wrapping all 4 call sites, `Record`'s own body is wrapped in `safego.Guard` (its original body renamed to an unexported `record`) — one change covers every current and future caller. To make this testable without a live Postgres connection, added `episodeWriteDB` (an `Exec`-only interface) and changed `Recorder`'s field from a concrete `*pgxpool.Pool` to that interface — this mirrors the same package's own pre-existing `episodeStore`/`pgxEpisodeStore` read-side split (`store.go`), not a new convention. `New(pool *pgxpool.Pool) *Recorder`'s public signature is unchanged.
No change to any of the three targets' actual business logic (approval resolution, token-usage POST construction, episode DB insert) or their existing non-panic error handling (`slog.Warn`/`slog.Error` on expected failures) — confirmed via diff: `router.go` (+24/-9), `main.go` (+16/-7 net across the writer extraction), `recorder.go` (+31/-4), all wrapper/extraction only.
**Test coverage — B-027 (gateway):** `internal/safego/safego_test.go` (9 tests covering `Guard` and `GuardErr`: panic doesn't propagate, no-panic runs normally, panic value/name/stack land in the log, a panicking call doesn't block a subsequent call from completing, non-string panic values, `GuardErr`'s panic-to-error conversion and pass-through of a normal non-nil/nil error). `internal/approval/router_test.go` (3 tests): `TestHandleNotification_PanicResolvingOneApproval_DoesNotCrash` triggers `resolve`'s real type-assertion panic by storing a non-`*pendingEntry` value under an approval ID directly (a genuinely reachable panic path, not a synthetic one) and proves `handleNotification` returns cleanly; `TestHandleNotification_NextNotification_StillProcessesNormally` proves a second, unrelated notification is still handled normally right after; `TestHandleNotification_MalformedPayload_StillHandledCleanly` is a regression guard for the pre-existing malformed-JSON/missing-id paths (no panic risk, unchanged behavior). `cmd/gateway/main_test.go` (3 tests, this package had zero tests before): panic in `tokenUsageWriteFunc` doesn't crash `safeWriteTokenUsage`, a subsequent call with a normal function still performs a real write (asserted via the payload it receives), and the no-panic path is unchanged. `internal/episode/recorder_test.go` (5 tests, new `fakeEpisodeWriteDB`): panic in `Exec` doesn't crash `Record`, a subsequent `Record` call still reaches a real `Exec` call afterward, no-panic behavior unchanged, and the pre-existing empty-`steps`-slice early-return (no DB call at all) is unaffected. **Verified 2026-07-25 with a real toolchain: `go build ./...`, `go vet ./...`, `go test ./...` clean, 0 failures**, full module — all pre-existing packages (`audit`, `config`, `identity`, `proxy`, and `episode`'s own pre-existing `http_test.go`/`reader_test.go`, 18+ tests) confirmed unaffected. General code-review pass (one pass covering both `eami-api` and `eami-gateway` diffs together): clean — confirmed per-iteration recovery at every required granularity (not just recover-once-at-goroutine-start), no goroutine leaks/races/deadlocks from the new closures, `ctx.Done()`/shutdown paths in `Router.Run` untouched, no sensitive data (tokens/secrets) newly reachable via panic-value or stack-trace logging, and all new test seams (`tokenUsageWriteFunc`, `episodeWriteDB`, the bogus `pending.Store` value in `router_test.go`) confirmed confined to `_test.go` files.
**Known limitations:** none — this closes the panic-recovery gap for all 4 identified background/ticker goroutines across both services; HTTP request-path panic handling (already correct) was explicitly out of scope and untouched.

**Interfaces — B-039 (2026-08-04), the approval flow actually works end-to-end for the first time:** an original module audit found the human-in-the-loop escalation flow — this codebase's flagship AI-governance mechanism — had never completed a single real cycle. Re-verified all three claimed root causes directly against current code before touching anything, per the task's own instruction, and all three still held exactly as described: (1) `internal/approval/router.go`'s `Submit()` did a raw `INSERT INTO approval_requests` omitting five real `NOT NULL` columns (`justification`, `risk_level`, `expires_at`, `gateway_session_id`, `gateway_node_address`) — failed every time with a constraint violation; (2) `Hold()`/`resolve()` queried/updated columns `decision`/`reason`, which don't exist on `approval_requests` at all (real columns: `status`/`decision_reason`) — plausible root cause noted: `audit_log`, a *different* table in the same schema, has its own column literally named `decision` with `CHECK (...IN ('allowed','denied','escalated'))` — the exact name and vocabulary this code mistakenly used, suggesting the two tables' schemas were confused during the original implementation, not a random typo; (3) `resolve()`'s switch checked for status `"allowed"`, but `eami-api`'s `DecideApproval` (independently re-confirmed correct and unchanged, per the audit's assumption) only ever writes `"approved"`/`"denied"`.
**Fix confined to `router.go` alone, per an explicit scope decision** (not threading the policy engine's real per-rule escalation reason or the gateway's configured listen address in from `cmd/gateway/main.go`): `justification` is synthesized from `req.Tool`/`req.Action` (truthful — every escalation genuinely matched some policy for that exact tool/action — just less specific than the real per-rule reason would be); `risk_level` defaults to `"medium"` (no risk-classification concept exists anywhere in the policy/schema today, confirmed by inspection of `policies`/`policy_conditions`, not assumed — see `BACKLOG.md` for the new follow-up item this gap is tracked under); `gateway_node_address` uses `os.Hostname()` rather than `cfg.ListenAddr`; `gateway_session_id` reuses `req.SessionID` (already present, just never wired into the INSERT); `expires_at` is `now() + r.holdTimeout`. `Hold()`/`resolve()` now reference `status`/`decision_reason` throughout, and `resolve()` checks for `"approved"` (not `"allowed"`). A closely related bug found in the same pass, on the same line, was fixed too: `Hold()`'s timeout path wrote `status='denied'` for a plain timeout, when the schema has a distinct `'expired'` value and the code's own preexisting comment already said the intent was "so eami-api knows the request expired" — changed to write `'expired'`, and `resolve()`'s switch now explicitly handles it (blocks the same as `denied`, since anything other than an explicit approval must never let the original action through).
**A fourth, previously-unknown root cause was found and fixed** — outside `router.go`, in `internal/mcp/handler.go`, only after confirming with the user this needed to expand beyond the stated file scope: `ServeMessages` responds `202 Accepted` immediately, then runs the actual policy-eval/`Submit()`/`Hold()` dispatch in a detached `go func()` — but that goroutine was passed `r.Context()`, the *HTTP request's* context, which Go's `net/http` cancels the instant `ServeHTTP` returns (i.e., right after the 202 write, before `dispatch` has any real chance to run). Every escalation's `Submit()` INSERT was therefore racing against its own context being torn down and losing almost every time — a real, separate reason the flow had never completed a cycle, on top of the three already known. Fixed with `context.WithoutCancel(r.Context())` (Go 1.24, this module's version) so the detached goroutine survives the request's own lifetime while still carrying any context values; `dispatch`'s own internal timeouts (`Hold`'s `holdTimeout`) remain the real bound on how long it can run.
**Reviewer + security subagent passes, both mandatory (this is the human-in-the-loop control for AI governance decisions):** security review found no High/Medium bypass — walked every branch of `resolve()`'s switch and confirmed no non-`"approved"` status ever forwards the action, confirmed the schema's `status` `DEFAULT 'pending'` can never resolve to an implicit approval, confirmed full SQL parameterization throughout. One real **Medium**, fixed: a genuine timing race at the exact `Hold()` timeout boundary could silently drop an already-approved action (report a bogus timeout and never forward it, though the DB itself was never left wrong) — fixed by having the timeout path do one final non-blocking channel check, then (if still timing out) a conditional `UPDATE ... WHERE status = 'pending' RETURNING ...`; if that matches zero rows (the row was already decided in the race window), the real decision is fetched and honored via a new shared `outcomeFromStatus` helper (also now used by `resolve()`, so the two paths can never diverge on how a status is interpreted) instead of overwriting it with a stale `'expired'`. One **Low**, fixed: `notifySlack`'s message text interpolated `AgentName`/`Tool`/`Action`/`SessionID` raw — fields that can originate from a compromised caller — into Slack mrkdwn, which treats `&`/`<`/`>` specially (`<url|label>` link syntax); a malicious value could have injected a fake link or misleading text into what a human approver reads before making a judgment call. Fixed with `escapeSlackText` (Slack's own documented escaping rules), applied only to the four interpolated fields — the message-sending mechanics (client, request, goroutine) are unchanged, per the task's own constraint. Code review separately caught and this session fixed: `nilIfEmpty(req.OrgID)`/`nilIfEmpty(req.AgentID)` silently turned an empty string into SQL `NULL` for two columns that are actually `NOT NULL` FKs — would have reproduced this exact bug class if either was ever empty, untested by the original fix's own test suite. Fixed with an explicit early validation in `Submit()`; `nilIfEmpty` had no other caller and was deleted. Also fixed a stale doc comment on `Hold()` that still described the pre-fix `"allowed"`/`"auto-denies"` behavior, contradicting the code in the same file.
**Test coverage:** new `internal/approval/router_pg_test.go` — a deliberate departure from this package's usual fake-DB convention (`router_test.go`'s existing tests), because the three original bugs (a real `NOT NULL` violation, queries against columns that don't exist, a vocabulary mismatch against a real `CHECK` constraint) are exactly the class of bug a fake/mock DB can never surface. 9 integration tests against a real Postgres: `Submit()` writes a row satisfying every `NOT NULL`/`CHECK` constraint; `Hold()`'s timeout path writes `'expired'` via the real columns; `resolve()` correctly forwards on `"approved"` (via a real fake-downstream `httptest.Server` through `proxy.Forward`) and blocks on `"denied"`/`"expired"`; two new tests specifically proving the timeout-race fix (an already-`"approved"`/already-`"denied"` row is honored, not clobbered with a stale `'expired'`, when `Hold()`'s own timeout path is the only thing that would otherwise notice). Plus `TestEscapeSlackText` in `router_test.go`. **Verified 2026-08-04 with a real toolchain: `go build ./...`, `go vet ./...` clean; `go test ./...` clean across the full `eami-gateway` module** (not just the new tests) **and `eami-api` confirmed unaffected**.
**Live, end-to-end verification against the real running `docker-compose` stack (not just unit-tested in isolation) — every acceptance criterion proven, not asserted:** rebuilt and redeployed both `eami-gateway` and `eami-api` first (this session's own standing lesson about stale images, applied proactively). Seeded a real org/agent/ESCALATE policy/admin user directly in Postgres (firing `pg_notify('policy_reload', '')` manually, since a direct SQL seed doesn't trigger the reload `eami-api`'s real policy-create endpoint would), obtained a real signed agent JWT from the real `POST /v1/gateway/tokens`, opened a real SSE session, and POSTed a real JSON-RPC `tool_call` matching the seeded policy. **AC1**: a real `approval_requests` row appeared (`Submit()` no longer fails). **AC2**: confirmed visible via a real admin login + the real `GET /v1/approvals/{id}` (the exact endpoint the Approvals UI calls). **AC3**: called the real `POST /v1/approvals/{id}/decide {"decision":"approved"}` and confirmed, via a temporary local fake downstream MCP server (`downstream_sse_addr` pointed at it for this test only, via a `docker-compose.override.yml` + volume-mounted config, both removed afterward), that the gateway's real LISTEN/NOTIFY → `resolve()` → `proxy.Forward` chain delivered the forwarded result over the real SSE stream. **AC4**: a second real escalation, denied via the same real endpoint, confirmed blocked — an `error` JSON-RPC response delivered via SSE, zero additional hits on the fake downstream. **AC5**: Slack notification confirmed reached (a temporary local fake webhook receiver, `GATEWAY_APPROVAL_SLACK_WEBHOOK` pointed at it for this test only, since this environment's real configured webhook was deliberately left untouched rather than posting a live test message to a real Slack workspace) — received the correctly-formatted, correctly-escaped notification for both escalations. **AC6**: all of the above ran against the real deployed containers, not a test binary. A container-recreation mistake made during test-environment setup (a stray anonymous Docker volume left an empty config file at the gateway's config path, briefly breaking startup) was caught and fixed transparently before finishing, and the real gateway image was confirmed to redeploy and run cleanly afterward with none of the temporary test-only overrides.
**Known limitations:** `risk_level` remains a hardcoded `"medium"` default pending a real risk-classification design (new backlog item, see `BACKLOG.md`); `justification` is synthesized, not the real per-rule policy reason (documented tradeoff, in scope to revisit if `main.go`'s dispatch closure is ever touched for other reasons). **Unrelated bug discovered incidentally during live verification, fixed separately as B-041 below (was out of scope for this task): `internal/identity`'s revocation-list DB hydration on startup queried a column (`expires_at`) that doesn't exist on `revoked_ai_tokens`** — see B-041.

**Interfaces — B-041 (2026-08-04), JWT revocation persistence actually works: both hydration AND the write path it depends on:** `internal/identity/tokens.go`'s `dbRevocationStore` (the Postgres-backed revocation store used by `NewManagerWithDB`, the constructor `cmd/gateway/main.go` actually calls in production) referenced a column, `revoked_ai_tokens.expires_at`, that has never existed on that table — confirmed by reading `schema.sql` and every file in `schema/migrations/`. Two independent breakages from the same root cause, both fixed: (1) `loadAll`'s startup-hydration `SELECT ... WHERE expires_at > NOW()` errored on every single gateway startup (`column "expires_at" does not exist`, SQLSTATE 42703) — caught and downgraded to a bare `slog.Warn`, so the gateway always started with a silently-empty in-memory revocation set; (2) `save`'s `INSERT INTO revoked_ai_tokens (jti, expires_at) VALUES (...)` — `Revoke()`'s only DB side effect — also referenced the same nonexistent column *and* omitted the real `agent_id` column, which is `NOT NULL` with a FK to `gateway_agents`; every DB-backed revoke had therefore always silently failed to persist (logged via `slog.Error`, never surfaced to any caller). Net effect: nothing had ever actually reached `revoked_ai_tokens` for hydration to load in the first place, so fixing the SELECT alone (the bug as originally reported after B-039) would not have closed the loop — confirmed by investigation before writing any fix, then fixed both together with the user's explicit sign-off to expand scope beyond the original "hydration query" framing.
**Real schema, revocation is permanent by design:** `revoked_ai_tokens (jti TEXT PK, agent_id UUID NOT NULL REFERENCES gateway_agents(id), revoked_at TIMESTAMPTZ DEFAULT NOW(), reason TEXT)` — no expiry column at all, unlike `sessions`/`approval_requests` elsewhere in the same schema, which do have real `expires_at` columns. `loadAll` is now the unfiltered `SELECT jti FROM revoked_ai_tokens`; `save` is now `INSERT INTO revoked_ai_tokens (jti, agent_id) VALUES ($1, $2) ON CONFLICT (jti) DO NOTHING`.
**Contract change:** `revocationStore.save(ctx, jti, expiresAt time.Time)` → `save(ctx, jti, agentID string)`; `Manager.Revoke(jti string)` → `Revoke(jti, agentID string)`. Confirmed via repo-wide grep `Revoke()` has **zero production callers today** — the `POST /v1/gateway/tokens/{token}/revoke` route `api/openapi.yaml` documents was never wired into `main.go`'s router — so this signature change is safe (only `tokens_test.go`/new `tokens_pg_test.go` call it) and wiring that route is filed as **B-042**, not built here.
**A real, non-obvious gotcha found and closed during this task's own live-verification pass, not left for B-042 to rediscover the hard way:** `Claims.Subject` (the JWT `sub`) is `"agent:<name>"` — `internal/registry`'s documented convention, the same value `internal/mcp`/`internal/episode` already resolve via `registry.LookupByName(strings.TrimPrefix(claims.Subject, "agent:"))` before using it — **never** a `gateway_agents.id` UUID directly. `Revoke()`'s `agentID` parameter needs the real UUID; a naive `Revoke(claims.ID, claims.Subject)` (the obvious-looking call, and initially what this task's own plan proposed) would fail the FK insert on every real revoke, reproducing this exact bug class via a new trigger. `Revoke()`'s doc comment now states this explicitly with a pointer to the existing resolution pattern; `tokens_pg_test.go`'s restart-simulation test deliberately issues a token in the real `"agent:<name>"` shape and resolves it itself (rather than taking the UUID-shaped shortcut an earlier draft of the test used) so a future regression on this exact point would be caught.
**Hydration failure is now fatal, not a silent warning:** `newManager()` returns the `loadAll` error instead of catching it and logging `slog.Warn`; `main.go`'s single production call site (`identity.NewManagerWithDB`, line 96) already treats a returned error as fatal (`return fmt.Errorf("identity init: %w", err)`, propagating to `main()`'s `os.Exit(1)`) — this reuses existing error-propagation plumbing, no new fail-fast mechanism was added. `NewManager` (file-backed, dev/test path) gets the same propagation for consistency; its own `loadAll` only errors on a genuine read failure (a missing file is still `nil, nil`, unaffected).
**Doc-comment corrections, in the same file being touched, not left stale:** `tokens_test.go`'s header and two per-test comments claimed `TestManager_Validate_WrongIssuer_ReturnsError` and `TestManager_Validate_RevokedToken_SurvivesRestart` were "CURRENTLY FAILING" (linked to TASK-052/TASK-053) — both were actually already passing (`WrongIssuer` since `Validate()` gained `jwt.WithIssuer()` at some undated prior point; the file-backed `SurvivesRestart` since the file store's `save()`/`loadAll()` never had the DB store's column-name bug in the first place — only `NewManagerWithDB`'s DB-backed path was ever broken, and this particular test only ever exercised `NewManager`'s file-backed path, so it never caught B-041's actual bug). Comments corrected to state this; `t.Fatal` regression-failure messages left untouched (still accurate on an actual regression).
**Reviewer + security subagent passes, both mandatory (credential-revocation control):** both ran clean against this diff specifically — confirmed the real-schema column names, confirmed the hydration-failure-is-fatal propagation has no swallow-point, confirmed no SQL injection (fully parameterized throughout), confirmed no lock-ordering issues in `Revoke()` (releases `revokedMu` before the `store.save()` I/O call), confirmed zero production callers of the changed `Revoke()` signature (so no latent breakage from the signature change itself), confirmed no sensitive data (jti/agent UUID/token/key material) newly reachable via any changed log line. Both independently surfaced and agreed on the same two **PLAUSIBLE, currently-latent** (not exploitable today, since `Revoke()` has no live caller) gaps: `Revoke()`/`save()` still don't return persistence errors to their caller, and `Issue()`/`HandleIssue` accept any caller-supplied `AgentID` with no validation against `gateway_agents` — together meaning a future revoke of a token for a stale/deleted/malformed agent would silently fail the INSERT and silently lose that revocation, reproducing this exact bug class via a new trigger the moment a real HTTP route calls `Revoke()`. Both folded into **B-042**'s acceptance criteria rather than fixed here (no live caller exists yet to make them exploitable, and fixing them meaningfully requires the same design work as the route itself — who's authorized to revoke, what error the caller sees).
**Test coverage:** new `internal/identity/tokens_pg_test.go` (4 tests, following `internal/approval/router_pg_test.go`'s B-039-established real-Postgres pattern — `TEST_DATABASE_URL`/`POSTGRES_PASSWORD` fallback, throwaway `orgs`/`gateway_agents` fixtures via `t.Cleanup`, relying on the schema's own `ON DELETE CASCADE` chain to clean up the resulting `revoked_ai_tokens` row too): `TestDBRevocation_Save_WritesRealRow`, `TestDBRevocation_LoadAll_ReturnsSavedJTIs` (both exercise `dbRevocationStore` directly against the real schema), `TestManagerWithDB_Validate_RevokedToken_SurvivesRestart` (the real restart-simulation test this bug always needed — mirrors `tokens_test.go`'s existing fake/file-store version but through `NewManagerWithDB` against a real pool, using the real `"agent:<name>"`-then-resolved-UUID shape described above), `TestNewManagerWithDB_HydrationFailure_IsFatal` (a pool pointed at an unreachable port, proving `NewManagerWithDB` returns a real error rather than starting in a degraded state). **Verified 2026-08-04 with a real toolchain: `go build ./...`, `go vet ./...`, `go test ./...` all clean, 0 failures across the full `eami-gateway` module** — 17 tests in `internal/identity` alone (13 pre-existing + 4 new), all passing.
**Live, end-to-end verification against the real running `docker-compose` stack, rebuilt fresh first (not a stale image — this task's brief explicitly called out avoiding B-035's earlier mistake here):** Docker Desktop was not running at session start; started with the user's explicit confirmation first, per this session's standing norm around system-state changes. Issued a real token via the real `POST /v1/gateway/tokens` for a real seeded org/`gateway_agents` row; confirmed the already-running gateway process still accepted that token immediately after the DB-level revoke (expected: only the DB row was written, directly via the real `dbRevocationStore.save` code path, since no HTTP route yet exists to call `Manager.Revoke()` end-to-end — see B-042; this isolates exactly what B-041 fixes — persistence + hydration — independent of the not-yet-built route, rather than being a gap in the proof). Confirmed the row landed in `revoked_ai_tokens` via a direct query. Restarted the `eami-gateway` container and confirmed the startup log now reads `"identity: hydrated revocation list" count=1` — the exact spot that previously logged the `WARN` this item exists to close. Confirmed a real `GET /v1/mcp/sse` request bearing that exact token now returns `401 unauthorized: identity: token ... has been revoked` — **AC1, proven end-to-end, not asserted.** Separately, stopped Postgres entirely and restarted the gateway container: confirmed it fails fast and crash-loops (`gateway: fatal: postgres ping: ...`) rather than starting in a silently-degraded state — this specific scenario is actually caught upstream of identity hydration by the pre-existing `pool.Ping` check, but exercises the same end-to-end "fail loud, not silent" contract this fix relies on; the hydration-specific fatal path itself is covered directly by `TestNewManagerWithDB_HydrationFailure_IsFatal` above. All test fixtures (throwaway org/agent, the revocation row, and a scratch-only `_test.go` file used solely to invoke the real revoke code path against this specific live token) were deleted/cleaned up afterward; final `go build`/`go vet`/`go test ./...` re-run clean post-cleanup.
**Known limitation, closed by B-042 below:** the `POST /v1/gateway/tokens/{token}/revoke` route this fix makes finally-viable didn't exist — `Manager.Revoke` had no production caller. See B-042.

**Interfaces — B-042 (2026-08-04), the JWT revocation route is finally reachable over HTTP:** new `POST /v1/gateway/tokens/{jti}/revoke` (`internal/identity/revoke_http.go`, `RevokeHandler`), wired into `cmd/gateway/main.go`. **Two deliberate deviations from `api/openapi.yaml`, investigated and user-approved before building, both documented in the handler's own doc comment, not silent:**
1. **Auth**: `openapi.yaml` documents `security: BearerAuth` for this route, but that scheme is defined elsewhere in the same spec as "JWT (RS256) issued by `POST /v1/auth/login`" — `eami-api`'s user-session JWT, which `eami-gateway` has no code path to validate at all (different issuer/signing key/claims shape). Real precedent found for the *intended* eventual design: `eami-api/internal/api/router.go` gates the structurally-identical `POST/PATCH/DELETE /v1/gateway/agents*` behind `requireRole("admin", "operator")`. Building that full proxy now was considered and explicitly rejected as exceeding this task's scope — it would also require solving an unscoped problem (no "list active tokens" surface exists anywhere for an admin to discover which `jti` to target). **Shipped instead**: gateway-local only, gated by a new `X-Service-Key` — `GATEWAY_TOKEN_REVOKE_SERVICE_KEY`, required/fail-closed at startup via `isPlaceholderSecret` (same treatment as the pre-existing `EpisodeReadServiceKey`, wired through `eami-gateway.yaml`/`docker-compose.yml`/`docker-compose.prod.yml`/`.env.example`) — with a real `eami-api`-hosted, role-gated proxy left as intended future follow-up work, not built here.
2. **Path shape**: `openapi.yaml` names the path param `{token}` (implying the full JWT in the URL). Implemented as `{jti}` instead — just the token's ID — specifically so a live bearer credential is never written into a URL, and therefore never into proxy/access logs, a real leak vector the literal spec shape would have reintroduced.

**Closes B-041's two flagged latent gaps**, both confirmed against the actual review findings before being treated as fixed (not assumed from memory): (1) **subject-format trap** — the handler resolves the request body's `agent_name` via the registry (see gap 3 below for exactly which method), never passing `Claims.Subject`/a raw JWT `sub` string (`"agent:<name>"`) directly to `Revoke`, which needs a real `gateway_agents.id` UUID; (2) **silent persistence-failure swallowing** — `Manager.Revoke(jti, agentID string)` now returns an `error` (was `void`, only `slog.Error`-logging internally) — the one approved exception to `tokens.go`'s otherwise-frozen scope for this task, mirroring `Issue()`'s existing convention of the *function* not logging, only the *HTTP handler* layer doing so. The handler surfaces a real `500` on a persistence failure, never a false `204`.

**A third, more severe gap found during this task's own security review — required to be fixed in-task per explicit user direction, not deferred to a future task the way B-041→B-042 itself was staged:** `registry.LookupByName`'s query (`WHERE name = $1`, no `org_id` filter) is safe for every *existing* caller (`internal/mcp`, `internal/episode`) only because their `agent_name` always comes from a signature-verified JWT `sub`, itself inherently bound to one org at issuance. This new handler is the **first** caller passing a raw, client-supplied `agent_name` with no such cryptographic binding — since `gateway_agents.name` is only unique per `(org_id, name)` (`schema.sql`'s own `UNIQUE` constraint), an Org A caller could otherwise resolve, and cause the permanent-audit-record revocation of, Org B's identically-named agent's token. **Fixed** with a new, purely additive `internal/registry.LookupByNameAndOrg(ctx, name, orgID string)` — direct `WHERE name = $1 AND org_id = $2` query, deliberately uncached (avoids any interaction with `LookupByName`'s existing name-only cache, and this is a low-frequency admin action, not a hot path) — `LookupByName` itself and all its existing callers are completely untouched, confirmed via `git diff` (pure 38-line addition, zero deletions). The revoke request body now requires `org_id` (validated as a well-formed UUID via `uuid.Parse` before any query, 400 otherwise) — caller-supplied and trusted, matching `episode/http.go`'s own already-shipped, already-documented precedent for its service-key path's `org_id` query param, not a new trust-model pattern invented for this task.

**Also correctly preserved, not accidentally dropped in the port:** `registry.LookupByName`'s existing behavior of returning a *populated* `AgentRecord` even when the agent's status is `suspended`/`revoked` (only a genuinely-unresolvable name returns `nil`) — `LookupByNameAndOrg` matches this exactly. The handler deliberately treats revoking a token for an already-suspended agent as a valid action, not an error (an existing session established before suspension may still hold a live, revocable token) — traced and confirmed correct, not assumed.

**Reviewer + security subagent passes ran twice**: an initial pass against the base implementation (service-key auth, gap 1/2 fixes) came back clean on the auth/persistence-error design, and separately flagged three findings — the org-scoping gap above (fixed, see above), the multi-node broadcast gap (real, pre-existing, logged as new **B-043**, not fixed here — out of scope for wiring one HTTP route), and transient-DB-errors-collapsing-to-`403`-with-raw-error-text-in-the-response (confirmed to be a pre-existing pattern already shipped in `episode/http.go`/`mcp/handler.go`, not a regression introduced here — left as-is, matching this codebase's convention of not fixing unrelated pre-existing patterns outside a task's own diff). A focused follow-up pass, run specifically against the org-scoping fix after it was built, came back fully clean.

**Test coverage:** 9 new tests. `internal/identity/revoke_http_pg_test.go` (5 tests, real Postgres, real `registry.Registry`, following B-041's established pattern): `TestRevokeHandler_HappyPath_RevokesRealToken`, `TestRevokeHandler_WrongServiceKey_Returns401` (both wrong-key and missing-key cases, plus confirms the token is untouched), `TestRevokeHandler_UnknownAgent_Returns403`, `TestRevokeHandler_CrossOrgAgentName_Returns403` (creates a real second org, submits the real agent's name with the *wrong* real `org_id`, confirms `403` and the token stays valid — proven together with the happy-path test to demonstrate correct scoping, not a "reject everything" bug), `TestRevokeHandler_PersistenceFailure_Returns500NotSilent204` (a `fakeResolver` test seam returns a fabricated, nonexistent agent UUID that genuinely violates the real `NOT NULL` FK on `INSERT`, proving the `500` path is real, not simulated). `internal/config/config_test.go` gained `TestValidate_RejectsPlaceholderTokenRevokeServiceKey` plus updates to the shared `validConfig()`/`clearSecretEnv()`/`TestLoad_*` fixtures for the new required secret. **Verified 2026-08-04 with a real toolchain: `go build ./...`, `go vet ./...`, `go test ./...` all clean, 0 failures across the full `eami-gateway` module** — `internal/identity` alone now has 26 tests (17 pre-B-042 + 9 new).

**Live-verified end-to-end against the real `docker-compose` stack, rebuilt fresh first (not a stale image):** seeded a real org/`gateway_agents` row, issued a real token via `POST /v1/gateway/tokens`, confirmed it valid via a real `GET /v1/mcp/sse` request. **AC2 (unauthorized rejection)**: a revoke attempt with the wrong `X-Service-Key` returned real `401`, and the token was confirmed still valid immediately after. **AC1 (legitimate revoke)**: a request with the real service key, correct `agent_name`, and correct `org_id` returned real `204`; the row was confirmed present in `revoked_ai_tokens` with the correct `agent_id`; a subsequent `GET /v1/mcp/sse` with that exact token returned `401 unauthorized: identity: token ... has been revoked`. **Went further than the brief's minimum**: restarted the `eami-gateway` container and confirmed this specific HTTP-route-driven revocation survives the restart too (`"identity: hydrated revocation list" count=1`, token still rejected afterward) — proving the full B-041→B-042 chain works together end-to-end, not just each half verified in isolation. All test fixtures (throwaway orgs/agents, the revocation row) deleted/cleaned up afterward; final `go build`/`go vet`/`go test ./...` re-run clean post-cleanup.

**Known limitation, tracked as new B-043, not built here:** `Manager.Revoke()` only updates the in-memory revoked-set on whichever gateway node handles the request — other nodes don't learn of a revocation until their own restart. `schema.sql`'s `revoked_ai_tokens` comment and `NewManagerWithDB`'s own doc comment both claim Serf-based multi-node broadcast; a repo-wide grep found no such mechanism anywhere. Predates B-042 (the gap has existed since multi-node support was first claimed) — B-042 is simply the first task where it became concretely reachable/consequential, since it's the first time `Revoke()` has had a live HTTP caller at all. Also not built here (explicit, approved scope boundary): the real `eami-api`-hosted, role-gated proxy layer (matching `/v1/gateway/agents*`'s `requireRole` precedent) that would be this endpoint's real long-term caller.

**Interfaces — B-044 (2026-08-06), dynamic MCP tool routing for `rest_api`-type `gateway_tools`:** `proxy.Forward` previously forwarded every tool call to one hardcoded static URL (`cfg.Proxy.DownstreamSSEAddr`), regardless of which tool was named — `gateway_tools` (built by B-022/B-023) had zero live consumers anywhere in `eami-gateway`, confirmed by a routing investigation via repo-wide grep. New `internal/toolrouter` package (`Router.Resolve`, org-scoped `gateway_tools` lookup by name; `Router.Forward`, dispatch to a resolved `rest_api` row's real `base_url` using real decrypted credentials) is consulted by `dispatch` (via new `resolveDynamicTool` in `cmd/gateway/main.go`) before policy evaluation and before the existing `ActionAllow` forward branch. **Two of the task brief's own stated assumptions were corrected before building** (kickoff line's own instruction): B-023's SSRF guard (`safeDialContext`/`isBlockedTestTarget`) and B-022's credential cipher (`toolcreds.Cipher`) both live in `eami-api/internal/...` — `eami-gateway` and `eami-api` are separate Go modules under `go.work`, so neither is importable regardless of export status (Go's `internal/` visibility is scoped to the module tree). Matched this repo's own B-025 precedent (duplicate small security-relevant helpers into each module rather than build shared infrastructure) instead: `toolrouter/dial.go` and `toolrouter/creds.go` are deliberate, documented duplicates — `creds.go`'s `Cipher` is decrypt-only (`eami-gateway` never writes credentials); `eami-api`'s originals are completely untouched, still needed for its own connectivity-test feature.
**A genuine design fork, presented to the user rather than decided unilaterally:** when a tool_call's name matches no `gateway_tools` row for the caller's org (the case for 100% of today's live traffic), dynamic routing is **strictly additive/opt-in** — falls through to the exact same static `fwdProxy.Forward` behavior as before this brief, never a new rejection path for a previously-working call. `proxy.go` and `internal/approval/router.go` (the escalation/approval path) are completely untouched by this task — dynamic routing applies only to `dispatch`'s immediate-allow branch.
**Policy engine, additive:** `eami-policy`'s `ActionContext.ToolServerID string` / `Conditions.ToolServerIDs []string`, new matcher block in `structural.go` mirroring the existing `ToolNames` pattern exactly — lets a rule pin to a resolved `gateway_tools.id` (immutable) in addition to a tool name (mutable). Migration `009_tool_server_routing.sql` adds `policy_conditions.tool_server_ids TEXT[]`; `policyloader.go`'s `queryRules` scans/populates it the same way as every other condition column.
**A real correctness bug found and fixed during code review, before shipping:** `Router.Forward` dereferenced `row.Type` with no nil-guard — the single production call site is already correctly guarded, so this couldn't panic today, but `Router` is an importable package and its own doc comment promises no failure path ever panics. Fixed with a one-line check + `TestForward_NilRow_Rejected`.
**Reviewer + security subagent passes both ran** (mandatory — new outbound dispatch to admin-supplied destinations, at least B-023 rigor): **both clean, no exploitable vulnerabilities, no blocking findings.** Security review re-derived (not trusted from comments) that the duplicated SSRF guard is byte-for-byte equivalent in protective effect to the original (including the resolve-once/dial-the-validated-IP DNS-rebinding protection); confirmed no code path reaches the network without the guarded dialer (the only unrestricted dialer is confined to `router_pg_test.go`, verified via grep); confirmed Go's default redirect-following does NOT bypass the guard (implemented as the `Transport`'s `DialContext`, re-invoked on every redirect hop, not a one-time pre-check); confirmed no decrypted credential ever reaches a log line or error message; confirmed `toolrouter.Cipher`/`toolcreds.Cipher` are genuinely interoperable (compared line-by-line, verified with a real round-trip test). One informational note, not a vulnerability: the guard's correct, conservative blocking of RFC1918 addresses also blocks an on-prem gateway's legitimate access to a customer's own internal APIs — flagged for product as a future allowlist decision, not fixed here (fails closed correctly in the meantime). Code review's two other minor findings (no `gateway_tools.status` filter in `Resolve`; `db_connection_string` `auth_type` silently ignored on `rest_api` rows, same class as B-023's already-accepted `basic`-auth gap) left as documented, non-blocking limitations.
**Test coverage:** 22 new tests. `internal/toolrouter/router_pg_test.go` (11 real-Postgres tests, following this session's established pattern, including an injectable-dialer test seam mirroring B-023's own `dialContextFunc` convention — production `New()` always uses the real guard; only `_test.go` files construct the unrestricted one used to reach a local `httptest` server without tripping the loopback block): `Resolve` found/not-found/cross-org; `Forward` real REST round-trip, real credential decrypt+header application, undecryptable-credentials/empty-base_url/wrong-type/nil-row clean rejections, and the real SSRF guard rejecting `127.0.0.1`/`169.254.169.254` through the actual production dialer. `cmd/gateway/main_pg_test.go` (6 tests for `resolveDynamicTool`'s every branch). `eami-policy/policy_test.go` (`TestMatchesRule_ToolServerIDs`, 5 cases). `eami-gateway/internal/policyloader/loader_pg_test.go` (1 real-DB round-trip test). **Verified 2026-08-06 with a real toolchain: `go build`/`go vet`/`go test ./...` clean across `eami-gateway`, `eami-policy`; `eami-api` confirmed unaffected** (untouched module).
**Live-verified end-to-end against the real `docker-compose` stack, rebuilt fresh first:** seeded a real org/agent/`gateway_tools` row (real AES-256-GCM-encrypted credentials, real `TOOL_CREDENTIALS_ENCRYPTION_KEY`), issued a real token, opened a real SSE session, posted a real `tool_call`. **AC1**: routed to the tool's real `base_url` against a real public echo endpoint (`postman-echo.com/post`); confirmed the real decrypted API key arrived as a real `Authorization: Bearer` header at the real destination. **AC3, discovered organically, not just designed as a test case**: the first live-verification attempt used `host.docker.internal` (a local echo server) as the tool's `base_url` — the real SSRF guard correctly rejected it, since Docker Desktop's `host.docker.internal` resolves to a private-range address inside Docker's own network. Genuine, unplanned live proof the guard is active on the real dispatch path. **AC4**: a second real org/agent calling the identical tool name fell through to the exact same static-fallback error shape as an ordinary unregistered call, proving cross-org isolation live. **AC6**: a real `mcp`-type `gateway_tools` row, called live, also fell through to the identical static-fallback path, confirming `mcp`/`database`-type registration doesn't accidentally trigger dynamic dispatch. `audit_log` correctly recorded all four live calls (`allowed` for the real successful dispatch, `denied` for the SSRF-blocked/cross-org/mcp-fallback attempts). All test fixtures cleaned up afterward; final `go build`/`go vet`/`go test ./...` re-run clean post-cleanup.
**Known limitations, documented not silently left:** no `gateway_tools.status` filter (a disabled tool is still routable); `db_connection_string` auth on a `rest_api` row sends no auth header; on-prem customers' legitimate internal REST targets are unreachable by the SSRF guard's design (no allowlist mechanism, flagged for product); ~~no per-endpoint action-to-path mapping (every action for a tool POSTs to the same single `base_url`...)~~ — **closed by B-046, see immediately below.** `mcp`/`database`-type dynamic routing and MCP server capability discovery remain future, separate work.

**Interfaces — B-046 (2026-08-06), per-endpoint action-to-path mapping for `rest_api`-type `gateway_tools`:** closes the gap B-044 itself flagged, confirmed live during that task's own manual testing (a non-`/`-suffixed action against `base_url` alone 404'd — real REST APIs have more than one endpoint). New `gateway_tools.action_paths JSONB` column (migration `010`, `{"<action_name>": {"path": "...", "method": "..."}}`) — JSONB chosen over a new table, matching this schema's own established convention for small structured per-row config (`alert_rules.condition_config`, `notification_config.config`, `episodes.steps`) and avoiding a join on every dispatch for data always read as one small whole per tool. `toolrouter.Router.Forward` (`router.go`): when a resolved tool has `ActionPaths` entries, an incoming action matching one dispatches to `joinURLPath(base_url, entry.Path)` with `entry.Method` (default `POST`, matching the flat-behavior default) instead of always POSTing straight to `base_url`. Request body envelope unchanged (`{tool, action, params, session_id}`) — only the URL path and HTTP method vary per mapped action; deliberate scope boundary to avoid building a generic API-schema importer, per this task's own brief.
**Fallback behavior for an unmapped action, a deliberate product decision, not an oversight:** a tool with **no** `action_paths` at all keeps B-044's exact original flat behavior (every action POSTs to `base_url`) — fully additive, zero effect on any tool that doesn't define mappings. A tool that **has** other mappings defined, called with an action that isn't among them, is a **clean hard rejection** (`toolrouter: tool ... has action mappings defined but none for action "..." -- refusing to fall back to base_url for an unmapped action`), confirmed explicitly with the user before building (not assumed either way) — routing an unmapped action to a URL/credential context the admin never authorized for it was judged worse than a clear failure, matching this same file's existing precedent of rejecting cleanly on unusable state (nil row, wrong type, missing `base_url`, bad creds) rather than silently falling through.
**Security review finding, traced and empirically verified, not just asserted:** the one real question worth answering carefully was whether an admin-supplied `action_paths[action].path` value could redirect a dispatch to a different host than `base_url`'s own (an SSRF-guard bypass) — `joinURLPath` is pure string concatenation (`strings.TrimRight(base,"/") + "/" + strings.TrimLeft(path,"/")`) forming **one** URL string parsed **once**, never a `url.ResolveReference`-style relative resolution (the pattern that has known scheme-relative `"//host"` bypass pitfalls). Verified empirically with a standalone Go program feeding `joinURLPath` six candidate malicious `path` values (`//evil.example.com/steal`, `http://evil.example.com/steal`, `https://evil.example.com/steal`, `@evil.example.com/steal`, a `..%2F..%2F@evil.example.com` traversal attempt) against `base = "https://good.example.com"` and parsing each result with `net/url` — every single one resolved to `Host="good.example.com"`, confirming the SSRF guard (which validates the dialed address derived from the final request URL's host) cannot be bypassed via a crafted mapped path. Added a defense-in-depth write-time guard anyway in `eami-api` (below) rejecting scheme-qualified paths — not because it's exploitable, but because it's a real, easy admin misconfiguration worth catching early with a clear error. `dial.go`/`creds.go` (SSRF guard, credential decrypt) confirmed byte-for-byte untouched by this task, per its explicit scope boundary.
**Reviewer + security subagent passes, both mandatory:** both automated subagent passes failed on a platform-wide session-limit error before completing (not a finding, a tooling outage — resets time noted in-session). Substituted with a direct, careful manual review of the actual diff plus the empirical `joinURLPath` verification described above, rather than skipping the mandatory review step.
**Test coverage:** `internal/toolrouter/router_pg_test.go` gained 5 new real-Postgres tests (`TestResolve_ParsesActionPaths`, `TestForward_ActionPathMapping_RoutesCorrectly` — two different mapped actions, two different methods, both routed correctly; `TestForward_UnmappedActionOnMappedTool_CleanRejection`; `TestForward_NoActionPathsAtAll_FlatBaseURLBehaviorUnchanged`; `TestForward_SSRFStillBlocked_WithActionMapping` — the guard still applies on the mapped-path dispatch branch, not just the flat one) plus a new `insertToolWithActionPaths` fixture helper. **Verified 2026-08-06 with a real toolchain: `go build ./...`, `go vet ./...`, `go test ./...` clean across the full `eami-gateway` module** (17 tests in `internal/toolrouter` alone, all passing against real Postgres).
**Live-verified end-to-end against the real `docker-compose` stack, rebuilt fresh first (`eami-api`/`eami-gateway`/`eami-ui` images):** migration `010` applied to the running Postgres; created a real `rest_api` tool (`b046-live-verify`, `base_url=https://postman-echo.com`) via a real admin JWT with two real action mappings (`echo_get`→`GET /get`, `echo_post`→`POST /post`); issued a real agent token, opened a real MCP SSE session, posted three real `tool_call`s. **AC1**: `echo_get` reached `https://postman-echo.com/get` (confirmed via the echoed `"url"` in the real response) with the real decrypted `Authorization: Bearer` header attached; `echo_post` reached `https://postman-echo.com/post` with the correct unchanged envelope in the request body. **AC3**: a third call to an undefined action on that same tool returned the exact designed rejection error over the live SSE stream, not a 200 or a silent fallback. **AC2**: a separate pre-existing tool with zero `action_paths` (left over from B-044's own manual testing) was called with an arbitrary action name and still flatly POSTed to its `base_url`, completely unaffected. **AC4 (credentials/SSRF unchanged)**: real decrypted credentials were injected correctly in every call above; the SSRF guard's continued correctness on the mapped-path branch is covered by the automated real-Postgres test above (not separately re-proven live, since it requires a genuinely blocked target and the empirical `joinURLPath` trace-through already answers the theoretical question directly). One test-environment note: the existing `dev@example.com` admin account's password was reset to a known value to obtain a login token for this verification (no other credential was available) — flagged here since the user may want to reset it again.

**Interfaces — B-061 (2026-08-12), real `tools/list` support for MCP discovery:** `internal/mcp`'s SSE transport previously implemented only the non-standard `tool_call` JSON-RPC method — any other method, including the real spec's `tools/list`, got a `-32601 method not found`, so a connecting agent had to already know exact tool/action names in advance (standing assumption since B-044). Per the MCP Discovery investigation's Part A/B findings, added real `tools/list` so an agent can discover its own org's actual `rest_api` connectors and their known actions. New `ToolDefinition{Name, Description, InputSchema}` (matches the real spec's shape), `ListToolsHandler` (a 4th callback-injection parameter on `mcp.NewHandler`, mirroring `DecisionHandler`'s existing DB-agnostic pattern — `internal/mcp` still has zero direct DB access). `ServeMessages` gained an early `if req.Method == "tools/list"` branch dispatching to new `serveToolsList`; the pre-existing `tool_call` branch below it is byte-for-byte unmodified (confirmed by direct diff review, not assumed). `cmd/gateway/main.go`'s new `listGatewayTools(ctx, pool, orgID)` mirrors `resolveDynamicTool`'s exact org-scoping shape (`WHERE org_id=$1 AND type='rest_api'`), building one `ToolDefinition` per `action_paths` key (`Name="<connector>/<action>"`, matching `splitToolAction`'s primary separator; `Description` synthesized from the real stored method+path, e.g. `GET /get on connector "b046-live-verify"`; `InputSchema={"type":"object"}`, honestly generic since `action_paths` carries no parameter schema).
**Design decision:** only `type='rest_api'` rows are eligible — `ai_provider` connectors dispatch via a completely different mechanism with no `action_paths` concept (representing one as "zero actions" would be misleading, not honestly empty); `mcp`/`database` types have no working direct-dispatch path in `main.go` today. A `rest_api` row with no `action_paths` set (B-044's flat-POST fallback) naturally contributes zero entries — not a separate rule, just the consequence of building the list from `action_paths` keys. `serveToolsList` follows `tool_call`'s exact 202-then-SSE-`message` response mechanism and the same `context.WithoutCancel(r.Context())` pattern (B-039) for its detached goroutine.
**Reviewer + security passes, both clean, zero findings.** Code review confirmed `tool_call` is genuinely untouched, `context.WithoutCancel` is applied correctly, and the mandatory `t.Cleanup(pool.Close)`-before-DB-cleanup ordering convention (CLAUDE.md) is followed in every new test helper — no recurrence of the B-056-class bug. Two nitpicks noted, left as-is: non-deterministic map-iteration order in the returned tool list (harmless for `tools/list` semantics); a pre-existing, non-regression nil-handler-panic pattern shared with `dispatch`. Security review confirmed `orgID` is always read from `sess.Agent.OrgID` (registry-resolved from the JWT-validated agent at session creation, never client-supplied on the `tools/list` request itself), the query is fully parameterized, no credential material (`credentials_encrypted`/`base_url`) is ever read into a `ToolDefinition`, and the synthesized `Description` string is safely JSON/SSE-escaped via `encoding/json`.
**Test coverage:** `cmd/gateway/main_tools_list_pg_test.go` — 5 real-Postgres unit-shape tests on `listGatewayTools` (multi-action tool → one definition per action with real method/path; cross-org isolation; `mcp`/`database`/`ai_provider` types all contribute zero; a `rest_api` tool with no `action_paths` contributes zero; empty orgID short-circuits with no query) plus 2 new real end-to-end SSE tests — genuinely new test infrastructure (`readSSEFrame`, no prior test in this module opened a live SSE stream): a real agent JWT opens a real `GET /v1/mcp/sse`, POSTs a real `tools/list`, and reads back the real `message` SSE event, asserting the exact `{"tools":[{"name","description","inputSchema"}]}` shape a real MCP client would parse, built from a real seeded connector with a real `ai_provider` row proven absent from the result; plus a `tool_call` regression test over the same live harness. **Verified 2026-08-12 with a real toolchain: `go build`/`go vet`/`go test ./... -count=1` clean across the full `eami-gateway` module** (14 packages).
**Live-verified end-to-end against the real `docker-compose` stack, rebuilt fresh:** a real agent JWT issued via `POST /v1/gateway/tokens`, a real `GET /v1/mcp/sse` opened, a real `tools/list` POSTed and its real `message` SSE event read back — result showed exactly the two real `b046-live-verify` actions (`echo_get`, `echo_post`) with their real method/path in each description; the org's real `claude` `ai_provider` connector and the two `rest_api` tools with no `action_paths` configured (`manual_verify_tool`, `Test`) correctly did not appear. A follow-up real `tool_call` to `b046-live-verify/echo_get` over the same live transport still dispatched correctly (real response from `postman-echo.com`), confirming no regression outside the automated suite too.
**Known limitations, documented not silently left:** `ai_provider`/`mcp`/`database` tool discovery is out of scope (a natural, separate future brief); no real pagination (`nextCursor` never populated); `InputSchema` is the generic `{"type":"object"}` for every tool, not derived per-parameter (no schema exists in `action_paths` to derive from); no agent-facing client in this codebase actually consumes `tools/list` yet — this closes the server-side protocol gap only.

**Interfaces — B-070 (2026-08-17), per-agent-identity rate limiting on workflow-run:** `POST /v1/gateway/workflows/{workflowId}/run` (B-059) had no rate limiting at all — a real resource-exhaustion vector, since each run is fully synchronous and can block for up to 10 minutes on an escalation `Hold()`. New `internal/workflow/ratelimit.go`: a small in-memory, per-key fixed-window `rateLimiter` (deliberately duplicated from `eami-api/internal/api`'s identical type — `eami-gateway`/`eami-api` are separate Go modules under `go.work`, matching this repo's own B-025/B-044 precedent for this exact situation) plus `RateLimitRunMiddleware(idm, resolver, limit, window, next)`, wrapping `workflowHTTP.HandleRun` at its `mux.HandleFunc` registration in `cmd/gateway/main.go` — **this module has zero chi dependency at all** (routing is stdlib `http.ServeMux`, confirmed by grep before building), so the brief's own assumption of reusing "chi's rate-limiting middleware" didn't apply here; a hand-rolled wrapper around the raw `http.HandlerFunc` was the only option. `HandleRun`/`workflow/http.go` are **byte-for-byte unmodified** — the middleware duplicates exactly two read-only, side-effect-free things `HandleRun` already does (bearer JWT validation via `identity.Manager.Validate`, agent-name resolution via the same `AgentResolver`) purely to learn the calling agent's identity before deciding whether to rate-limit; missing/invalid auth or an unresolvable agent name is NOT rejected by the middleware — it passes through unchanged so `HandleRun` produces its own real 401/403, avoiding two slightly different auth-error code paths. New `config.RateLimitConfig` (`WORKFLOW_RUN_RATE_LIMIT_PER_AGENT`/`WORKFLOW_RUN_RATE_LIMIT_WINDOW_SECONDS`, defaults 5/60s — chosen because a single run can legitimately hold resources for up to 10 minutes, so the real threat is a bursty retry/runaway-agent loop starting many overlapping runs, not sustained high-frequency polling).
**Ambiguously-confirmed single-instance assumption, checked not assumed:** ADR-020 Model A (the only deployment model that exists today, `DECISIONS.md`) is a fully self-contained one-process-per-service appliance — an in-memory limiter is the whole story here, not a partial one, same reasoning already established for B-055's setup-wizard limiter.
**A real cross-org rate-limit bypass found by this task's own mandatory security review, before shipping, not after:** the first version keyed the limiter on the raw JWT `claims.Subject` (an agent name, e.g. `"agent:researcher"`). `gateway_agents.name` is unique only per `(org_id, name)` (`schema.sql`'s own `UNIQUE` constraint), **not globally** — two different orgs can each register an agent named `"researcher"`, and both tokens would carry the identical `claims.Subject`, so one org's agent exhausting its quota would also rate-limit the other org's identically-named agent, which had made zero requests. Fixed by keying on the **resolved** `agentRec.ID` (a real, globally-unique registry UUID) instead of the bare JWT subject — the same class of fix B-042's `registry.LookupByNameAndOrg` made for the token-revoke path, applied here more simply since a resolved `*registry.AgentRecord` already carries a real unique primary key.
**A second real finding from the same review, fixed:** `rateLimiter.Allow` indexed `kept[0]` once `len(kept) >= rl.limit`, which is immediately true (`0 >= 0`) for a non-positive `limit` on the very first request — a misconfigured (e.g. typo'd `"0"`) env var wouldn't disable limiting, it would **panic every request** to the guarded route. Fixed two ways: `Allow` now fails closed (denies, doesn't panic) for `limit <= 0`, independent of config; `config.go`'s `validate()` also now explicitly rejects a negative `WORKFLOW_RUN_RATE_LIMIT_PER_AGENT`/`_WINDOW_SECONDS` at startup (0 is still treated as "use the default," matching this file's own existing convention for every other zero-valued field).
**Reviewer + security subagent passes, both mandatory (explicit security-hardening brief):** code review's automated pass found the two real issues above (cross-org key collision, non-positive-limit panic) plus a third finding shared with the `eami-api` side (see that module's B-070 entry) — all three fixed before shipping, not deferred.
**Test coverage:** `internal/workflow/ratelimit_test.go` — 7 tests against a fake `AgentResolver` (avoids a real registry/DB; these tests are about the rate-limit decision itself) and a real ephemeral-keypair `*identity.Manager`: same-agent threshold trip with `Retry-After`; independent quotas for two genuinely different agents; **`TestRateLimitRunMiddleware_SameName_DifferentOrgs_IndependentLimits`**, the regression test for the cross-org fix — two tokens with the identical `claims.Subject` shape, resolved via a context-value-keyed fake resolver to two distinct `AgentRecord`s (same `Name`, different `ID`/`OrgID`), proving org A exhausting its quota never touches org B's identically-named agent; single-run-not-blocked; invalid-auth and unresolvable-agent pass-through (both confirm `HandleRun`'s own error path is reached, not intercepted). **Verified 2026-08-17 with a real toolchain: `go build ./...`, `go vet ./...`, `go test ./... -count=1` clean across the full `eami-gateway` module**, including this module's pre-existing real-Postgres suites (`internal/identity`, `internal/toolrouter`, `internal/approval`, etc.), confirmed unaffected.
**Live-verified end-to-end against the real `docker-compose` stack, rebuilt fresh (before AND after the security-review fixes):** issued a real agent JWT via `POST /v1/gateway/tokens` for a real registered agent (`manual-test-agent`), then rapid-fired 6 real `POST /v1/gateway/workflows/{workflowId}/run` calls (a bogus `workflowId`, so `HandleRun` itself returns a real `400` — proving the request genuinely reached the handler each time, not blocked earlier): the first 5 returned real `400`s, the 6th returned a real `429` with `Retry-After: 60`, exactly matching the configured window. See the `eami-api` B-070 entry below for the shared login/setup-wizard verification run in the same session.
**Known limitations, disclosed not hidden:** distributed/multi-instance rate limiting is explicitly out of scope (ADR-020 Model A is single-instance; matches this brief's own stated boundary); no rate limiting on any other gateway route (MCP `tool_call`/`tools/list`, episode read, token issue/revoke) — a natural, separate follow-up, not attempted here.

---

## eami-policy
**Purpose:** Shared policy-evaluation library (not a service), imported by `eami-gateway`. Structural (JSON-match) and semantic (LLM-intent) rule evaluation.
**Status:** STABLE (structural), PARTIAL (semantic — intentional stub).
**Key files:** `evaluator.go`, `structural.go`, `semantic.go`, `types.go` (flat module, no `internal/`).
**Interfaces:** Go library API — `Evaluate(rules, context) Decision`. Imported directly by `eami-gateway`; no network interface. `ActionContext.ToolServerID`/`Conditions.ToolServerIDs` added B-044 (2026-08-06) — lets a rule match a resolved `gateway_tools.id`, additive to the existing `ToolNames` by-string match.
**Data owned:** none (pure library over caller-supplied rule/context data).
**Test coverage:** 1 `_test.go` file (`policy_test.go`) vs 4 source files, including a test that pins the stub behavior (`TestEvaluate_SemanticRuleSkippedByStub`). **Verified 2026-08-06 with a real toolchain: `go build ./...`, `go test ./...` clean** (B-044's `TestMatchesRule_ToolServerIDs` addition).
**Known limitations:** `semantic.go` — semantic/LLM rule evaluation **always returns no-match** (`// TODO(BE-Policy): Implement full LLM-based semantic evaluation`). Any policy relying on a semantic rule silently falls through to the next rule or default action. Blocked on ADR-009/ADR-012 (LLM endpoint choice).

---

## eami-api
**Purpose:** SaaS REST backend. Auth, org/user/RBAC management, gateway-resource CRUD (agents/policies/tools/nodes), audit query, FinOps analytics, alerting, discover/ingest write paths, memory/episode read paths.
**Status:** STABLE (broad — largest, best-tested module). **Data-sovereignty landmine resolved 2026-07-22** (B-002 fully closed — see below; no longer a landmine).
**Key files:** `cmd/api/main.go`; `internal/{api,auth,alerting,config,store}/`. `internal/store/*.sql.go` — one file per domain, sqlc-style, parameterized queries throughout (no `fmt.Sprintf` SQL construction found). **`internal/api/memory.go` and `internal/store/episodes.go` no longer exist** — deleted in B-002 Brief 3 (2026-07-22); see below.
**Interfaces — full route table** (from `internal/api/router.go`, more authoritative than any doc):
- Public: `GET /health`, `POST /v1/auth/login`, `POST /v1/auth/refresh`
- Service-key (collector/gateway → API, no JWT): `POST /v1/reports`, `POST /v1/ingest/batch`, `POST /v1/internal/token-usage`, `POST /v1/reports/paste-events` (B-032, no live caller yet)
- JWT + admin: org/notification settings, user management (`/v1/users*`)
- JWT + admin/operator: API keys, agents/policies/tools/nodes CRUD, agent-config, alert-rule CRUD, approval creation
- JWT + admin/operator/approver: approval decide, alert acknowledge/resolve
- JWT + admin/operator/viewer (read): agents/policies/tools/nodes list+get, `/v1/audit`, `/v1/audit/export`, `/v1/audit/verify`, `/v1/alerts*`, `/v1/finops/summary`, `/v1/finops/timeseries`, `/v1/memory/episodes`, `/v1/memory/episodes/search`, `/v1/memory/episodes/{episodeId}` (all three now served by the gateway-proxy handlers, see below), `/v1/gateway/episodes`, `/v1/gateway/episodes/search`, `/v1/gateway/episodes/{episodeId}` (same handlers, second mount, unused by the frontend), `/v1/endpoints*`, `/v1/discover/endpoints*`, `/v1/paste-events`, `/v1/paste-events/timeseries` (B-038)
- JWT + any role: `GET /v1/approvals`, `GET /v1/approvals/{id}`
**Data owned:** `orgs`, `users`, `refresh_tokens`, `api_keys`, `agents` (gateway_agents), `agent_configs`, `policies`/`policy_conditions`, `gateway_tools`, `gateway_nodes`/`gateway_node_metrics`, `approval_requests`, `audit_log` (read; write owned by gateway), `alert_rules`/`alerts`, `token_usage`, `model_pricing`, `discovered_endpoints` + `endpoint_ai_apps`/`endpoint_mcp_servers`/`endpoint_model_files`/`endpoint_reports`, `paste_events` (B-032), `notification_config`. **`episodes` table: no longer directly readable from eami-api at all** — the only path to it is via `eami-gateway`'s org-isolated proxy (`internal/api/gateway_episodes.go`, Brief 2, frozen since).
**Interfaces — B-002 Brief 2 (2026-07-22, `internal/api/gateway_episodes.go`), gateway episode proxy — frozen/unchanged by Brief 3:** `GET /v1/gateway/episodes` (+`/search`, `/{episodeId}`) proxy to `eami-gateway`'s Brief-1 endpoint via `X-Service-Key` (config `Gateway.EpisodeReadServiceKey`, env `GATEWAY_EPISODE_READ_SERVICE_KEY` — same var name/value eami-gateway itself validates inbound; `Gateway.URL`/env `API_GATEWAY_URL`, defaults `http://eami-gateway:8080`). Both config fields optional at eami-api startup — unset means the routes 502 per-request ("gateway proxy is not configured"), not a boot failure. Org isolation (the actual point of this brief, since Brief 1's service-key path enforces none): every gateway call's `org_id` is always `claimsFromContext(r).OrgID`, never client input; an optional `org_id` query param is accepted only as a tamper-check that 403s on mismatch before the gateway is ever called. Gateway error responses collapse to a real 404 (episode not found / cross-org, same value) or a generic 502 (everything else, including network failures) — gateway's plain-text body is never read past the status code, so nothing can leak through. Exported types `GatewayEpisode`/`GatewayListResult`/`GatewayError`/`GatewayEpisodeClient` (exported only so the external `api_test` test package can construct fakes/errors — not intended as a public API).
**Interfaces — B-002 Brief 3 (2026-07-22, branch `b-002-memory-cutover`), memory.go cutover — B-002 fully closed:** `eami-api/internal/api/memory.go` and `eami-api/internal/store/episodes.go` **deleted entirely** (verified zero other callers of `store.Queries.ListEpisodes`/`SearchEpisodes`/`ListEpisodesParams`/`store.Episode` before removal). `router.go`'s `/v1/memory/episodes` and `/v1/memory/episodes/search` registrations — the actual `api/openapi.yaml`-documented, `MemoryPage.tsx`-consumed routes — now point directly at Brief 2's `ListGatewayEpisodes`/`SearchGatewayEpisodes` handlers (same functions, new mount, zero duplicated logic). Added `GET /v1/memory/episodes/{episodeId}` → `GetGatewayEpisode`, filling a route `openapi.yaml` documented but no implementation ever existed for. `MemoryPage.tsx` required **zero code changes** — verified the JSON response shapes are byte-identical between the old `memory.go` output and Brief 2's output (same field names, same envelope) before relying on that. Security review for this brief specifically re-verified the org-isolation chain (`jwtMiddleware` → `checkOrgID` → `claimsFromContext(r).OrgID`) at the new `/v1/memory/episodes*` mount points rather than assuming Brief 2's review transferred automatically — confirmed the leak is fully closed, not just superseded by a safer alternative running alongside it.
**Test coverage:** 7 `_test.go` files vs 44 source files (post Brief 3 — 2 files net change: `memory.go`'s production code gone, `memory_test.go` added). Covers agents, approvals, auth, finops, policies, gateway episode proxy (Brief 2, 11 tests), and memory cutover (Brief 3, new `memory_test.go`, 8 tests reusing Brief 2's `fakeGatewayClient`/`newGatewayTestServer` with zero duplication) — including `TestMemoryEpisodes_List_MismatchedOrgIDSupplied_Returns403_GatewayNeverCalled`, re-proving the org-isolation centerpiece at the real frontend-facing route, and `TestMemoryEpisodesDetail_ValidRequest_ReturnsFullSteps`, asserting non-empty step content actually round-trips (not just a 200 status). **Verified 2026-07-22 with a real toolchain: `go build ./...`, `go vet ./...`, `go test ./...` all clean, 0 failures.** Two subagent reviewer/security passes per brief (Brief 2 and Brief 3) all came back clean. **Frontend (`eami-ui`) build/lint/typecheck not run for Brief 3** — Node/npm confirmed genuinely absent from this machine (checked common install locations directly, unlike Go which was merely off-`PATH`) — `MemoryPage.tsx`'s correctness rests on manual shape-verification, not a compiler run. **`docker compose up`-based manual verification (requested by Brief 3's brief) not performed** — no Docker in this environment; flagged before implementation, not discovered after. **Pre-existing, unrelated issue discovered while verifying Brief 2:** several `TestFinOpsTimeSeries_*` subtests in `finops_test.go` panic internally (nil `s.queries` dereferenced in `finops.go:269`) but still report `--- PASS` because chi's `Recoverer` catches the panic; confirmed pre-existing, not fixed (out of scope) — see `BACKLOG.md` B-016.
**Known limitations / landmines:**
- `router.go` — the historical "Memory episodes (stubs...)" comment that used to sit above these routes is gone now that the routes themselves were rewritten in Brief 3 (was previously flagged as stale in `BACKLOG.md` B-012 — worth confirming that item can close too).
- `notification_channels` table (`schema.sql`) appears unused by any Go code — superseded by `notification_config`. Likely dead schema.
- Search over episodes is `ILIKE` text match on the `task` column, not vector similarity, despite pgvector being provisioned — consistent with the embedding placeholder (both wait on ADR-009). See `BACKLOG.md` B-008.
- **New, discovered during Brief 3:** `api/openapi.yaml`'s `EpisodeStep` schema doesn't match the real step JSON shape (`eami-gateway/internal/episode/recorder.go`'s `Step` struct) — predates B-002 entirely, now visible because `/v1/memory/episodes/{episodeId}` is the first real route where the documented schema is checkable against actual output. See `BACKLOG.md` B-017.
**Interfaces — B-022 (2026-07-23), gateway tool credential encryption:** `POST /v1/gateway/tools` now actually persists the `credentials` object it was always documented (`api/openapi.yaml`'s `ToolCreate.credentials`) but never implemented — previously silently discarded (decoded, never read, never written to `gateway_tools.credentials_encrypted`, 201 returned anyway; a gap surfaced by a prior full-application audit, not previously tracked under its own B-ID). New package `eami-api/internal/toolcreds` (`toolcreds.go`) implements AES-256-GCM encrypt/decrypt with a hex-encoded 32-byte key (`TOOL_CREDENTIALS_ENCRYPTION_KEY` env var, `config.Config.ToolCredentialsEncryptionKey`) — encryption happens in the API process; Postgres never sees the key or the plaintext. Deliberately not pgcrypto (the column's own schema comment says "encrypted using pgcrypto," but that extension is otherwise unused anywhere in the codebase — using it would mean sending the key to Postgres as a query parameter on every write, a weaker posture than app-side AEAD). `Server` gets a `toolCreds *toolcreds.Cipher` field (nil if the key is unconfigured — optional at startup, same convention as the B-002 gateway proxy config) and a `toolStoreOverride`/`toolQueries()` test seam scoped to `tools.go` only (mirrors `storeIface`/`WithGatewayClient` elsewhere in this file, doesn't touch the broader `Store` interface). `CreateTool` decodes `credentials` as `json.RawMessage` and a `credentialsProvided` helper decides presence structurally (any JSON object with ≥1 key, regardless of key names) rather than via the typed `ToolCredentials` struct — this was a security-review-caught bug in an earlier version of this change: deciding presence via the typed struct meant any unrecognized field name (a typo, a future field) decoded to an all-empty struct and silently reproduced the exact bug being fixed. The raw submitted bytes are what gets encrypted and stored, not a re-marshaled struct, so nothing the client sends can be dropped by the struct's fixed field set. If credentials are submitted but no key is configured, the request fails closed (500, no store call, no tool row created) — never a silent-success-with-data-loss. Credentials are write-only: `store.GatewayTool`/`ToolResp` have no field for the encrypted blob, and no query in `tools.sql.go` selects `credentials_encrypted` back out, so there is no leak surface in `ListTools`/`CreateTool`'s responses (structurally, not just by convention).
**Test coverage — B-022:** `eami-api/internal/toolcreds/toolcreds_test.go` (9 tests: round-trip, non-deterministic nonce, tamper/wrong-key/truncated-ciphertext rejection, key-validation edge cases) + `eami-api/internal/api/tools_test.go` (10 new tests, `tools.go` had zero test coverage before this: encrypted storage + decrypt-and-verify retrieval-proof, unaffected-without-credentials, empty-object treated as no-credentials, the unrecognized-field-name regression + its fail-closed variant, non-object-credentials-rejected, response-leak checks on both `CreateTool` and `ListTools`, and a nil-store-configured regression test — `tools.go`'s other three handlers previously called `s.queries` with no nil guard, unlike every other handler file in this package; a `Server` built via `NewHandler` for another handler's tests would have nil-panicked into an opaque 500 instead of a clean error, caught by the general code-review pass). **Verified 2026-07-23 with a real toolchain: `go build ./...`, `go vet ./...`, `go test ./...` all clean, 0 failures** (full module, not just the new packages). Two independent subagent passes: security review caught and the fix confirmed the fail-closed-bypass-via-unrecognized-field-name bug above (re-verified clean after the fix); general code review caught the missing nil-store guard (fixed) and a stray non-standard error code, `"config_error"` vs. the codebase's uniform `"internal_error"` (fixed).
**Known limitations:** Key rotation/loss is operational, not code: losing `TOOL_CREDENTIALS_ENCRYPTION_KEY` makes any already-stored `credentials_encrypted` values unrecoverable (same class of risk as losing `POSTGRES_PASSWORD`), and there is no rotation tooling (re-encrypt-with-new-key) — out of scope for this fix.

**Interfaces — B-023 (2026-07-24), real `TestTool` connectivity check:** `POST /v1/gateway/tools/{toolId}/test` no longer returns a synthetic "connected" result — it was a stub (`tools.go`'s old comment: "For now returns a synthetic 'connected' result after a brief probe" — there was no probe). New file `eami-api/internal/api/tool_connectivity.go` performs a real check per `type`: `rest_api` does a real `http.Client` GET to `base_url` (`Authorization: Bearer <api_key>` if present, HTTP Basic using `oauth_client_id`/`oauth_client_secret` as a best-effort fallback for non-standard "OAuth" APIs that accept it, `auth_type=basic` always reports `misconfigured` since `ToolCredentials` has no username/password fields to supply it) — 401/403 classify as `auth-failed`, any other completed response (including 4xx/5xx) as `connected` (reaching the server and getting *a* response proves DNS/TLS/network work, which is what the test is actually asserting). `database` does a real `pgx.ConnectConfig` handshake against `connection_string` — a successful connect already proves both reachability and valid credentials (Postgres's own auth happens during the handshake), and a failure is classified via SQLSTATE: class `28xxx` (`invalid_password`/`invalid_authorization_specification`) → `auth-failed`, anything else → `unreachable`. `mcp` always reports `misconfigured` with an explanatory message: `mcp_command` describes a local subprocess meant to run on the gateway host, not eami-api's cloud process — executing an admin-supplied command string from the SaaS backend would itself be a command-injection/RCE surface, and `eami-gateway/internal/proxy` (read for this task) confirmed the gateway's real dispatch path doesn't even consult `gateway_tools` per-tool either, so there's no existing per-tool MCP connection behavior to mirror. Every outcome is bounded by an 8-second context timeout (`toolTestTimeout`) applied once, wrapping both the decrypt step and the dial — no retries, per this task's scope. Response shape changed from the old stub's `{status, latency_ms}` to `{success, latency_ms, error}`, which is what `api/openapi.yaml` has documented for this route all along (another previously-undetected spec-vs-implementation mismatch, now closed). The richer 4-way outcome (`connected`/`auth-failed`/`unreachable`/`misconfigured`) is only in the HTTP response — `gateway_tools.status`'s DB `CHECK` constraint only allows 3 values (`connected`/`degraded`/`disconnected`, schema frozen, not touched by this task), so `toolTestOutcome.dbStatus()` maps `auth-failed` → `degraded` (reached the endpoint; credentials are what's wrong) and both `unreachable`/`misconfigured` → `disconnected` for the persisted column only.

**Security-critical addition, found by review, not in the original plan:** `eami-api` is EAMI's own cloud SaaS backend (per `ARCHITECTURE.md`), architecturally distinct from `eami-gateway`, which runs on-prem inside the customer's own network. Without a network-target guard, an org-scoped `admin`/`operator` (a role with no other legitimate reach into EAMI's cloud environment) could point a tool's `base_url`/`connection_string` at cloud metadata endpoints (`169.254.169.254`) or internal-only services and use the connected/auth-failed/unreachable distinction as a reachability oracle against EAMI's own infrastructure — a real boundary crossing this feature would otherwise introduce, not a restatement of capability the caller already has via `eami-gateway` (which runs *in* the customer's own network, where they already have legitimate reach). Added `safeDialContext` + `isBlockedTestTarget`: resolves the target host (or parses it directly if already an IP literal), rejects the request before dialing if *any* resolved address is loopback, link-local (covers `169.254.169.254` and `fe80::/10`), unspecified, or private/RFC1918/ULA (via Go's own `net.IP.IsLoopback`/`IsPrivate`/`IsLinkLocalUnicast`/`IsLinkLocalMulticast`/`IsUnspecified`), then dials the already-validated IP directly (not the original host) so a DNS answer that changes between the check and the actual connection (rebinding) can't slip through. Wired into both paths: `testRESTTool` via `http.Transport.DialContext`, `testDatabaseTool` via `pgx.ParseConfig` + `cfg.DialFunc` + `pgx.ConnectConfig` (replacing the simpler `pgx.Connect(ctx, connString)`, which has no dial-level control point). Security review re-verified the fix clean, tracing the `169.254.169.254`/RFC1918 cases end-to-end through the real (non-test) code path and confirming the new `Server.toolDialOverride` test-injection seam (needed so tests can exercise real round-trips against local `httptest` servers, which the guard would otherwise correctly reject) is assigned only in `_test.go` files — never reachable from any HTTP request.

**Other fixes from the general code-review pass, applied before commit:** (a) `safeDialContext` originally only dialed the first resolved address; fixed to fall back through every validated address in order, mirroring `net.Dialer`'s own multi-address behavior — a host whose first A/AAAA record is down but whose second works no longer incorrectly reports `unreachable`. (b) `testRESTTool`'s per-call `http.Transport` had keep-alives enabled with nothing ever calling `CloseIdleConnections()` — since the ad-hoc Transport is discarded after each call but the underlying connection and its `readLoop`/`writeLoop` goroutines stay reachable through the open connection itself, every `TestTool` call against a REST tool leaked one idle socket/goroutine; fixed with `DisableKeepAlives: true` (a one-shot connectivity check has no reuse benefit anyway). (c) `testDatabaseTool`'s deferred `conn.Close(context.Background())` had no deadline of its own — if the peer went unresponsive after a successful handshake, `Close` could hang past the 8s bound the rest of the design promises; fixed with a dedicated 2-second close-only context, independent of the (possibly already-expired) outer context.

**Credentials never leak, first production `Decrypt` caller:** a new `toolStore` interface method, `GetToolForTest`, is the only place `credentials_encrypted` is ever read back — implemented via `toolStoreWithConnectivity`, a thin wrapper running one raw SQL query through `s.queries.DB()` (the same escape hatch `finops.go` already uses for queries with no sqlc-style wrapper), so `eami-api/internal/store/tools.sql.go` needed zero changes (explicitly confirmed acceptable with the user before implementing, since it's a deviation from this codebase's usual sqlc-style convention). The decrypted plaintext only ever reaches an `Authorization` header or a Postgres connection string — `classifyHTTPError`/`classifyPgConnectError` return only static, categorical strings (`"timed out"`, `"connection failed"`, `"database rejected the credentials"`), never `err.Error()` on the underlying error, which could otherwise echo back request/DSN details. `toolcreds.Decrypt` failures (wrong/rotated key, corrupted blob) are a clean `misconfigured` result, never a panic, never exposing the underlying decrypt error text.

**Test coverage — B-023:** 30 new tests. `tool_connectivity_test.go`: `classifyPgConnectError`/`classifyHTTPError` (pure-function, no live Postgres/network needed), `testRESTTool`/`testDatabaseTool` outcome classification via real `httptest` servers and refused/timed-out listeners, `testToolConnectivity` dispatch + decrypt-failure handling (including the no-leak assertion on the decrypted plaintext), `dbStatus()`'s 4-to-3 mapping, and direct coverage of the SSRF guard itself (`TestIsBlockedTestTarget` — loopback v4/v6, the AWS/GCP metadata address, RFC1918 ×3, IPv6 ULA, unspecified, plus two real public IPs confirmed *not* blocked; `TestSafeDialContext_*` — blocks by IP literal and by hostname resolution, blocks the metadata address, blocks RFC1918, rejects a malformed address). `tools_testtool_test.go`: full HTTP-round-trip handler tests for each acceptance-criterion outcome (valid-credentials-connected, invalid-credentials-auth-failed, connection-refused-unreachable-not-a-hang, no-credentials-still-testable, mcp-type-misconfigured-not-fabricated-connected, tool-not-found-404, decrypted-credentials-never-leak-in-response). **Verified 2026-07-24 with a real toolchain: `go build ./...`, `go vet ./...`, `go test ./...` all clean, 0 failures — 149 tests total in the module (up from 143 pre-B-023).**
**Known limitations:** none remaining — the frontend gap this created (`handleTest` not reading `success`) is fixed, see the `eami-ui` section below, B-024.

**Interfaces — B-025 (2026-07-24), config validation closes a live auth-bypass:** `internal/config/config.go`'s `Load()` **had no `validate()` at all** — `defaults()` hardcoded `ServiceKey: "changeme"` and a DSN with a `changeme` password, so an unset `API_SERVICE_KEY` env var meant `middleware.go`'s `requireServiceKey` (already correctly `subtle.ConstantTimeCompare`, untouched by this fix) would accept a literal `X-Service-Key: changeme` header from any caller against the collector write paths (`/v1/reports`, `/v1/ingest/batch`, `/v1/internal/token-usage`) — a real, working authentication bypass on a naive deployment, not a cosmetic placeholder. Added `validate(cfg *Config) error`, called at the end of `Load()` (its error already propagates to `main.go`'s existing `log.Fatalf`, so no `main.go` change was needed): rejects `cfg.ServiceKey` if empty or a known placeholder, and rejects `cfg.Database.DSN` if empty or its password segment is empty/placeholder. `defaults()` no longer ships any literal `changeme` — the default `ServiceKey`/`Database.DSN` fields are simply absent now, so an unconfigured deployment fails `validate()` instead of silently working. Two new unexported helpers, duplicated verbatim in `eami-gateway`'s config.go (separate Go module via `go.work`, no shared package introduced for ~30 lines): `isPlaceholderSecret(v string) bool` (trims + lowercases, checks against `{"", "changeme", "devpassword"}`) and `dsnHasPlaceholderPassword(dsn string) bool`, which — after a security-review-caught bug in the first version (see below) — extracts the DSN's password segment via `dsnPassword()` (`scheme://user:password@host` shape, last `:`/`@` delimiters) and runs it through `isPlaceholderSecret` rather than raw substring matching.
**Security-review-caught bug, fixed before commit:** the first version of `dsnHasPlaceholderPassword` did a raw `strings.Contains(lower, ":changeme@")`-style substring check directly against the whole DSN, with no trimming. `isPlaceholderSecret` (used for `ServiceKey`) already trimmed whitespace, but this sibling function didn't — so `API_DB_PASSWORD=" changeme"` (leading space) or `API_DB_PASSWORD="changeme\r"` (a CRLF-corrupted `.env` value — this repo has already hit a real CRLF bug once, see B-020) would build a DSN like `...: changeme@host...` or `...:changeme\r@host...`, and the un-trimmed substring check would miss it, silently starting with an effectively-default password. Fixed by extracting the password segment first and validating it through the same trimmed/lowercased `isPlaceholderSecret` path as every other secret, closing the inconsistency rather than patching the substring list.
**Test coverage — B-025:** new `eami-api/internal/config/config_test.go` (8 tests, package had none before): `validate()`-level tests for placeholder/empty rejection and real-value acceptance on both `ServiceKey` and `Database.DSN` (including the CRLF/leading-space regression cases above and an unparseable-DSN case), plus `Load()`-level integration tests (`TestLoad_RejectsUnsetServiceKey`, `TestLoad_RejectsPlaceholderDBPassword`, `TestLoad_AcceptsRealSecrets`) added after the general code-review pass flagged that pure `validate()` unit tests alone wouldn't catch a regression like someone dropping the `validate(cfg)` call from `Load()` or re-adding a hardcoded default — the `Load()` tests exercise the actual env-var wiring, the real attack surface, using `t.Setenv` and a nonexistent config path (`os.IsNotExist`-tolerated, matching how docker-compose configures every service via env vars, not by editing the shipped YAML). **Verified 2026-07-24 with a real toolchain: `go build ./...`, `go vet ./...`, `go test ./...` all clean, 0 failures** (full module). Independent reviewer + security subagent passes both ran against the diff; security review's one finding (the CRLF/whitespace gap above) was fixed and the full auth-bypass trace re-verified with no surviving path. **Manually verified live, not just via `go test`:** built and started `postgres`+`eami-api`+`eami-gateway` via `docker compose up -d --build` using this machine's real, already-generated `.env` secrets — both services started clean (`eami-api: database connected`, `... listening on :8081`); then, with the stack still running, ran `docker compose run --rm -e API_SERVICE_KEY=changeme eami-api` and `-e API_SERVICE_KEY= eami-api` and confirmed both print the exact `validate()` error and refuse to start, and separately confirmed `-e API_DB_PASSWORD=devpassword eami-api` is rejected the same way; stack torn down after (`docker compose down`).
**Out-of-scope observations logged, not fixed (see `BACKLOG.md`):** `eami-api/eami-api.yaml`/`eami-gateway/eami-gateway.yaml` still contain literal `"changeme"` values baked into the Docker image's default config file — no longer a security bug (`validate()` correctly rejects them if no env override is present), but a stale/misleading example config now inconsistent with this task's `.env.example` cleanup; `scripts/seed-db.sh`/`scripts/create-audit-partition.sh` still have a `${DATABASE_URL:-postgresql://eami_app:devpassword@...}`-style fallback, same silent-weak-default pattern this task closed elsewhere. Neither file was in this task's `MAY MODIFY` scope.

**Interfaces — B-026 (2026-07-24), persistent RSA key for user-session JWTs:** `internal/auth/auth.go`'s RS256 signing key **regenerated in memory on every container restart in the actual running system** — not because no persistence mechanism existed in the code (`NewService` already had a `loadPrivateKey` path for a non-empty `keyPath`), but because `internal/config/config.go` had **no env-var override for `cfg.Auth.RSAPrivateKeyPath` at all**, and neither `eami-api.yaml` (ships `rsa_private_key_path: ""`) nor `docker-compose.yml`/`.prod.yml` set one — so the docker-deployed system was always in ephemeral dev-mode, force-logging-out every user on every restart. Mirrors `eami-gateway/internal/identity`'s `loadOrGenerateKey` pattern (read as the reference implementation, not modified by this task): new `loadOrGenerateKey(path string) (*rsa.PrivateKey, error)` in `auth.go` tries the existing `loadPrivateKey` (unchanged — still supports both PKCS1 `"RSA PRIVATE KEY"` and PKCS8 `"PRIVATE KEY"` PEM formats, a bit more flexible than gateway's PKCS1-only loader), and only if the error is specifically `os.ErrNotExist` (checked via `errors.Is`, correctly unwraps through `loadPrivateKey`'s `%w`-wrapped `fmt.Errorf`) generates a new 2048-bit key, `os.MkdirAll`s the parent dir (`0700`), and writes PKCS1-PEM at `0600`. Any other load failure (corrupt file, wrong format, permission error) propagates as a real error instead of triggering silent regeneration — deliberately, since overwriting a corrupt-but-real key file would itself invalidate every outstanding token, the exact failure mode this fix exists to prevent. `NewService` now calls `loadOrGenerateKey` instead of the old `loadPrivateKey` when `keyPath != ""`; the `keyPath == ""` fully-in-memory ephemeral branch (dev mode, never touches disk) is untouched. New `API_JWT_KEY_PATH` env var wired into `config.go` (mirrors `GATEWAY_JWT_KEY_PATH`'s existing pattern exactly) overrides `cfg.Auth.RSAPrivateKeyPath`; `main.go` gained one `else` branch logging `"eami-api: using persistent RSA key at %s"` for parity with the pre-existing ephemeral-mode warning.
**Infra:** `docker-compose.yml`/`.prod.yml` — new named volume `api_certs` (not a bind mount, not baked into the image) mounted at `/certs` on the `eami-api` service, `API_JWT_KEY_PATH: /certs/api.key` added to its environment — mirrors the pre-existing `gateway_certs`/`GATEWAY_JWT_KEY_PATH` pattern exactly, distinct volume name (no collision). `scripts/setup.sh` — new `generate_api_keypair()`, a near-duplicate of the existing `generate_gateway_keypair()` (same openssl-in-a-throwaway-alpine-container mechanism, writes to `${project_name}_api_certs` instead of `${project_name}_gateway_certs`), called immediately after the existing gateway-keypair call; `API_JWT_KEY_PATH=/certs/api.key` added to the generated `.env`'s eami-api section. `.env.example` documents the new var next to the pre-existing `API_JWT_SECRET` entry. **Discovered, not fixed (out of scope):** `API_JWT_SECRET` (`.env.example`/compose/`config.go`) appears to be dead code — defined and generated by `setup.sh` but never read by any Go code (the service is 100% RS256; no HS256 fallback path exists despite the `.env.example` comment claiming one) — pre-existing, unrelated to this fix, not logged as a new B-ID since it's cosmetic/dead-config rather than a behavior gap.
**Test coverage — B-026:** new `eami-api/internal/auth/auth_test.go` (5 tests, package had zero tests before this): `TestNewService_GeneratesAndPersistsKey_WhenMissing`, `TestNewService_LoadsExistingKey_TokenValidAfterRestart` (the core contract proof — `Service` A issues a token at a temp key path, `Service` B constructed against the *same* path, simulating a restart, successfully verifies A's token, proving B loaded the identical key rather than minting a new one), `TestNewService_CorruptKeyFile_ReturnsError_NotOverwritten` (seeds garbage bytes at the key path, confirms `NewService` errors AND the file is byte-for-byte unchanged afterward — not silently regenerated over), `TestNewService_EphemeralMode_EmptyPathStillWorks` (regression guard for the untouched `keyPath == ""` branch), `TestNewService_MissingParentDir_CreatesIt`. **Verified 2026-07-24 with a real toolchain: `go build ./...`, `go vet ./...`, `go test ./...` all clean, 0 failures** (full module; `eami-gateway` module also re-verified clean and confirmed untouched by this change). Independent reviewer + security subagent passes both ran against the diff (mandatory per this task's standing rules, given it generates/persists key material for the first time in this code path) — both came back clean, no findings: security review traced every `fmt.Errorf`/`log.Printf` in the changed paths and confirmed key bytes never reach a log line or error message, confirmed `0600`/`0700` permissions are applied atomically at creation (no window of looser permissions), confirmed the `errors.Is(os.ErrNotExist)` branch correctly rejects (not silently regenerates over) permission-denied/corrupt-file errors, and confirmed `api_certs` is a genuine named volume with no collision against `gateway_certs`. **Manually verified live, not just via `go test`:** built and started `postgres`+`eami-api`+`eami-gateway` via `docker compose up -d --build` (confirmed `api_certs` volume auto-created, startup log now reads `"eami-api: using persistent RSA key at /certs/api.key"` instead of the old ephemeral warning); seeded a throwaway test org/user directly in Postgres, logged in via `POST /v1/auth/login` to get a real access token, confirmed it authenticates (`GET /v1/gateway/agents` → 200) *before* restart; ran `docker compose restart eami-api`; confirmed the **same token, no re-login**, still authenticates (200) *after* restart. Went further than the brief's minimum: also ran a full `docker compose down` (no `-v`) + `up` cycle and confirmed the volume survived and the same token still worked afterward too. Test org/user cleaned up from Postgres after verification; stack torn down.

**Interfaces — B-027 (2026-07-25), panic recovery for the alerting engine's background loop:** `internal/alerting/engine.go`'s `Engine.Run` — a `time.Ticker`-driven loop evaluating active alert rules once a minute — had no panic recovery anywhere: an unrecovered panic in `ListAllActiveAlertRules` or in evaluating any single rule would crash the whole `eami-api` process (chi's `Recoverer` only protects the HTTP request path; this loop runs outside it). New unexported `guard(name string, fn func())` in a new `recover.go` (local to this package — it's the only background/ticker goroutine in this module, so no cross-package helper was needed; see the `eami-gateway` section below for the sibling module's shared `internal/safego`, used by three call sites there). Two layers, both required: `Engine.evaluateRules` (extracted from `tick`'s existing per-rule `for` loop, same body, same error handling) calls `guard` around each rule's `evaluateRule` call individually — a panic evaluating one rule is logged and the loop moves to the next rule in the *same* tick, not just "recovers once and dies." `Engine.safeTick` (new, wraps `tick`) is defense-in-depth for anything that could panic outside the per-rule loop, e.g. `ListAllActiveAlertRules` itself — `Run`'s ticker loop calls `safeTick` instead of `tick` directly. No change to `tick`/`evaluateRule`'s actual logic or any existing `log.Printf` line — confirmed via diff (31 lines changed, all wrapper/extraction, zero business-logic edits).
**Test coverage — B-027 (alerting):** new `recover_test.go` (4 tests: panic doesn't propagate, no-panic runs normally, a panicking call doesn't prevent a subsequent call from running, panic value + name land in the log) and `engine_test.go` (new `fakeDBTX` implementing `store.DBTX` lets tests deterministically panic inside `QueryRow`/`Query` without a live Postgres connection): `TestEvaluateRules_PanicInOneRule_OthersStillEvaluate` proves acceptance criterion #1 directly — two rules, the first's metric query panics, and the test asserts the second rule's `QueryRow` was still reached (2 calls, not 1); `TestEvaluateRules_NoPanic_BothRulesEvaluateNormally` is the control case; `TestSafeTick_PanicListingRules_DoesNotCrash_AndNextTickWorks` proves the outer layer — a panic in `ListAllActiveAlertRules` on the first tick doesn't prevent a second `safeTick` call (simulating the next ticker fire) from completing normally. **Verified 2026-07-25 with a real toolchain: `go build ./...`, `go vet ./...`, `go test ./...` clean, 0 failures**, full module including all pre-existing packages (`api`, `auth`, `config`, `toolcreds`) confirmed unaffected. General code-review pass (covering both modules' diffs together, see the `eami-gateway` section below for the rest): clean — one trivial redundant `rule := rule` loop-variable capture in an early version (harmless under Go 1.22+'s per-iteration loop vars, since this codebase's toolchain note confirms `go1.26.5`) flagged and removed before commit. See `BACKLOG.md` B-027 for the full cross-module writeup (this task touched both `eami-api` and `eami-gateway`, separate Go modules, in one session).

**Interfaces — B-032 (2026-07-25), paste-detection groundwork: `paste_events` table + batch ingestion endpoint:** backend foundation for a future browser-extension paste-detection epic, built ahead of the extension itself (no live caller yet, verification uses synthetic requests) after an investigation this session concluded `discovered_endpoints`/`POST /v1/reports` was the wrong shape for it (dedup-by-`(org,source_host,method,host,path)` tuple with a single overwritten `last_seen` — can't answer "how many paste events happened, and when"; that investigation also found `/v1/reports` currently has **zero real producers** — `eami-collector`'s forwarder only ever posts to `/v1/ingest/batch`, a separate table/pipeline). New `POST /v1/reports/paste-events` (`internal/api/paste_events.go`, `IngestPasteEvents`), service-key auth (`requireServiceKey`, same middleware as `/v1/reports`), accepts a JSON array, returns `202 {"accepted": N, "rejected": N}`.
**Schema (`schema/migrations/008_paste_events.sql` + `schema.sql`):** `paste_events` — new TimescaleDB hypertable on `occurred_at` (append-only, no `ON CONFLICT`/dedup, one row per event — deliberately unlike `discovered_endpoints`), `org_id`/`source_endpoint_id` real FKs (`orgs`/`endpoints`, both `ON DELETE CASCADE`), `destination_domain` TEXT (not a FK — no domains dimension table exists anywhere in this schema), `content_length`/`content_hash` (coarse indicators only, nullable), optional anonymised `os_username` (mirrors `eami-agent`'s browser scanner `BrowserExtension.UserPath` convention — no employee/user-directory table exists to FK against; `users` is dashboard-login accounts, a different identity space). **Hypertable, not `audit_log`'s pg_cron monthly-partition pattern:** investigated both existing time-series conventions in this codebase and picked based on query shape, not habit — `audit_log`'s manual partitioning exists for its RLS insert-only policy + hash-chain tamper-evidence requirements (compliance-driven), which don't apply here; `paste_events` is pure time-series analytics shaped like `token_usage`, so it follows that table's `create_hypertable()` pattern (no PK on `id`, same as `token_usage`/`audit_log` — the partition column must be part of any unique constraint/PK). Three indexes for the actual reporting query shapes (not a generic `org_id`/`created_at` composite): `idx_paste_events_org` (org-only trend), `idx_paste_events_org_domain` (org+domain+time — the CIO-dashboard query), `idx_paste_events_endpoint` (per-machine drill-down). **Retention:** 90-day Timescale `add_retention_policy` (background-job-enforced, unlike `endpoint_reports`' comment-only "kept for 90 days" convention it matches numerically). **Migration idempotency fixed during review** (see below) to match `005_token_usage.sql`'s established `IF NOT EXISTS`/extension-guarded convention, which the first version didn't follow.
**Source-identity resolution — deliberately NOT `UpsertAgentEndpoint`:** new `ResolvePasteSourceEndpoint` (`internal/store/paste_events.sql.go`) finds-or-creates the `endpoints` row by `(org_id, agent_id)` like the existing `UpsertAgentEndpoint` (used by `ingest.go`'s full agent-scan pipeline), but only refreshes `last_seen` on conflict — it does not touch `hostname`/`agent_version`/`os_info`. `UpsertAgentEndpoint` unconditionally overwrites all three on every call; a paste event only ever carries a hostname (never agent_version/os_info), so reusing it here would silently blank out richer metadata the full `eami-agent` scanner already collected for that machine every time a paste event arrived. Confirmed against `UpsertAgentEndpoint`'s real definition during code review (not just asserted) and now has direct regression coverage (see tests below) proving scanner-written `agent_version`/`os_info` survive a subsequent paste event untouched.
**Batching — the gap this was built to avoid repeating:** `BatchInsertPasteEvents` writes an entire batch via a single multi-row `INSERT ... SELECT * FROM unnest($1::uuid[], ...)` (parameterized arrays, no string-built SQL) — one Postgres round trip regardless of batch size, unlike `reports.go`'s `IngestReports`, which calls `UpsertEndpoint` once per event in a loop (N round trips per request, flagged as a real gap by this session's prior investigation, not fixed here since it's out of this task's scope — `reports.go` itself was explicitly untouched). Proved quantitatively, not just by code reading: a `pgx.QueryTracer`-instrumented test asserts exactly 1 query fires for both a 1-event and a 50-event batch. One real behavioral difference from `reports.go`'s per-item resilience, caught by code review and worth knowing: because the write is one batch statement, a DB failure on `BatchInsertPasteEvents` fails the *whole* batch's already-validated events in one 500, rather than `reports.go`'s per-item partial-success pattern — low practical risk since every value reaching the batch has already round-tripped through a successful `ResolvePasteSourceEndpoint` (FKs pre-validated), but not free.
**Content-scrub guarantee, structural not conventional:** `PasteEventReport` (the request type) has a fixed field set — `org_id, agent_id, hostname, destination_domain, occurred_at, content_length, content_hash, os_username` — with no field capable of carrying raw pasted text, mirroring B-022's toolcreds "no field to carry it" pattern. Verified two ways: a reflection test asserting the exact field set (fails immediately if a future edit adds a carrying field), and a live test that POSTs a crafted payload with smuggled `"content"`/`"raw_text"` keys carrying a sentinel string against the real endpoint/DB and confirms the sentinel appears in no stored column — proving Go's default unknown-field-dropping behavior actually holds end-to-end, not just in principle.
**Domain allowlist — new list, not a new convention:** `internal/api/paste_domains.go`'s `KnownPasteDestinations`/`MatchesKnownPasteDestination` mirrors `eami-agent/internal/detection/network_activity`'s `KnownAIHosts`/`MatchesKnownAIHost` shape exactly (case-insensitive exact-or-subdomain-suffix match) but is a separate list, not an import — `eami-agent`'s list lives under an `internal/` package in a different Go module (`go.work`), which Go does not allow importing across module boundaries. Covers browser-facing UI domains (`chat.openai.com`, `claude.ai`, etc.), deliberately distinct from `KnownAIHosts`' API hostnames (`api.openai.com`) since paste events are reported against the UI a human pastes into, not the API host. Enforced server-side before any write (never trusts the client's own classification) — confirmed by a security review pass specifically checking for suffix-match bypasses (e.g. `chat.openai.com.attacker.com`), none found.
**Test coverage — B-032:** new `eami-api/internal/api/paste_events_test.go`, 9 tests, all integration-level against a real Postgres+TimescaleDB (no mock/fake layer exists for `*store.Queries` in this codebase — `reports.go`'s `IngestReports` has the same property and, notably, has **zero** test coverage; this sibling endpoint is now better-tested). Covers: structural + live no-raw-content guarantee (above), batch write correctness with no dedup/collapsing (25-event batch, same domain/source, distinct timestamps — proving `paste_events` doesn't inherit `discovered_endpoints`' collapsing behavior), the one-round-trip batching proof (above), server-side allowlist rejection (mixed valid/invalid-domain batch, confirms the rejected event never reaches a row), the `ResolvePasteSourceEndpoint`-vs-`UpsertAgentEndpoint` non-clobbering regression (added after code review flagged it as untested — the single riskiest piece of new logic), a `discovered_endpoints`/`reports.go` regression test proving the two ingestion paths coexist in the same process/DB with zero behavioral change to the pre-existing upsert-by-tuple hit-count semantics, and a seeded-volume performance test (below). **Verified 2026-07-25 with a real toolchain, including a real database, not just static review:** `go build ./...`/`go vet ./...` clean across all 5 workspace modules (`eami-api`, `eami-gateway`, `eami-policy`, `eami-collector`, `eami-agent`); `go test ./...` clean across `eami-api` (all packages, not just the new one) and `eami-gateway` (unmodified, sanity-checked). Docker Desktop was paused at session start (confirmed via the user before unpausing) — brought up via `docker desktop start`/`docker compose up -d postgres` against this repo's real `docker-compose.yml`, migration 008 applied by hand against the existing (non-fresh) dev database exactly as its own header instructs, then re-applied a second time after the idempotency fix to confirm the re-run is genuinely clean (`NOTICE: ... already exists, skipping` throughout, no errors).
**Performance verification (acceptance criterion, not assumed):** seeded 200,000 synthetic `paste_events` rows (2 orgs × 200 endpoints × 100k rows, 60-day spread, 5 rotating domains) directly via set-based SQL, then ran a real `EXPLAIN (ANALYZE, BUFFERS)` on the representative CIO-dashboard query ("paste events by domain, last 30 days, for org X") through the actual HTTP-independent store path. Result: `Index Only Scan` on `idx_paste_events_org_domain` for every TimescaleDB chunk touched, zero `Seq Scan` anywhere in the plan, 5.3ms total execution time. Full plan text captured in the test log (`TestPasteEvents_ReportingQuery_UsesIndex_AtRealisticVolume`, gated on `testing.Short()` since seeding takes real time).
**Reviewer + security subagent passes (mandatory per this task's standing rules — new privacy-sensitive ingestion surface):** both ran independently against the diff. **Security review:** no HIGH/MEDIUM-confidence new vulnerabilities — traced the parameterized `unnest()` batch insert (no SQL injection surface), the structural no-raw-content guarantee, and the domain-allowlist suffix-match logic for bypasses (none found); noted the endpoint reuses the existing client-supplied-`org_id`-under-service-key trust model already used by `/v1/reports`/`/v1/ingest/batch`/`/v1/internal/token-usage`, an existing pattern this change continues rather than a new deviation. **General code review** found and this session fixed three real issues before commit: (1) migration 008 was missing the `IF NOT EXISTS`/extension-guard idempotency pattern every prior migration since 005 established — fixed and re-verified by re-running the migration live against the same DB; (2) the `ResolvePasteSourceEndpoint`-vs-`UpsertAgentEndpoint` non-clobbering behavior (the riskiest new logic) had no test — added `TestResolvePasteSourceEndpoint_DoesNotClobberScannerMetadata` (first version had a false failure comparing `jsonb` output byte-for-byte instead of semantically — Postgres's `jsonb` column re-serializes with its own canonical spacing on read; fixed to parse-and-compare); (3) a trivial `gofmt` misalignment in the new test file's helper functions, fixed. Also flagged, not fixed (deferred, see `BACKLOG.md` B-033): `api/openapi.yaml` has no entry for the new route, unlike every other `reports.go` handler — out of this task's `MAY MODIFY` scope (`openapi.yaml` is owned by Architect-EAMI per `BOUNDARIES.md`).
**Known limitations:** no `GET` endpoint exists yet for `paste_events` — reporting/dashboard consumption is explicitly out of this task's scope (ingestion groundwork only); a materialized view / pre-aggregated daily rollup table was investigated per this task's brief and *not* built — the base table's indexes already prove fast enough at 200k-row volume via the real `EXPLAIN ANALYZE` above, so a rollup is a "when it's actually needed" optimization, not a day-one requirement. `api/openapi.yaml` not updated (see above, B-033). No browser extension exists yet — this endpoint has no live caller; verification is entirely synthetic-request-based, as the brief specified. ~~No `GET` endpoint exists yet~~ — **now built, see B-038 immediately below.**

**Interfaces — B-038 (2026-08-03), read-only admin API over `paste_events` (B2):** the `GET` gap flagged directly above is now closed. New `ListPasteEvents`/`CountPasteEvents` in `internal/store/paste_events.sql.go` (mirrors `ListAudit`/`CountAudit`'s exact shape: optional `Domain`/`From`/`To` filters via `NULL`-coalescing SQL, `Limit`/`Offset` pagination, a shared `listPasteEventsWhereSQL` fragment so the list and count queries can never drift out of sync on which rows match) and `Server.PasteEventsTimeSeries` (`internal/api/paste_events.go`) — raw `time_bucket()` SQL built directly in the handler via `s.queries.DB()`, mirroring `FinOpsTimeSeries`'s exact structure (dynamic bucket interval via a fixed `switch`, never user input, so no injection surface). Two new routes, `GET /v1/paste-events` and `GET /v1/paste-events/timeseries`, mounted in the existing admin/operator/viewer read group next to `/v1/audit`. **A deliberate improvement over the `FinOpsTimeSeries` pattern it mirrors:** that endpoint only returns total cost per bucket, and `FinOpsPage.tsx` distributes it across models *proportionally* as a display-only approximation (no real per-model-per-day breakdown exists server-side); `PasteEventsTimeSeries`'s query already groups by `(bucket, destination_domain)` directly, so its response is real per-domain counts, not an estimate.
**Data-source decision, made before writing any code (per this task's own brief, which required checking rather than assuming):** confirmed live via direct `psql` query that the real `Dev Org` has **zero rows** in `paste_events` — the 800,000 rows present in the table belong entirely to B-032's own disposable `paste-events-perf-*` synthetic test orgs, still left over from prior test runs. The only real, live-captured paste events (from B1/B-036's manual test pass) sit in `endpoint_reports`' raw JSON blob, the interim path B-035 exists to retire. Built this read API against `paste_events` anyway — it is the correct, purpose-built, indexed table the task's own contract named, and the alternative (reading the blob) would be throwaway work — but this means the new UI shows an honest empty state for any real org today. **B-035 is now the explicit next task**, raised to High priority in `BACKLOG.md`, specifically because it's the only thing separating this shipped, fully-tested feature from showing real data.
**No raw content, structurally:** `PasteEventResp` (`types.go`) carries only `id`, `destination_domain`, `occurred_at`, `content_length`, `content_hash`, `os_username` — literally the only columns `paste_events` has. There is no raw-content column anywhere upstream of this struct for it to accidentally expose; a reflection-based test locks the exact field set the same way B-032's own `PasteEventReport` test does for the write side.
**`api/openapi.yaml` not touched** (owned by Architect-EAMI, same reasoning B-032 used for the ingestion route) — widened B-033 to cover these two new routes too rather than filing a separate doc-gap item. Frontend calls them via `apiFetch`'s documented escape hatch (`client.ts`) instead of the generated typed client, matching `MemoryPage.tsx`'s pre-B-002-Brief-3 precedent for exactly this situation.
**Test coverage — B-038:** new `internal/api/paste_events_read_test.go`. 6 fast integration tests against a real Postgres: domain/time filtering (plus a reflection check locking `PasteEventResp`'s field set, AC4), pagination correctness (distinct rows across pages), org isolation (a second org's viewer sees zero rows from the first org's seeded events — proven via a real signed JWT through the actual `jwtMiddleware`/`requireRole`/`claimsFromContext` chain, not a bypass; needed its own `newReadEnv` test harness since `paste_events_test.go`'s existing `pasteEventsTestEnv` was built with `authSvc=nil`, sufficient for its own service-key-only ingestion tests but not for these JWT-protected read routes), timeseries bucket/domain grouping correctness, and timeseries input validation (missing from/to, invalid granularity). Plus 2 real `EXPLAIN (ANALYZE, BUFFERS)` tests, reusing B-032's own 100,000-row-per-org seeded-volume convention (`seedPasteEventsPerfData`, a second org seeded alongside so the org filter is actually selective) rather than inventing a new one — proving the *exact* queries `ListPasteEvents` and `PasteEventsTimeSeries` issue use `idx_paste_events_org_domain`/`idx_paste_events_org` respectively, zero `Seq Scan`, at that volume (list query: 0.224ms; timeseries aggregation over 100k rows: 42ms). **Verified 2026-08-03 with a real toolchain: `go build ./...`, `go vet ./...` clean; `go test ./...` clean across the full module** (not just the new file) **including the two seeded EXPLAIN ANALYZE tests run without `-short`.**
**Known limitations:** none new for the backend; inherits B-035's gap noted above (empty for real orgs until that ships). ~~empty for real orgs until B-035 ships~~ — **B-035 now done, see immediately below.**

**Interfaces — B-035 (2026-08-04), paste events wired into the real `paste_events` table:** closes the gap B-034/B-036/B-038 each explicitly flagged and deferred. **Investigated rather than assumed, per this task's own brief:** re-confirmed `eami-agent` still has no `org_id` anywhere, and B-032's original client-facing `POST /v1/reports/paste-events` still trusts a per-event client-supplied `org_id` directly — not reusable as-is for agent-originated data. **Corrected the brief's own assumption**: `eami-collector` does not resolve `org_id` anywhere in its code (confirmed by grep across the whole module) — it's a pure blind relay (`/v1/ingest` → SQLite buffer → forwarder → `POST /v1/ingest/batch`), authenticated only by a single collector-wide service key. The only place `org_id` is ever resolved is `eami-api`'s `IngestBatch`, via the pre-existing `GetDefaultOrgID` ("oldest org") single-tenant placeholder. Traced the wire format end-to-end: `eami-agent`'s native-messaging relay (B0) already sends paste events through the **existing, completely unmodified** `/v1/ingest` → buffer → forwarder → `/v1/ingest/batch` pipeline (B0's `outboundReport{agent_id, hostname, collected_at, paste_events}`, forwarded as opaque bytes throughout) — so **`eami-agent` and `eami-collector` needed zero code changes**, a real simplification versus the brief's own initial sketch (which anticipated a new `eami-collector` relay route). The entire fix is confined to `eami-api`.
**The actual fix, `internal/api/ingest.go`:** `agentReport` gained a `PasteEvents []rawPasteEvent` field. `processIngestItem` now branches on `rep.PasteEvents != nil` (checked via nil-ness, not `len() > 0` — see the code-review-caught bug below) to a new `processPasteEventRelayItem`, which resolves the endpoint via `ResolvePasteSourceEndpoint` (B-032's non-clobbering upsert, only touches `last_seen`) and writes straight into `paste_events` via `BatchInsertPasteEvents` — never calling `UpsertAgentEndpoint`/`InsertEndpointReport`/any normalised-table insert for that item. A normal full-scan report's path is completely untouched. `orgID` is always the value `IngestBatch` already resolved server-side before `processIngestItem` is ever called — `agentReport` has no `OrgID` field anywhere in its struct tree, so nothing in the JSON a compromised `eami-agent` controls can influence which org its paste events land in, structurally, not by convention.
**Real correctness bug found by the design itself, before any code was written:** a paste-event-only relay report has no scan data (blank `agent_version`/platform). Routing it through the pre-existing `UpsertAgentEndpoint` (which unconditionally overwrites `agent_version`/`os_info` on every call, by design, for real scans) would have silently clobbered a real endpoint's already-known metadata — the exact bug class `ResolvePasteSourceEndpoint` was built to avoid for B-032's own direct-caller path. Avoided entirely by design, not patched after the fact: paste-relay items never reach `UpsertAgentEndpoint` at all.
**Shared validation, not duplicated:** extracted `validatePasteEvent` (`paste_events.go`) — domain-allowlist check (`MatchesKnownPasteDestination`, never trusts client classification) + RFC3339 timestamp parse + lowercasing — used by **both** the new relay path and the original `IngestPasteEvents` (refactored to call it, zero behavioral change to its existing accept/reject counting, confirmed by the full pre-existing test suite passing unmodified).
**Reviewer + security subagent passes (mandatory, org-resolution trust boundary):** both ran independently. **Security review:** no High/Medium findings. Confirmed org resolution cannot be spoofed (structurally, not just empirically — `agentReport` has no field to carry one) and confirmed the design stays safe even after `GetDefaultOrgID` is eventually replaced by real multi-tenant resolution, *as long as* whatever replaces it keeps deriving `orgID` from an authenticated channel rather than from request-body fields — flagged as the one thing to preserve, not a bug today. Confirmed full SQL parameterization throughout (`unnest()`-based batch insert, parameterized `ON CONFLICT` upsert). One **Low**: no length caps on `content_hash`/`os_username`/`destination_domain` or on the paste-events-per-item array size (this endpoint is reachable directly by anything holding the collector's service key, bypassing B0's own 1MB native-messaging protocol cap entirely) — **fixed**: added `maxDestinationDomainLen`/`maxContentHashLen`/`maxOSUsernameLen` to `validatePasteEvent` (applies to both entry points) and `maxPasteEventsPerRelayItem = 100` to the relay path. **Code review:** one **Medium-High** real bug, caught before it ever shipped: the original branch condition was `len(rep.PasteEvents) > 0`, which can't distinguish "the `paste_events` key is absent from the JSON" from "the key is present as an empty `[]`" (Go's `encoding/json` sets a slice to `nil` only in the former case) — a relay item with an explicitly-empty-but-present array would have silently fallen through to the clobbering scan-report path. **Fixed** by checking `rep.PasteEvents != nil` instead, and added `TestIngestBatch_EmptyPasteEventsArray_StillDoesNotClobber` to lock it in. Also caught and fixed a **Medium**: a hypothetical future item carrying both real scan data and `paste_events` together would have silently discarded the scan data with no log line — now logs a warning (defensive; today's two report shapes remain structurally mutually exclusive, confirmed, not just assumed). One **Low** dead-code note (an unreachable `agentID == ""` guard) cleaned up in the same pass.
**Test coverage — B-035:** new `internal/api/ingest_paste_relay_test.go`, 6 integration tests against a real Postgres, all sending the *exact* JSON shape B0's real relay produces (not a hand-simplified proxy): a relayed event lands in `paste_events` and produces zero `endpoint_reports` rows (AC1/AC4); a smuggled fake `org_id` in the relay payload is provably inert — confirmed both that the resulting row uses the real resolved org and that no org row was ever created for the fake ID (AC2); the server-side domain allowlist still rejects an unrecognized domain on this path; a normal full-scan report in the same batch shape is completely unaffected, still writing `endpoint_reports`/normalised tables exactly as before (AC5); the centerpiece, `TestIngestBatch_PasteEventRelay_DoesNotClobberScannerMetadata`, seeds real scanner metadata via a genuine full-scan item, then relays a paste event for the same `agent_id` with a *different* hostname, and confirms `hostname`/`agent_version`/`os_info` all survive untouched; and the empty-array regression test added during review. Because `GetDefaultOrgID` resolves to "the oldest org in the whole database" (not a disposable per-test org, unlike B-032's own synthetic-org tests), every test asks the server what its actual default org is via the same `GetDefaultOrgID` call `IngestBatch` itself uses, rather than assuming one, and cleans up via a uniquely-prefixed `agent_id` (`ON DELETE CASCADE` from `endpoints`) so nothing outside what each test created is ever touched. **Verified 2026-08-04 with a real toolchain: `go build ./...`, `go vet ./...` clean; `go test ./...` clean across the full `eami-api` module** (not just the new file), **plus `eami-agent`/`eami-collector`/`eami-gateway` all still build clean** (confirming the "zero changes needed" claim above, not just asserting it). A genuine environment hiccup was hit and diagnosed mid-task, unrelated to this code: this machine's `localhost` resolves IPv6-first (`::1` before `127.0.0.1`), and Docker Desktop's IPv6 port-forward on this machine accepts the TCP handshake but silently drops all subsequent data — confirmed via a minimal standalone Go probe isolating the exact failure point, worked around for test runs via `TEST_DATABASE_URL` forcing IPv4 (no code changes), full Docker stack confirmed healthy afterward via `127.0.0.1`.
**Known limitations:** none — this was the last explicitly-tracked gap in the paste-detection epic (B-032→B-034→B-036→B-038→B-035, in build order). `GetDefaultOrgID`'s single-tenant-v1 design itself is unchanged and out of scope (pre-existing, not introduced here) — see the security review note above for what must be preserved if/when it's replaced.

**Interfaces — B-045 (2026-08-06), Tools page gains Edit:** the Tools admin page had `ListTools`/`CreateTool`/`DeleteTool`/`TestTool` and no way to correct a mistake short of delete-and-recreate — confirmed as a real, live gap during B-044's own manual testing (a wrong `base_url` needed a raw SQL fix). New `PATCH /v1/gateway/tools/{toolId}` (`store.UpdateTool` + `Server.UpdateTool`) is a partial update using the exact `COALESCE($n, column)` convention `UpdateAgent`/`agents.sql.go` already established in this codebase — a `nil` Go value means "leave unchanged." Applied to `credentials_encrypted` specifically: omitted `credentials` in the request → `nil` bytes passed → `COALESCE` preserves the existing encrypted blob; `credentials` present → re-encrypted via `CreateTool`'s exact same `credentialsProvided`/`s.toolCreds.Encrypt` path, same fail-closed guarantee if no cipher is configured. `type`/`auth_type` deliberately not editable (matches `UpdateAgent`'s precedent of only exposing operational fields). `api/openapi.yaml` deliberately not touched (Architect-EAMI-owned) — ships undocumented, matching B-038's precedent.
**Investigated the "is this a broader pattern" question directly, per the task brief:** Tools was the *only* resource missing Edit. `Agents`/`Policies`/`Settings` already had `PATCH`/`PUT` routes — confirmed via `router.go`'s full route table, not assumed.
**A real, necessary scope correction, confirmed with the user before building:** the frontend's generated OpenAPI client has no typed call for an undocumented `PATCH`; the documented fallback, `apiFetch()` in `eami-ui/src/api/client.ts` (CLAUDE.md's own "no raw fetch in components" escape hatch), was GET-only. Extended it with optional `{method, body}` — small, generic, reusable by future edit flows on other undocumented endpoints, not tool-specific — rather than writing a raw `fetch()` in the component.
**AC4 (no stale routing after an edit), verified not assumed:** `toolrouter/router.go` (B-044) has no caching — `Resolve` is a direct `pool.QueryRow` on every dispatch, grepped to confirm. Proven with a new real-Postgres test, `TestResolve_PicksUpEditedConfig_NoStaleCache`: dispatch to server A, edit `base_url` via a real `UPDATE` (the same effect `UpdateTool`'s `COALESCE` produces), dispatch again, confirm server B is hit.
**Reviewer + security passes both ran** (mandatory, credential handling): security review found the core logic sound (independently re-derived that `toolcreds.Cipher.Encrypt` can never produce a zero-length ciphertext, so "submitted" vs "omitted" credentials can never collide) but flagged a real gap — every credential-preservation test ran against a fake store, never real Postgres. **Closed**: new `tools_update_pg_test.go`, 4 real-Postgres tests using the real `store.Queries.UpdateTool`, reading real stored bytes back and decrypting them to confirm the actual end state (including that a rotation produces the NEW plaintext and the OLD one is genuinely gone, not just "no error"). Code review caught and this session fixed two real gaps before shipping: (1) **Medium** — `UpdateTool` had no empty-`name` validation, unlike `CreateTool`'s identical check; `{"name":""}` would have silently blanked a tool's name via `COALESCE` — fixed. (2) **Low** — the Edit UI's `mcp_args` handling treated whitespace-only input as "provided" (JS truthiness), producing an explicit `[]` that would silently wipe existing stored args — fixed to check `.trim()` first. A follow-up focused review, including running the new real-Postgres tests live, confirmed both fixes correct and complete.
**Test coverage:** 17 new — 10 fake-store unit tests (`tools_update_test.go`: persistence, credential preserve/rotate/fail-closed, no-leak response, not-found, invalid-id, empty-name rejected), 4 real-Postgres tests (`tools_update_pg_test.go`: credential preservation and rotation against real encrypted bytes, cross-org isolation, `mcp_args` preservation verifying pgx's nil-slice-to-NULL array encoding directly), 1 real-Postgres routing test in `eami-gateway` (AC4, above), plus `tsc && vite build` clean via the Docker builder-stage trick (Node/npm still absent locally), run twice — before and after the `mcp_args` fix. **Verified 2026-08-06 with a real toolchain: `go build`/`go vet`/`go test ./...` clean across `eami-api` and `eami-gateway`.**
**Live-verified end-to-end against the real `docker-compose` stack, rebuilt fresh first:** edited the real "Test" `gateway_tools` row from B-044's own manual-testing session via a real admin JWT (a throwaway admin user seeded for this session, deleted afterward) — renamed it, then changed `base_url` to a distinguishable real `postman-echo.com` path. A single subsequent real `tool_call` through the live gateway proved AC2 and AC4 simultaneously: the dispatch reached the **edited** `base_url` (not stale data), while the **original** credential — set during B-044's testing, never touched by these edits — was still applied correctly as a real `Authorization` header. `base_url` restored to a clean value afterward; all throwaway fixtures cleaned up.
**Known limitations, documented not silently left:** no way to clear `base_url`/`mcp_command` to empty via the Edit panel (a blank field means "no change," matching the credential field's own established semantic, not "clear it"); a rename colliding with another tool's name in the same org surfaces as a raw `500` with the Postgres error text, not a clean `409` (pre-existing pattern shared with `CreateTool`, not introduced here).

**Interfaces — B-046 (2026-08-06), `gateway_tools.action_paths` persistence + validation:** full dispatch-side writeup in the `eami-gateway` section above. This module owns validation and storage: `ActionPathMapping` (`tools.go`) mirrors `toolrouter.ActionPathEntry`'s JSON shape exactly (a deliberate duplicate, not shared — same B-025/B-044 cross-module-`internal/`-visibility constraint). `validateActionPaths` normalizes and validates a submitted map: non-empty action name and path required, method uppercased and restricted to a fixed `GET/POST/PUT/PATCH/DELETE` allow-list (defaulting to `POST`, matching `toolrouter.Forward`'s own default), and — the one piece of hardening added directly from the security review's trace-through (see `eami-gateway` section) — a path containing `"://"` is rejected outright as a likely full-URL paste-in mistake, even though it was proven not to be an actual host-confusion vector. `GatewayTool.ActionPaths`/`CreateToolParams.ActionPaths`/`UpdateToolParams.ActionPaths` are raw JSONB `[]byte` passthrough, matching `AlertRule.ConditionConfig`'s existing convention exactly — no new wrapper type needed.
**`UpdateTool`'s tri-state semantics, following `MCPArgs`' existing precedent exactly:** `action_paths` omitted from the PATCH body (or explicit JSON `null`) → `nil` map after decode → `COALESCE` leaves existing mappings untouched. `action_paths` present as **any** object, including an empty `{}` → non-nil map → encoded as the literal bytes `"{}"` when empty so `COALESCE` still overwrites → this is how an admin explicitly clears all mappings, a real affordance intentionally added (not just left as a side effect) since without it mappings could be set but never fully removed short of raw SQL.
**Reviewer + security subagent passes:** both automated subagent passes failed on a platform session-limit error (see `eami-gateway` section) — substituted with a direct manual review plus the empirical `joinURLPath` host-confusion trace-through described there, which is what motivated the `"://"`-rejection hardening above.
**Test coverage:** 11 new handler-level tests (`tools_action_paths_test.go`, fake-store-based, mirroring `tools_test.go`/`tools_update_test.go`'s convention): valid mappings persisted with method uppercased, omitted-method defaults to `POST`, empty-path/unsupported-method/full-URL-path all rejected with the store never called, omitted-on-update leaves `ActionPaths` nil, explicit `{}` produces the literal clear signal, `ToolResp.ActionPaths` round-trips correctly and is omitted entirely (`omitempty`) for a tool with none. 3 new real-Postgres tests (`tools_action_paths_pg_test.go`, same `TEST_DATABASE_URL`/`POSTGRES_PASSWORD` convention as `tools_update_pg_test.go`): `CreateTool`'s JSONB bind round-trips exactly, `UpdateTool`'s `COALESCE` genuinely preserves real stored mappings when omitted, and genuinely clears them when given the literal `{}` — proving the tri-state semantics against real Postgres, not just a fake store. **Verified 2026-08-06 with a real toolchain: `go build ./...`, `go vet ./...` clean; `go test ./...` clean across the full `eami-api` module**, including all real-Postgres tests.
**Known limitations:** duplicate action names within a single submitted `action_paths` object silently resolve to "last one wins" (ordinary Go map/JSON-object semantics, not flagged as an error) — acceptable for a hand-edited admin form, not surfaced as a UX warning in this iteration.

**Interfaces — B-055 (2026-08-10), first-boot setup wizard (B-053 follow-up):** the last open gap B-053's own README flagged ("the next brief needs its own race-safety design for whatever mechanism lets the first visitor claim admin") is closed. New `internal/api/bootstrap.go`: three pre-auth routes (no user exists yet to authenticate as) — `GET /v1/setup/status` (`{configured: bool}`, drives the frontend's post-setup redirect), `POST /v1/setup/token/validate` (read-only token check, reveals the org/admin form without consuming anything), `POST /v1/setup/bootstrap` (the real atomic operation). New `setup_tokens` table (`schema/migrations-v2/000003_add_setup_tokens.up.sql`) and a new `Queries.Begin(ctx)` helper (`internal/store/db.go`) so `Bootstrap` can run inside one real `pgx.Tx`.
**Concurrency guard, the brief's central requirement — a real DB lock, not check-then-act:** `Bootstrap` runs `SELECT id, expires_at, consumed_at FROM setup_tokens WHERE token_hash = $1 FOR UPDATE` inside its transaction. Two concurrent requests for the same token genuinely serialize on that row lock — the loser re-reads the committed `consumed_at` after the winner's transaction commits, not a stale in-memory value. The independent "system still unconfigured" check (`SELECT count(*) FROM orgs`) runs inside the *same* transaction, so it can't itself race a concurrent bootstrap either. Every failure path returns before `tx.Commit()`; the deferred `tx.Rollback()` discards any partial work (including token consumption), which is also the abandoned-session-recovery mechanism — an interrupted attempt (crash, dropped connection, VM reboot mid-request) never leaves the token spent or the system half-configured; a fresh attempt with the same still-valid token succeeds normally.
**Trust boundary:** the raw token is a `openssl rand -hex 32` (256-bit) value generated appliance-side (`appliance/scripts/eami-stack.sh`, see `appliance` section below) and printed only to the VM's console/journal output — the same channel B-053 already established as the trusted emergency-access boundary (SSH is permanently disabled). It is never transmitted, logged, or echoed anywhere over the network by `eami-api`; `BootstrapResponse` carries only `org_id`/`org_name`/`admin_email`. Rate limiting (`setupRateLimiter`, hand-rolled in-memory fixed-window, no new dependency) on `/token/validate` and `/bootstrap` is defense-in-depth only — the token's 256-bit entropy is the real defense against guessing.
**A real deviation from B-053's own stated assumption, found and required to be surfaced rather than worked around silently:** B-053's README said "whatever serves the wizard reads `/data/eami/state` directly." That's not actually possible — `docker-compose.prod.yml` is frozen for this brief too, and nothing bind-mounts `/data/eami` into any container, so `eami-api` has no way to read that host file. The real gate implemented here is DB-based (`SELECT count(*) FROM orgs`), which is also strictly more correct (always live, no separate file-sync step). `/data/eami/state` still exists and is best-effort-reconciled by `eami-stack.sh`, but is now informational only — see `appliance/README.md`'s rewritten contract section.
**TLS tradeoff, stated rather than silently accepted:** these routes run over the same plain-HTTP-only exposure every other `eami-api` route already has in this appliance (`docker-compose.prod.yml` has no TLS anywhere — `/v1/auth/login` already sends real admin passwords over plaintext HTTP today). The wizard introduces no new class of risk beyond what already ships; full TLS is correctly out of this brief's scope, tracked as the same pre-existing, already-accepted residual.
**Test coverage:** `bootstrap_test.go`, real-Postgres integration tests, each creating its own throwaway database (mirrors `schema/migrationtest`'s per-test pattern, since that module can't be imported directly — migrations applied by reading the real `schema/migrations-v2/*.up.sql` files via pgx's simple query protocol, which supports the multi-statement files the extended protocol can't run in one `Exec`). Covers: token validation (missing/unknown/expired/already-consumed/valid), a real concurrent race (`TestBootstrap_ConcurrentRequests_ExactlyOneWins` — 5 goroutines racing one token, asserts exactly 1 success and exactly 1 row in `orgs`/`users`), abandoned-session recovery (`TestBootstrap_InterruptedTransaction_TokenStaysValidForRetry` — locks and "consumes" the token in a transaction that's then rolled back instead of committed, confirms a subsequent real attempt still succeeds), the full state transition (`/v1/setup/status` false→true, a second bootstrap attempt with a *different* valid token still rejected once configured — proving the system-state check is independent of token validity), rate limiting, input validation, and a real login proof (`TestBootstrap_Success_LoginWorksAndTokenIsDeadAfterward` — creates an org/admin, logs in via the real `POST /v1/auth/login` with the created credentials, confirms a real JWT comes back, confirms the token can't be reused). **Verified 2026-08-10 with a real toolchain: `go build ./...`/`go vet ./...` clean; `go test ./...` clean across the full `eami-api` module** (all 8 new tests passing against real Postgres, plus the full pre-existing suite unaffected).
**Reviewer + security subagent passes: both ran, and code review found a real, high-value gap — fixed before shipping, not glossed over.** Security review (an independent subagent pass plus this session's own direct trace-through) found no exploitable findings — confirmed no network-only path exists to create an org or to leak/derive the raw token, confirmed no SQL injection (every query parameterized), confirmed no secret is ever logged or echoed in a response.
Code review found **one real Medium-severity concurrency gap contradicting this brief's own explicit requirement**: the original design's `SELECT ... FOR UPDATE` on the `setup_tokens` row only serializes two requests that share the *same* token — it does nothing for two requests each holding a *different*, independently valid, unconsumed token, which could both observe `orgCount == 0` under READ COMMITTED and both insert an org. In normal operation only one unconsumed token ever exists (`eami-stack.sh` deletes any prior one before minting a new one), but that's an operational invariant enforced by a shell script, not a database guarantee — exactly the "not just app-level check-then-act" gap the brief warned against. **Fixed** with a global `pg_advisory_xact_lock(hashtext('eami_setup_bootstrap'))` held for the transaction's lifetime, serializing *every* concurrent `Bootstrap` call regardless of which token it holds. **Reproduced the bug empirically before trusting the fix, not just reasoning about it**: temporarily disabled the new lock and ran the two-different-tokens regression test 5 times — it failed 3 of 5 (a genuine, non-deterministic race, exactly as theorized) — then restored the fix and confirmed it passes reliably. New test `TestBootstrap_ConcurrentRequests_DifferentValidTokens_ExactlyOneWins` added specifically to catch a regression of this exact gap (the original `TestBootstrap_ConcurrentRequests_ExactlyOneWins`, using one shared token, could not have caught it).
Three smaller findings also fixed: (1) `SetupWizardPage.tsx`'s `onSubmitBootstrap` error handler set the error message on the wrong form (`bootstrapForm`, which was about to be unmounted by the very same `setStage('token')` call) — a real bug caught in this session's own manual re-read, found before the code-review agent even ran, fixed by setting it on `tokenForm` instead. (2) The server's email regex required a literal dot in the domain, stricter than the frontend's zod `.email()` check, so a client-accepted address like `admin@localhost` could be server-rejected — loosened to match. (3) Combined with (2): `onSubmitBootstrap` unconditionally sent the admin back to re-enter the 64-character console token on *any* failure, even a fixable input error — now `apiFetch` throws a status-carrying `ApiFetchError`, and only a 401 (dead token) or 409 (already configured) triggers the return-to-token-gate; every other failure keeps the admin on the form with their already-typed org/admin details intact. One finding intentionally left as a disclosed limitation, not fixed: `setupRateLimiter`'s map never removes a key once created, even after its entries expire — real but low-severity for a single-appliance pre-auth endpoint with no realistic reason to see traffic from thousands of distinct IPs; a correct fix needs a background sweep goroutine, judged not worth the added complexity here.
**Live-verified end-to-end against a real, isolated, genuinely-empty stack, twice** (`docker compose -p eami-wizard-live-test`/`-test2`, fresh throwaway Postgres volumes, reusing the already-built local `eami-api`/`eami-gateway`/`eami-ui` images — not the shared dev stack). First pass, pre-fixes: generated a real console-style token via the exact commands `eami-stack.sh` runs, confirmed `GET /v1/setup/status` → `false`, validated the token (`204`), completed `POST /v1/setup/bootstrap` (`201`, real org+admin created), **logged in for real with the created credentials** (`POST /v1/auth/login` → `200`, real signed JWT returned — AC2's actual requirement), confirmed `/v1/setup/status` flipped to `true`, confirmed re-submitting the same token was cleanly rejected (`401 "setup token has already been used"`). Second pass, after rebuilding the image with all the code-review fixes above: repeated the identical flow end-to-end, this time deliberately using a dot-less-domain email (`admin@localhost`) to prove the email-regex fix live, not just in a unit test — bootstrap succeeded (`201`), login succeeded (`200`, real JWT), status flip and reuse-rejection both reconfirmed. Confirmed `eami-ui`'s dev server actually serves the `/setup` route and its router module resolves to the real `SetupWizardPage` component (not just type-checked in isolation). Real `npm run type-check`/`npm run build` both clean, run after every code change including the post-review fixes (Node/npm newly installed this session via `winget` — previously absent on this machine, see `CLAUDE.md`'s toolchain note, now resolved). Both throwaway isolated projects fully torn down afterward (`docker compose down -v`).
**An incidental, pre-existing discovery while checking the real dev stack was untouched, not caused by this task's own code:** the dev database's org count read 29 before this session and 31 partway through — turned out to be 30 synthetic `paste-events-perf-a`/`-b` fixtures accumulated since 2026-07-25 across many prior sessions' `go test ./...` runs (`paste_events_read_test.go`'s perf-seed test registers a `t.Cleanup` that isn't reliably firing), leaving only 1 genuine org (`Dev Org`/`dev@example.com`) once cleaned up. Confirmed by name/timestamp before deleting anything, confirmed the real org was untouched afterward. The leaked rows were deleted; the underlying test-cleanup bug itself was not investigated or fixed (out of this task's scope) — logged as **B-056**.
**Not performed: interactive browser click-through** — browser automation tools were unavailable this session (the user chose not to complete the Claude-in-Chrome install); the frontend's actual served bundle and routing were confirmed live via direct HTTP requests and source inspection instead, and the wizard's own client-side logic (status check → gate → form → bootstrap) is a thin, already-build-verified consumer of the exact backend contract proven live above.
**Post-push CI failure, real and fixed — two distinct bugs, not one, confirmed by reading the actual job logs rather than assumed:** the first push broke `Test — schema/migrationtest` and `Test — eami-api` in CI. (1) `migrate_test.go` hardcoded an expected schema version of 2 in four places; B-055's new migration (000003) makes the real version 3 — a stale test expectation, not a defect in the migration mechanism itself (every other module's migration-apply CI step, including `eami-api`'s own, succeeded). Fixed all four checks, gave the fresh-vs-incremental comparison test a third `Steps(1)` call (was silently missing `setup_tokens`, causing a table-count mismatch), added `setup_tokens` to the fresh-schema spot-check list. (2) A **separate bug**, in `bootstrap_test.go` itself: its connection helper copied `migrate_test.go`'s narrower env-var pattern (checks `TEST_DATABASE_URL` is set, but reads the password from `POSTGRES_PASSWORD` separately) — CI's matrix `test` job (which runs `eami-api`) only ever sets `TEST_DATABASE_URL`, never `POSTGRES_PASSWORD` (`build.yml` already has a comment, predating this session, explaining exactly why `test-migrationtest` needs both and the matrix job doesn't). Fixed by parsing `TEST_DATABASE_URL` as a full DSN, matching every other `eami-api` real-Postgres test file's existing convention. Both fixes verified locally by reproducing CI's exact env-var setup (`TEST_DATABASE_URL` only) before the second push — **confirmed genuinely green via the GitHub API afterward: zero failed jobs, Docker image builds (gated on both test jobs) also succeeded.**
**Known limitations:** the full Packer/QEMU appliance image was not rebuilt from scratch this session (B-053 already proved that boot mechanism separately in its own session; this brief's only change to it is the token-generation step added to `eami-stack.sh`, which is itself exercised for real above via the identical commands) — a from-scratch appliance rebuild is a reasonable follow-up if the founder wants the literal "boot the actual `.qcow2`" step repeated, not done here given it was already proven mechanically sound and unrelated to this brief's actual new logic.

**Interfaces — B-058 (2026-08-12), Multi-Hop Workflows Brief 1 — schema + CRUD foundation only (Thread B epic, briefs 2-7 not built):** new `workflows`/`workflow_steps` tables (`schema/migrations-v2/000006`), new `internal/api/workflows.go` (`GET/POST /v1/gateway/workflows`, `GET/PATCH/DELETE /v1/gateway/workflows/{workflowId}`), new `internal/store/workflows.sql.go`. Role gating identical to `gateway_tools`: `requireRole("admin","operator")` for write, `requireRole("admin","operator","viewer")` for read. `api/openapi.yaml` deliberately not touched — ships undocumented, matching B-038/B-045's precedent.
**Data model:** `workflow_steps.gateway_tool_id UUID REFERENCES gateway_tools(id) ON DELETE SET NULL` (not a blocking pre-check) — `DeleteTool` was out of scope to modify this brief and deletes unconditionally today, so a default-`RESTRICT` FK would have turned that untouched flow into a raw constraint-violation the moment any workflow referenced the deleted tool. A step with a deleted connector comes back from `GetWorkflow`/`ListWorkflowSteps` with `gateway_tool_id`/`tool_name` both null — surfaced, not hidden. `input_mapping JSONB` on `workflow_steps` is a genuinely inert placeholder for a later brief (output→input mapping between hops); never read or written beyond NULL passthrough here. v1 is strictly linear (`step_order INTEGER`, `UNIQUE(workflow_id, step_order)`) — no branching columns, per the Thread B investigation's own finding that branching is unbuildable until connector output normalization exists.
**Cross-org guard — the load-bearing security-relevant piece of this brief:** `ToolBelongsToOrg` (`SELECT EXISTS(... WHERE id=$1 AND org_id=$2)`), called by `validateWorkflowSteps` for every step on both `CreateWorkflow` and `UpdateWorkflow`, before any row is written. The FK constraint alone only proves a `gateway_tool_id` exists *somewhere* in `gateway_tools` — never that it belongs to the caller's own org; without this explicit application-level check, org A could reference org B's connector id directly. `UpdateWorkflow` additionally confirms workflow ownership (`GetWorkflow(id, uc.OrgID)`) before opening a transaction, and the transaction's own `UPDATE ... WHERE id=$1 AND org_id=$2` is independently org-scoped too (no TOCTOU between the pre-check and the mutating statement).
**sqlc note:** `workflows.sql.go` is hand-written, not `sqlc generate`-produced — the sqlc CLI is confirmed not on `PATH` on this machine (same reason `tools.sql.go` predates it as hand-written). A real `.sql` source file (`internal/store/query/workflows.sql`) was added anyway so a machine with sqlc available can regenerate for real later without drift.
**Test coverage:** `workflows_test.go` — 4 real-Postgres integration tests: full CRUD lifecycle (create with 2 steps inside a transaction, list with joined step count, get with joined tool names, update via full-replace reorder/add/remove, delete with cascade verified via a direct query); cross-org isolation (Get/Update/Delete all no-op/404 for another org's workflow, plus a full HTTP-level test via a real `httptest` server + real JWT proving `CreateWorkflow` rejects a step referencing another org's real connector with a clean 400 and zero rows created); dangling-reference behavior (a real `DeleteTool` call against a referenced connector leaves the workflow intact with the affected step's `gateway_tool_id`/`tool_name` both null). Plus `workflows_internal_test.go` (3 unit tests for the FK-violation-detection helper, see code review below). **Verified 2026-08-12 with a real toolchain: `go build ./...`, `go vet ./...`, `go test ./... -count=1` all clean across the full `eami-api` module.**
**A real test-authoring bug found and fixed live, not application code but worth recording — it reproduces a bug this exact file (`BACKLOG.md`, B-056) already documents once:** the first version of `workflows_test.go` used `defer pool.Close()` alongside `t.Cleanup`-registered per-org `DELETE FROM orgs` calls. `t.Cleanup` runs in a phase strictly *after* a function's own `defer`s finish, so the pool was already closed by the time any cleanup DELETE ran — every single cleanup silently no-op'd. Reproduced live: a full test run left 14 leaked `wf-*` orgs in the shared dev database (confirmed via `psql`). Fixed by switching every test to `t.Cleanup(func() { pool.Close() })`, registered *before* any org-seeding helper call so LIFO cleanup ordering runs the org deletes first, then closes the pool last — re-ran, confirmed zero leakage, shared dev DB back to just `Dev Org` for the remainder of the session (verified repeatedly, including after the final full-suite run and the live docker-compose verification below).
**Reviewer + security passes both ran.** Security review (this session, direct data-flow trace rather than a delegated pass, since org-isolation is always security-relevant per standing rules): traced org-scoping on every query, the `ToolBelongsToOrg` cross-org guard, `UpdateWorkflow`'s pre-mutation ownership check, confirmed all SQL parameterized (no injection surface) and no `dangerouslySetInnerHTML` anywhere in the new frontend code — zero findings, reported via the empty-findings path rather than manufacturing speculative issues. **Code review found 4 real code issues plus 1 real documentation-consistency gap, all fixed:**
1. `WorkflowsPage.tsx`'s step-row editor (`rowsToSteps`, now `validateAndConvertRows`) silently filtered out any incomplete row on submit — including a row flagged `toolDeleted` (a deleted connector's step, exactly what AC4's recovery flow depends on an admin fixing). Editing an unrelated field without touching the broken row would silently delete it instead of preserving it for repair. Fixed: submission is now blocked with a clear "Step N is incomplete" error naming the row, never a silent drop.
2. This file's own Thread B entry said "NOT YET SCOPED, no B-ID assigned" in the very diff that shipped a fully-built B-058 under that epic — fixed (see the Thread B entry above, now reflecting Brief 1 DONE).
3. `WorkflowUpdateRequest`'s doc comment incorrectly implied `{"steps": []}` was a supported way to clear a workflow to zero steps — `validateWorkflowSteps` actually rejects empty arrays unconditionally (a workflow needs at least one step to mean anything). Comment corrected to state this plainly; no behavior change, since the "bug" was documentation, not code.
4. A real, if narrow, TOCTOU: `validateWorkflowSteps`' `ToolBelongsToOrg` checks run before the transaction opens, so a connector deleted by a concurrent request in the gap between validation and the transactional `INSERT` surfaced as a raw 500 (unhandled FK violation) instead of a clean error. Fixed with a new `isForeignKeyViolation(err) bool` helper (checks Postgres SQLSTATE `23503`, mirroring `tool_connectivity.go`'s already-established `classifyPgConnectError` convention for checking `pgErr.Code`), applied on both `CreateWorkflow`'s and `UpdateWorkflow`'s `InsertWorkflowStep` calls — now a clean 400 ("one of the referenced connectors no longer exists"). Covered by 3 new pure unit tests (`workflows_internal_test.go`, constructed `*pgconn.PgError` values, no DB needed, same pattern as `TestClassifyPgConnectError_*`).
5. `UpdateWorkflow` discarded the row its own `UPDATE ... RETURNING` produced and then issued a second, redundant `GetWorkflow` after commit to reconstruct the same data. Fixed to reuse the already-fetched row directly, cutting one full DB round-trip from every successful `PATCH`.
One finding left as a disclosed, not-fixed style note: `AddWorkflowPanel`/`EditWorkflowPanel` are near-duplicate ~65-line components — matches this codebase's own pre-existing `AddToolPanel`/`EditToolPanel` precedent in `ToolsPage.tsx`, not a new departure from convention, so not treated as a defect requiring a fix in this brief.
**Live-verified end-to-end against the real `docker-compose` stack, rebuilt fresh (before AND after the code-review fixes, to prove both the original flow and the fixes themselves):** logged in as `dev@example.com` (password reset to a known value for this session, no other credential available — same disclosed precedent as B-045/B-046/B-054; the user may want to reset it again). Created a real workflow (`b058-live-verify`) with 2 real steps against the real `Test`/`b046-live-verify` `rest_api` connectors — confirmed via List (`step_count: 2`) and Get (both steps with correctly joined `tool_name`). Edited it live: renamed, activated (`draft`→`active`), reordered the 2 existing steps, and added a 3rd step referencing the real `claude` `ai_provider` connector — proving both connector types work as workflow steps, and that reorder+add all land correctly via the full-replace update path. Deleted it: `204`, subsequent `GET` `404`, `workflow_steps` row count confirmed `0` at the DB level (cascade). Post-code-review-fix smoke test confirmed both new validation paths return clean `400`s live (`"at least one step is required"` for an empty `steps` array; the cross-org/not-found guard for a nonexistent `gateway_tool_id`) rather than 500s. **Confirmed via direct grep that `eami-gateway`'s entire module (including `cmd/gateway/main.go`'s dispatch closure) has zero references to `workflow`/`workflows` anywhere** — this brief is genuinely definition-only; nothing executes a workflow yet. `eami-api`/`eami-ui` containers logged clean (no errors/panics) throughout. Shared dev database confirmed to contain only `Dev Org` both before this session's work and after every verification step, including the test-cleanup-bug incident above.
**Known limitations, disclosed not hidden:** no live interactive browser click-through of `/gateway/workflows` was performed (confirmed the route resolves with a real `curl` against the running Vite dev server instead, `200`, and the full CRUD lifecycle was proven via direct API calls that exercise the identical handler code a browser session would trigger) — matches the standing limitation most recent `eami-ui` briefs in this environment have disclosed the same way. No automated frontend test exists for the new `validateAndConvertRows`/`StepsEditor` logic (matches this repo's pre-existing 0%-frontend-test-coverage baseline, `BACKLOG.md` B-006, not a new gap introduced by this brief).
**Dependencies:** none (Brief 1 has no dependency on later Thread B briefs — see `BACKLOG.md`'s B-058 entry). **Out of scope, as specified in the task brief:** workflow execution, output→input mapping logic, per-hop TOCTOU pinning, escalation/approval integration, audit logging integration, branching/conditional logic.

**Interfaces — B-070 (2026-08-17), rate limiting on login and the setup wizard:** `POST /v1/auth/login` had zero rate limiting — a real credential-stuffing/brute-force surface, since this is the only real self-service auth path in the system. **A scope correction found during research, before building, not assumed from the task brief's own framing:** the brief's premise ("no rate limiting exists anywhere in this stack") wasn't accurate — B-055's `bootstrap.go` already rate-limited its setup routes per-IP (in-handler, not middleware), just without a `Retry-After` header. New `internal/api/ratelimit.go`: `setupRateLimiter` (B-055's original type) extracted and generalized into a shared `rateLimiter`, with `Allow` now returning `(ok bool, retryAfter time.Duration)` instead of a bare `bool`. New `internal/api/ratelimit_login.go`: `rateLimitLogin` chi middleware wraps `POST /v1/auth/login` (`router.go`'s only change to this route besides the wrapper), checking **both** a per-IP limiter (keyed on `middleware.GetClientIP`, the same non-spoofable source B-047's `ClientIPFromRemoteAddr` migration already established) and a per-account limiter (keyed on a case/whitespace-normalized email) — per-IP alone doesn't stop credential stuffing spread across many accounts from one IP; per-account alone doesn't stop one IP hammering many different accounts. The account key is extracted by peeking the request body and restoring it byte-for-byte, so **`Login()` itself has zero lines changed** — confirmed via diff. New `config.RateLimitConfig` (all six values — `LOGIN_RATE_LIMIT_PER_IP`/`_WINDOW_SECONDS`, `LOGIN_RATE_LIMIT_PER_ACCOUNT`/`_WINDOW_SECONDS`, `SETUP_RATE_LIMIT`/`_WINDOW_SECONDS` — individually env-overridable, matching this file's established pattern), defaults 20/5min (IP) and 5/5min (account) — account tighter than IP since one IP legitimately represents many real users behind NAT/a corporate proxy, while one account should never see 20 password attempts in 5 minutes from anyone legitimate; setup mirrors B-055's original hardcoded 10/15min, now configurable instead of fixed in code. `bootstrap.go`'s `ValidateSetupToken`/`Bootstrap` gained exactly one line each (`setRetryAfter(w, retryAfter)`) — their actual validation/DB-write logic is unchanged.
**Three real bugs found by this task's own mandatory security review, all fixed before shipping:**
1. **Fail-open on a body-read error.** `io.ReadAll(r.Body)` can return a non-nil `err` alongside a body it had *already fully read* (e.g. a malformed chunked-transfer trailer arriving after every real byte). The original code treated any read error as "skip rate limiting entirely, forward the request" — letting a syntactically valid, fully-readable credential-guess bypass both limiters. Fixed by ignoring the read error and always counting whatever bytes were captured against the IP limiter before forwarding.
2. **Parser disagreement let an attacker downgrade account-level protection to the looser IP-level one.** The middleware parsed the peeked body with `json.Unmarshal` (errors on trailing data) while `Login()`'s own `decodeJSON` uses a streaming `json.NewDecoder(...).Decode` (silently ignores anything after the first JSON value). A body with one byte appended (`{...}X`) would fail the middleware's parser — skipping the tighter per-account limiter — while still parsing normally in `Login()`, letting an attacker brute-force one account at the IP limit (4x the intended rate) just by appending garbage to every request. Fixed by switching the middleware to the identical `json.NewDecoder(...).Decode` call `Login()` uses, so both paths agree on what counts as "the same request."
3. **Non-positive rate-limit panic**, shared with the `eami-gateway` B-070 fix above: `rateLimiter.Allow` indexed `kept[0]` once `len(kept) >= rl.limit`, immediately true for `limit <= 0` on the very first request — a misconfigured (e.g. typo'd `"0"`) env var would crash every login attempt rather than disable limiting. Fixed with a fail-closed guard in `Allow` itself, plus `config.go`'s `validate()` now rejecting any of the six new values that are `<= 0` at startup (same "fail loudly at boot" convention as every other required secret/value in that function).
**Reviewer + security subagent passes, both mandatory (explicit security-hardening brief):** code review's automated pass found all three issues above; fixed, then the full test suite (including 3 new regression tests specifically reproducing each bug against the pre-fix code path) re-run clean.
**Test coverage:** `internal/api/ratelimit_login_test.go` (4 black-box tests via `MockStore`/`httptest`: per-account trip after 5, per-IP trip after 20 across distinct unseeded accounts, 2 genuine typos then a real successful login unaffected, case-insensitive account-key folding); `internal/api/ratelimit_login_internal_test.go` (white-box, `package api`: the body-read-error regression via a `flakyBody` that mimics `io.ReadAll`'s real partial-data-plus-error shape; the non-positive-limit fail-closed regression via direct `newRateLimiter(0/-1/-100, ...)` construction; the trailing-garbage account-key regression via a body with one appended byte); `internal/api/ratelimit_bootstrap_test.go` (real-Postgres, reusing `bootstrap_test.go`'s exact `newBootstrapTestEnv` fixture: token-validate trips after 10 with a real `Retry-After` header, and confirms the shared per-IP limiter also blocks a *genuinely valid* Bootstrap call while tripped — not just repeats of the same invalid request; a full below-threshold status→validate→bootstrap flow unaffected); `internal/config/config_test.go` gained `TestValidate_RejectsNonPositiveRateLimitValues`/`TestValidate_AcceptsPositiveRateLimitValues`/`TestLoad_RejectsZeroRateLimitEnvVar`, plus `validConfig()`'s fixture updated to include `RateLimit: DefaultRateLimitConfig()` (a real regression this task caused and fixed in the same session: the bare zero-value `RateLimit` the old fixture left in place would have made every one of that file's pre-existing "accepts a valid config" tests fail once the new bounds check landed). **Verified 2026-08-17 with a real toolchain: `go build ./...`, `go vet ./...`, `go test ./...` clean across the full `eami-api` module**, zero regressions in any pre-existing test.
**Live-verified end-to-end against the real `docker-compose` stack, rebuilt fresh (before AND after the security-review fixes):** rapid-fired 6 wrong-password login attempts against one real (throwaway, seeded-then-deleted-afterward) account — the first 5 returned real `401`s, the 6th a real `429` with `Retry-After: 294` — then confirmed even the *correct* password was rejected while rate-limited (the limiter gates before `Login()` ever checks credentials). Separately proved AC5 live: 2 genuine typos against that same account returned `401`, then a 3rd attempt with the correct password returned a real `200` with a real access token — a legitimate user is never falsely blocked. Rapid-fired 22 login attempts across 22 distinct unseeded accounts from one source IP — cumulative with the prior test's requests (same real IP, same real process), the per-IP limiter tripped at exactly the 21st request overall, matching the configured 20/5min threshold precisely. Rapid-fired 11 setup-wizard token-validate attempts against the (already-configured) real appliance — 10 real `409`s (already-configured, expected), the 11th a real `429` with `Retry-After: 897` (~15min window). All test fixtures (one throwaway org/admin user) created directly in Postgres and deleted afterward; final rebuild/redeploy re-confirmed both services healthy post-fix.
**Known limitations, disclosed not hidden:** no rate limiting on any other `eami-api` route (`/v1/auth/refresh`, API-key creation, etc.) — a natural, separate follow-up, matching this brief's own explicitly stated scope boundary (login + setup-wizard + workflow-run only, the three highest-risk endpoints identified). Distributed/multi-instance rate limiting is out of scope (ADR-020 Model A is single-instance).

---

## eami-ui
**Purpose:** React SPA — Dashboard, Discover, Gateway (Agents/Policies/Tools/Nodes), Approvals, FinOps, Memory, Paste Detection, Audit, Alerts, Settings.
**Status:** STABLE (all 14 pages are real implementations, 105–707 lines each; no bare placeholders remain).
**Key files:** `src/pages/{auth,dashboard,discover,finops,gateway,ops,settings}/*.tsx`; `src/hooks/*.ts` (15 resource hooks); `src/api/client.ts` (generated-client wrapper + documented `apiFetch()` escape hatch).
**Interfaces:** consumes `eami-api` exclusively via the OpenAPI-generated client (`npm run generate-client` → `src/api/schema.ts`, gitignored, build-time artifact — not present in a fresh checkout until generated).
**Data owned:** none (client only); Zustand for local UI/auth state.
**Test coverage:** **0** automated (no vitest/jest/playwright config exists anywhere in the repo, despite `BOUNDARIES.md` assigning `vitest.config.ts` + `playwright/` to QA-EAMI). `tsc --noEmit` itself still can't run directly (no Node/npm on this machine) — **but `npm run build` (`tsc && vite build`) can be, and was, run for real via `docker build --target builder -f eami-ui/Dockerfile .` from the repo root (B-024, 2026-07-24)** — this builds the `builder` stage of `eami-ui/Dockerfile`, which does `npm ci && npm run generate-client && npm run build` inside a `node:20-alpine` container, so `schema.ts` gets generated from `api/openapi.yaml` first (solving the "requires generate-client to run first" blocker noted below) and `tsc` runs for real. **This is a reusable verification path for any future eami-ui change on this machine** — no live `docker compose up`/browser click-through was performed for B-024 specifically (out of proportion for a narrow badge/error-parsing change already type-checked for real), but the `docker build --target builder` path itself is a genuine typecheck+build, not a manual read-through.
**Known limitations:**
- `SettingsPage.tsx:494` — email notification channel is explicitly disabled ("Coming soon — email dispatch is stubbed"); Slack/webhook channel is live.
- `MemoryPage.tsx` fetches directly via `apiFetch()` rather than a dedicated `useMemory.ts` hook — inconsistent with every other page's hook-per-resource pattern (all other 12 pages have one). Not broken, just a convention drift.
- `useAgents.ts:84,98` use raw `fetch()` for agent-config GET/PUT — likely a legitimate use of the documented "endpoint not yet in OpenAPI spec" escape hatch, not confirmed against the spec this session.
- `@ts-expect-error` count is 0 (TASK-049 fully closed).
**Interfaces — B-024 (2026-07-24), `ToolsPage.tsx` reads the real `TestTool` result:** `handleTest` previously only checked whether the `POST /v1/gateway/tools/{toolId}/test` call threw — since B-023 made that endpoint always resolve with HTTP 200 and put the real result in the body (`{success, latency_ms, error}`), the button always showed green "OK" regardless of actual reachability. Fixed: `handleTest` now reads `result.success`, and on failure parses B-023's `"<reason>: <detail>"` `error` string (`classifyTestError`, splits on the first `:`) into one of `auth-failed`/`unreachable`/`misconfigured`/a generic `failed` fallback for anything unrecognized. A new `TEST_STATE_CONFIG` map gives each state a distinct badge (green `success`, amber `auth-failed`, red `unreachable`, gray `misconfigured` via a new `HelpCircle` icon, red `failed`) reusing the same color language as the page's existing `StatusBadge` (connected/degraded/disconnected) rather than inventing new colors. The full backend message is surfaced via the button's `title` tooltip (e.g. hovering a failed test shows "auth-failed: remote returned HTTP 401"). Auto-clear delay extended from 4s to 6s since there's now a specific reason worth reading. Scope was strictly `ToolsPage.tsx`'s `handleTest` + its status-display button — `useTools.ts`, `AddToolPanel`, delete/list flows, and every other page are untouched (confirmed via `git diff --stat`, one file changed).

**Interfaces — B-038 (2026-08-03), `PasteEventsPage.tsx` — first admin view over captured paste-detection events (B2):** new page at `/paste-events`, new sidebar entry under the `Operations` group (`Navigation.tsx`, `Copy` icon — already used elsewhere in this codebase, so guaranteed present in the installed `lucide-react` version without needing `npm install` to confirm). **Placement decided against Discover, with reasoning recorded rather than just picked:** `DiscoverPage.tsx` is a per-endpoint asset inventory (one row per machine, drill into a detail drawer); paste events are an org-wide time-series event log, structurally identical to `AuditPage.tsx`/`FinOpsPage.tsx` (filter bar + paginated table + a `time_bucket`-driven chart), not to Discover's shape — so this is a new sidebar item, not nested under Discover.
**UI:** filter bar (domain select from the same bundled 6-domain list as `eami-api`'s `KnownPasteDestinations`/the browser extension's `domains.js`, from/to date range defaulting to the last 30 days) drives both a stacked `recharts` `BarChart` (counts by domain per day — real data straight from `PasteEventsTimeSeries`, no client-side estimation needed, unlike `FinOpsPage.tsx`'s per-model proportional-distribution workaround) and a paginated table (timestamp, domain, length, a click-to-copy truncated hash cell mirroring `AuditPage.tsx`'s `HashCell`, OS user). Empty state is deliberately generic ("No paste events detected yet — events appear here once the browser extension is installed...") with **no mention of B-035 or any internal wiring gap** — that context belongs in `BACKLOG.md`/this file, not in production UI copy shown to an admin.
**New hook, `src/hooks/usePasteEvents.ts`:** `usePasteEvents`/`usePasteEventsTimeSeries`, both via `apiFetch` (not the generated typed `api.GET`, since these two routes aren't in `api/openapi.yaml` yet — see B-033's widened scope) — same escape-hatch convention `MemoryPage.tsx` used for its episode hooks before B-002 Brief 3 documented those routes.
**Verification:** real `tsc && vite build` via the `docker build --target builder -f eami-ui/Dockerfile .` path B-024 established (Node/npm still absent locally) — clean, zero type errors, confirming both new files and the `Navigation.tsx`/`router.tsx` edits compile correctly against the rest of the app. The already-running dev container (bind-mounted, Vite) served the new page/route with no compile error logged after a direct request. **No live interactive browser click-through performed** — same standing limitation as B1 (no safe browser-automation tool in this environment; see the elevated-shell/`taskkill` incident logged under B-036/`CONTEXT.md`) — manual verification steps below cover what a human should confirm.
**Manual verification steps (not yet run by a human as of this entry):** 1) log in, confirm "Paste Detection" appears in the sidebar under Operations and navigates to `/paste-events`; 2) confirm the domain filter, date range, and pagination controls all actually refetch (network tab shows a new `GET /v1/paste-events`/`.../timeseries` per change); 3) ~~confirm the chart renders with no data today for the real `Dev Org`~~ — **B-035 shipped 2026-08-04: real paste events now flow into this table, so this page should show live data for any org with actual browser-extension activity** — re-verify the chart/table populate correctly with real rows (zero code changes were needed here, as intended); 4) if the buffer is still empty, confirm that's because no real paste events have occurred yet, not because the pipeline is broken.
**Known limitations:** `api/openapi.yaml` gap noted above (B-033); no export/alerting integration, explicitly out of scope per this task's brief. ~~empty for the real `Dev Org` until B-035 ships~~ — **B-035 done, see `eami-api` section.**

**Interfaces — B-045 (2026-08-06), `ToolsPage.tsx` gains an Edit flow:** full writeup in `eami-api` section above (backend + credential-handling detail); this entry covers the frontend-specific pieces. New `EditToolPanel` (near-identical structure to the existing `AddToolPanel`, pre-filled from the already-in-hand `useTools()` list row — no new GET endpoint needed). `type`/`auth_type` render read-only with an explanatory note (not editable, matching the backend's own scope decision). The credential input always starts blank (the current value is never returned by any API response, by design) with "leave blank to keep the current value" placeholder copy — submitted only if the admin actually types something, so `credentials` is entirely omitted from the PATCH body otherwise, never sent as an explicit `{}`/`null`. New `useUpdateTool()` hook in `useTools.ts`, and a small generic extension to `apiFetch()` (`src/api/client.ts`): optional `{method, body}` params (previously GET-only), used here because the generated OpenAPI client has no typed call for the new undocumented `PATCH` route — confirmed backward compatible with `apiFetch`'s only two existing callers (`MemoryPage.tsx`, `usePasteEvents.ts`, both call it with no second argument, unaffected by the new optional param). **Verified:** real `tsc && vite build` via the established `docker build --target builder` path, clean, run twice (before and after a code-review-caught fix to the `mcp_args` whitespace-handling — see `eami-api` section for detail). **No live interactive browser click-through performed** — same standing limitation as other recent frontend work in this environment; the PATCH flow itself was live-verified end-to-end via direct API calls against the real deployed stack (see `eami-api` section's B-045 entry) rather than through a browser.

**Interfaces — B-046 (2026-08-06), `ToolsPage.tsx` gains an Action mappings editor:** new `ActionPathsEditor` component (dynamic add/remove rows of action name / HTTP method / path), rendered in both `AddToolPanel` and `EditToolPanel`, `rest_api`-only (hidden entirely for `mcp`/`database` tools, matching the existing `base_url` field's own conditional-render pattern). `rowsToActionPaths` converts the row array to the `{action: {path, method}}` shape the API expects, silently dropping any row with a blank action name or path (an in-progress blank row shouldn't become a stored mapping to `""`) — duplicate action names resolve last-row-wins, matching the backend's own documented behavior for the same case.
**A real TypeScript typing problem, solved without touching the generated schema:** `Tool`/`ToolCreate` are frozen types generated from `api/openapi.yaml` (Architect-EAMI-owned, and `action_paths` isn't documented there — same undocumented-route situation B-045's `ToolUpdate` already established a precedent for). Solved with local intersection types in `useTools.ts`: `ToolWithActions = Tool & {action_paths?: ...}` (for reading the field back out of a list/edit response) and `ToolCreateWithActions = ToolCreate & {action_paths?: ...}` (for the create body) — a wider type is structurally assignable wherever the narrower generated type is expected, so `api.POST`/`api.GET`'s existing typed calls still compile unchanged, and the extra field survives at runtime (`JSON.stringify` doesn't know or care about TS's static type narrowing). `ToolUpdate` (already local, from B-045) simply gained an `action_paths?` field directly, no intersection needed there.
**Edit-panel prefill, submit semantics matching `mcp_args`' established precedent:** `EditToolPanel`'s row state initializes from `tool.action_paths` (present on the list response since `eami-api`'s `toolToResp` now includes it). On submit, `action_paths` is **always** sent from current row state (never diffed against the original) when the tool is `rest_api` — the same "reflect current form state, not a diff" convention every other field in this panel already uses — which is also how an admin explicitly clears all mappings: empty the rows, submit, the resulting `{}` is the backend's documented "clear" signal.
**Reviewer + security subagent passes:** both automated subagent passes failed on a platform session-limit error (see `eami-gateway` section) — the frontend-specific surface (the intersection-type trick, row-editor logic) was covered by this session's own substituted manual review; no exploitable surface here regardless, since all real validation/security enforcement happens server-side (`eami-api`'s `validateActionPaths`, `eami-gateway`'s dispatch-time guards) — this component only shapes what gets submitted, never trusts what it receives.
**Verification:** real `tsc && vite build` via the established `docker build --target builder` path — clean, zero type errors, confirming the intersection-type approach actually compiles against the real generated schema (not just plausible in isolation). **No live interactive browser click-through performed** — same standing limitation as B-045/other recent frontend work in this environment; the Action-mappings UI's actual request/response shape was proven correct indirectly, via the live end-to-end API+gateway verification in the `eami-gateway`/`eami-api` sections above (same JSON shape the UI constructs and consumes), not through a browser session.
**Known limitations:** no client-side warning for duplicate action names entered in the editor (silently last-row-wins on submit, matching the backend); no path-format hint/validation in the UI itself before submit (a full-URL paste-in is caught server-side with a clear 400, surfaced via the existing generic "Failed to add/update tool" toast — not a field-level inline error, consistent with every other validation failure in this form today).

**Interfaces — B-055 (2026-08-10), `SetupWizardPage.tsx` — the first-boot setup wizard (full backend design in `eami-api` section above):** new top-level `/setup` route (`router.tsx`, outside `AppShell` like `/login` — no auth guard applies, correctly, since no user exists yet). On mount, calls `GET /v1/setup/status`; an already-configured appliance redirects straight to `/login` via `<Navigate replace>` before rendering any form (closes the "wizard is unreachable after setup" requirement). Otherwise a two-step form matching `LoginPage.tsx`'s existing structure/Tailwind conventions and `react-hook-form`+`zod` pattern: step 1 is a single setup-token field, checked against the new read-only `POST /v1/setup/token/validate` (does not consume the token); step 2 (org name, admin email, admin password + confirm) only renders after step 1 succeeds, and submits everything together to `POST /v1/setup/bootstrap`. Success shows a confirmation with a link to `/login` — deliberately no auto-login, since AC2's "verified by actually logging in" means a real subsequent login, not an implicit session.
**A real bug caught in this session's own manual re-read, before the automated code-review pass even ran:** `onSubmitBootstrap`'s original catch block set the failure message on `bootstrapForm`'s error state, then immediately called `setStage('token')` — which unmounts the form that error was set on, so the admin would silently bounce back to the token screen with no visible reason why. Fixed by setting it on `tokenForm` instead when the failure really is token-related.
**A second issue, found by the code-review pass, fixed alongside the first:** *every* bootstrap failure forced the admin back to re-enter the 64-character console token, even a merely fixable input error — needlessly punishing, and it interacted badly with a real server/client validation mismatch also found (see `eami-api` section: the server's email regex was stricter than this page's zod `.email()` check, so `admin@localhost` was accepted here but 400'd on the server). Now `apiFetch` throws a new status-carrying `ApiFetchError` (`client.ts`), and `onSubmitBootstrap` only returns to the token gate for a `401` (dead token) or `409` (appliance configured by someone else) — every other failure keeps the admin on the form with their already-typed org/admin details intact, and shows the error there instead.
**`apiFetch` (`src/api/client.ts`) gained error-body surfacing (`ApiFetchError extends Error`, carries `status` alongside `message`):** previously threw a generic `HTTP ${status}`; now best-effort-parses the server's `{code, message}` body (`ErrorResponse`, matches every `eami-api` handler's `writeError` convention). Backward compatible — falls back to the old message shape for non-JSON bodies, still `instanceof Error`, and every existing `apiFetch` caller (`MemoryPage.tsx`, `usePasteEvents.ts`, `useTools.ts`) only ever checked `catch()` truthiness, never the exact string or type.
**Verified:** real `npm run type-check`/`npm run build` (`tsc && vite build`), both clean — run again after every fix above, not just once before review. Node/npm were newly installed this session (previously absent, per `CLAUDE.md`'s toolchain note). Live-verified against a real isolated stack, twice (before and after the fixes) — see the `eami-api` section's B-055 entry for the full end-to-end proof (token → validate → bootstrap → real login, second pass using a dot-less-domain email to prove the validation-mismatch fix); the served `/setup` route and its resolved `SetupWizardPage` component were confirmed directly over HTTP against the real running `eami-ui` dev server. **No interactive browser click-through** — browser automation was unavailable this session (declined Claude-in-Chrome install); disclosed, not glossed over.

**Interfaces — B-058 (2026-08-12), `WorkflowsPage.tsx` — new page for Multi-Hop Workflows Brief 1 (full backend + cross-org-guard design in `eami-api` section above; this entry covers the frontend-specific pieces):** new route `/gateway/workflows`, new sidebar entry (`Navigation.tsx`, `Workflow` icon from `lucide-react`, positioned after Tools within the Gateway group). Structure mirrors `ToolsPage.tsx` exactly: table + slide-out `AddWorkflowPanel`/`EditWorkflowPanel` drawers + `ConfirmDialog` for delete. New `StepsEditor` component reuses `ActionPathsEditor`'s reorderable-rows shape (`ToolsPage.tsx`) — add/remove rows — extended with explicit up/down move buttons, since a workflow step's order is load-bearing (unlike `action_paths`' rows, which have no inherent order). Each step row is a `<select>` populated from the existing `useTools()` hook (already fetches this org's connectors — no new data source needed) plus an action-name text input.
**Dangling-connector UX:** a step whose `gateway_tool_id` comes back empty from the API (its connector was deleted, `ON DELETE SET NULL`) renders with a red warning icon and a "connector removed -- select a new one" placeholder in the dropdown, rather than silently defaulting to a blank/wrong selection.
**New `useWorkflows.ts` hook** — mirrors `useTools.ts`'s exact shape (`useWorkflows`/`useWorkflow(id)`/`useCreateWorkflow`/`useUpdateWorkflow`/`useDeleteWorkflow`), all via `apiFetch()` (routes undocumented in `openapi.yaml`, same convention as `useUpdateTool`), query key `['workflows']` invalidated wholesale on mutation.
**A real bug found by the mandatory code-review pass, fixed:** the step-row-to-request-body conversion (`rowsToSteps`) originally silently filtered out any incomplete row — including a row explicitly flagged as having a deleted connector, i.e. exactly the row an admin is supposed to be able to see and fix per the dangling-connector UX above. Editing an unrelated field (renaming the workflow, reordering a different step) without touching that broken row would silently delete it on submit instead. Fixed: renamed to `validateAndConvertRows`, now blocks submission with a clear "Step N is incomplete -- select a connector and an action, or remove it" error instead of ever silently dropping a row.
**Verified:** real `npm run type-check`/`npm run build`, clean, run twice (before and after the code-review fix). Live-verified against the real running stack: created/edited/deleted a real workflow via direct API calls exercising the same handler code the page's `apiFetch()` calls trigger (see `eami-api` section's B-058 entry for the full transcript); confirmed via `curl` that `GET /gateway/workflows` resolves (`200`) against the real running Vite dev server, and that the dev server's hot-module-reload picked up the new page/route with no compile error logged. **No interactive browser click-through performed** — same standing limitation as every other recent `eami-ui` brief in this environment (browser automation unavailable/declined); the actual request/response shapes the page constructs and consumes were proven correct via the direct API verification instead, not through a browser session.
**Known limitations:** no automated frontend test exists for `validateAndConvertRows`/`StepsEditor` (matches this repo's pre-existing 0%-frontend-test-coverage baseline, `BACKLOG.md` B-006, not a new gap); `AddWorkflowPanel`/`EditWorkflowPanel` are near-duplicate components, matching `AddToolPanel`/`EditToolPanel`'s own pre-existing precedent (disclosed code-review finding, not fixed, consistent with existing convention).

**Interfaces — B-060 (2026-08-12), `WorkflowsPage.tsx`'s step "action" field becomes a real picker instead of free text.** Explicitly adjacent to Thread B (feeds its UX), not itself part of Thread B's epic — per the MCP Discovery investigation's own distinction (D8) between the workflow-chaining epic and MCP-protocol/connector-definition work that happens to feed it. Only file changed. `StepsEditor`'s `tools` was retyped from `any` to `ToolWithActions[]` (mirrors `ToolsPage.tsx`'s own identical pattern), giving real typing for `action_paths` (B-046) — already fetched by the existing `useTools()` call, no new endpoint or hook needed, confirming D8's own finding directly before building against it. Per row: if the selected connector's `action_paths` has ≥1 key, a real `<select>` of those action names replaces the `<input>`; a connector with none (`ai_provider`/`mcp`/`database`, or a `rest_api` tool with no mappings yet) falls back to free text completely unchanged, matching `action_paths`' own existing "unmapped falls back to base_url" convention with zero special-casing. A saved action no longer among the connector's current `action_paths` keys is appended as an extra dropdown option rather than dropped, so loading a workflow never silently mutates saved data. The connector `<select>`'s `onChange` now unconditionally resets `action: ''` in the same state update as the connector change — one unbranched code path, so a stale action from a previously selected connector can never survive a switch.
**Verified:** real `npm run type-check`/`npm run build`, clean. **Live-verified against the real running stack** (no interactive browser click-through — same standing limitation this environment has carried through every `eami-ui` brief, disclosed, and this machine's documented Chrome/Edge automation incident risk makes that the correct call again, not a shortcut): the real `b059-live-verify` workflow's two steps were switched to `b046-live-verify` (real `action_paths`: `echo_get`/`echo_post`, confirmed via `GET /v1/gateway/tools`) and `claude` (`ai_provider`, no `action_paths`) via real `PATCH`/`GET` calls exercising the identical save-and-reload data contract `handleSubmit`→`update.mutateAsync`→`useWorkflow` goes through — a dropdown-selected value (`echo_get`) and a free-text value (`messages`) persisted and reloaded byte-identical, proving the backend can't and doesn't distinguish which control produced the value. The connector-switch reset was verified by direct inspection of the committed, single unconditional `onChange` handler rather than a browser trace, disclosed as such.
**Known limitations:** same 0%-frontend-test-coverage baseline as the rest of `eami-ui` (`BACKLOG.md` B-006); no live browser click-through, per the standing limitation above.

**Fix — B-062 (2026-08-12), Vite dev server was serving stale code after edits (bind-mount watch failure), reported live against B-060's dropdown:** user reported the `Workflows` step editor's action field for `b046-live-verify` (a connector with real `action_paths`) was still rendering as plain text, not B-060's dropdown, and asked to rule out a stale Docker image first (same class as B-035/B-055) before assuming a code bug. **Investigation:** the running `eami-ui` image genuinely *was* stale (built ~6 hours before B-060's commit) — but `docker-compose.yml` bind-mounts `./eami-ui/src:/app/src` for hot reload, and `md5sum` confirmed the container's `WorkflowsPage.tsx` was already byte-identical to the committed B-060 code *before* any rebuild — so image staleness could not actually explain this symptom. Fetching the Vite dev server's actually-served module directly (`curl http://127.0.0.1:5173/src/pages/gateway/WorkflowsPage.tsx`) proved the real cause: the served module was pre-B-060 code (no dropdown logic at all), despite the on-disk file being current. **Root cause:** Vite's default `chokidar` FS-event watcher doesn't reliably see writes made from the Windows host into this container's bind-mounted `src/` (Docker Desktop's host↔Linux filesystem bridge doesn't propagate inotify events across that boundary on Windows) — so the dev server's module cache never invalidated after B-060 was written, and kept serving the old build indefinitely until the process itself restarted.
**Fix:** `eami-ui/vite.config.ts` — `server.watch.usePolling: true` (300ms interval), so Vite periodically re-stats watched files instead of relying on FS events. Required an image rebuild (`vite.config.ts` isn't inside the bind-mounted path, unlike `.tsx` source edits).
**Verified live:** restarting the container alone (before the fix) immediately started serving B-060's real code, confirming the diagnosis. After rebuilding with the fix, made a real trivial edit to `WorkflowsPage.tsx` and confirmed via direct curl to the dev server that it was picked up and served within ~2s **with no container restart** — proving the fix closes the recurrence risk, not just this one instance. Edit reverted afterward, confirmed zero residual diff. `npm run type-check` clean.
**Known limitation:** polling adds a small constant CPU/disk-stat cost, acceptable for a local dev-only server (`target: development`; production builds don't run a dev server or watch files at all).

**Interfaces — B-064 (2026-08-13), `WorkflowsPage.tsx` gains a real extraction-expression UI:** B-063 built the backend mechanism for a step's parameter to be extracted from a prior step's real recorded result, but shipped with zero UI. Two findings, re-verified directly against current code before building, changed what "minimal but real" meant: (1) no static-value step UI existed at all (`grep` confirmed zero "params" references anywhere in this file) — B-059's `PUT/GET .../workflow-steps/{stepId}/params` had only ever been driven by direct API calls, so this brief's real scope also had to include the first-ever static-param editor, a precondition for "static vs. extracted" to be a meaningful per-param choice; (2) no path exists today for fetching a prior step's last real run result (`workflow_run_steps`/`workflow_runs` are gateway-owned tables with zero references anywhere in `eami-api`, and `eami-ui` has no network path to `eami-gateway` at all — `vite.config.ts`'s only proxy target is `eami-api`, `docker-compose.yml`'s `VITE_GATEWAY_URL` is dead config, never referenced in any frontend source file). **Decision, confirmed with the user before building:** always use a plain-text/dropdown fallback, never attempt a live sample/preview — doesn't cross this brief's file scope (the safe choice, not an expansion); logged as a natural future brief, likely worth pairing with a runs-history view.
**`useWorkflows.ts`:** `WorkflowStep` gained `id`/`input_mapping`; Create/Update step-input shapes gained optional `id`/`input_mapping` (purely additive, matches B-063's own additive backend contract). New `useWorkflowStepParams`/`useSetWorkflowStepParams`, B-059's endpoint's first-ever UI consumer.
**`StepRow` gained `localId`** (client-generated `crypto.randomUUID()`, or the step's own real id once loaded) — the frontend-local analog of the exact "reference by stable id, not array position" problem B-063 already solved server-side, needed because a mid-edit-session reorder must not misdirect a reference either. `ParamRow` unifies static and extracted params into one per-step list with a mode toggle; an extract row's source `<select>` is built from `rows.slice(0, i).filter(r => r.id)` (label `"Step N: <connector> / <action>"`, never a raw UUID) — **eligibility requires both an earlier array position AND a real, already-persisted backend id**, since a step just added in the current session has no id yet for a later step to reference (real, disclosed constraint — AC1's actual save-then-reopen flow has zero friction from it).
**AC3, reorder/removal invalidation:** every row/param mutation routes through a single `commit()` that always re-runs new `revalidateExtractionRefs` — an extract row whose source is no longer earlier-and-real-id'd gets flagged `invalid` (visible red border + message), **never silently cleared or left looking valid**, mirroring the existing `toolDeleted` step-level pattern and `validateAndConvertRows`' established "block with a clear message, never silently drop" precedent, now extended to param rows.
**Static params solved with zero "save, reopen" friction** (a design choice, not in the plan's first draft): `saveStaticParams` fires B-059's PUT for every step with static params using the ids the SAME save's response just returned — so a brand-new step's own static params can be set in the exact submit that creates it. Only the extraction-*source* eligibility constraint above remains (can't extract from a sibling created in the identical submit, since `input_mapping` must be embedded in the request before the response is known) — minor, disclosed, blocks no stated AC.
**`EditWorkflowPanel`'s row-init:** extract rows seed synchronously from the loaded workflow's `input_mapping` (already embedded); static rows seed via `useQueries` (TanStack Query's multi-query primitive, one per already-real-id'd step) — `rows` only initializes once the workflow *and* every step's static params have loaded.
**Verified:** real `npm run type-check`/`npm run build`, both clean, run locally — Node/npm confirmed actually installed on this machine now, superseding `CLAUDE.md`'s prior "not installed" toolchain note. No dedicated reviewer/security subagent pass — this brief's own standing rules didn't mandate one (frontend-only, zero backend/SQL/extraction-logic surface touched, matching B-060's identical no-review precedent for a comparably-scoped brief) — diff self-reviewed directly before commit.
**Live-verified end-to-end against the real running stack, rebuilt fresh, served-module content confirmed via direct `curl` before proceeding (the B-062 lesson — never assume a rebuild landed):** browser automation remains unavailable in this environment (same standing limitation every prior `eami-ui` brief has disclosed) — "configured entirely through the UI" was verified by sending the exact request sequence and body shapes the UI's own code generates for a real simulated admin flow, not literal clicks: add step 1 with no params → reopen → add a static param on step 1 + a new step 2 extracting from step 1 → submit → reload. **AC1**: full round trip confirmed, step 1's static param and step 2's `input_mapping` (referencing step 1's real, preserved id) both reloaded byte-identical. **AC4**: the resulting workflow, run for real via a real agent JWT, genuinely dispatched step 2 carrying the value extracted from step 1's real recorded response — traced through the real `workflow_run_steps.result` column, the same evidence B-063 used. **AC2/AC3** verified by direct code trace, disclosed as such (matches B-060's own identical precedent for its connector-switch-reset claim) — AC3 in particular is a pure client-side state transition with no network round-trip to observe live at all. Test workflow deleted afterward; shared dev DB confirmed unaffected.
**Known limitations:** no live sample/preview of a prior step's real result (finding 2 above — needs a new backend read endpoint, a natural separate future brief); same 0%-frontend-test-coverage baseline as the rest of `eami-ui` (`BACKLOG.md` B-006); no live browser click-through, per the standing limitation above.

**Interfaces — B-065 (2026-08-13), `WorkflowsPage.tsx`'s step editor restyled into Freshservice-pattern cards:** presentation only — `useWorkflows.ts` confirmed zero diff (`git diff --stat`), no backend/logic touched. Confirmed before building: no graph-canvas library needed, a vertical stack of step cards with a connecting chevron reads as a clear sequence without node/port machinery, matching the standing v1-linear-only scope.
**The one real structural change:** a step's connector/action pickers and its full B-064 parameter editor, previously inline in one form row (B-064's own accordion), moved into a new, second, layered slide-out panel (`StepConfigPanel` — higher z-index + its own backdrop over the workflow panel), opened via a "Configure" button on each step's summary card. Every underlying handler (`moveRow`, `removeRow`, `validateAndConvertRows`, `revalidateExtractionRefs`, `updateParam`/`addParam`/`removeParam`, `saveStaticParams`, `commit`) is reused completely unchanged; `StepConfigPanel` holds zero state/validation logic of its own, purely a view over the same shared `rows` state.
**New `summarizeParams`** drives each card's always-accurate one-line summary (AC2), computed from already-loaded local state: "No parameters configured", a specific "Extracting `x` from Step N" sentence for a single extraction, or a count otherwise. Existing `toolDeleted`/invalid-extraction warnings surface directly on the card (red border + message), not hidden behind Configure.
**Status prominence + Activate action (AC4), a genuine small new capability, not just a restyle:** `EditWorkflowPanel` gained a prominent status badge plus a real "Activate" button when `draft`, firing an immediate standalone `PATCH {status: 'active'}` that touches nothing else — flipping status no longer requires a full steps save. The existing status dropdown is untouched and still works.
**Verified:** real `npm run type-check`/`npm run build`, both clean. No dedicated reviewer/security pass, matching B-064's own precedent (diff confined to `WorkflowsPage.tsx`, no backend/SQL/extraction-logic surface touched).
**Live-verified end-to-end against the real running stack, rebuilt fresh, served-module content confirmed via direct `curl` before proceeding:** browser automation still unavailable in this environment (same standing limitation) — verification reused B-064's exact request-sequence-equivalence method: create with step 1 (draft) → reopen → configure step 1's static param + add step 2 extracting from step 1 (the new panels' equivalent request shapes) → save → click Activate → reload. **Confirmed live:** step 1's id/static param and step 2's `input_mapping` all persisted correctly on reload; the standalone Activate PATCH flipped `status` to `active` while leaving both steps byte-identical (proving the status-only design); the resulting workflow, run for real via a real agent JWT, genuinely dispatched step 2 carrying the value extracted from step 1's real recorded response, traced through the real `workflow_run_steps.result` column, the same evidence B-063/B-064 used. **Disclosed, not overclaimed:** this proves the data/API side is unaffected by the redesign but cannot itself observe the new card layout rendering — the dev-server-served-module `curl` check (confirming `StepConfigPanel`/`summarizeParams` are the code actually served) is the closest available proof short of a rendered-pixel check. Test workflow deleted afterward; shared dev DB confirmed unaffected.
**Known limitations:** same as B-064 (no live sample/preview, 0%-frontend-test-coverage baseline, no live browser click-through); drag-to-reorder not built (up/down arrows preserved, restyled — explicitly a nice-to-have, not required, per the brief).

**Interfaces — B-066 (2026-08-13), Workflow Canvas Brief 1: `@xyflow/react` integration + read-only rendering, a new epic distinct from Thread B:** proves the library integrates cleanly and renders a real workflow's steps as ordered canvas nodes, before any editing/interactivity. B-065's card UI remains the only editor. Corrected a stated premise before building on it: this project's Tailwind *does* have a real JIT compiler (`postcss.config.js`/`tailwind.config.ts` confirmed) — doesn't change the outcome (React Flow ships its own precompiled CSS regardless) but worth verifying rather than assuming.
**`WorkflowsPage.tsx`, purely additive (19 lines, confirmed via `git diff --stat`):** `ParamRow`/`StepRow`/`summarizeParams` gained `export` — zero logic touched, every existing component byte-for-byte untouched. One new `Waypoints` icon link per table row to a new route.
**New `WorkflowCanvasPage.tsx`:** reuses `EditWorkflowPanel`'s exact data-fetching pattern (`useWorkflow` + per-step static-params `useQueries`) and the *imported* `summarizeParams` (not reimplemented) to drive a new `WorkflowStepNode` custom node styled identically to B-065's cards. Deterministic vertical stack from `step_order`, no position persisted. Explicitly, structurally read-only (`nodesDraggable`/`nodesConnectable`/`elementsSelectable` all `false`, every `Handle` `isConnectable={false}`) — no code path can mutate anything.
**Real, measured bundle-weight data:** `npm run build` before/after — JS 933.80 kB → 1,110.92 kB minified (256.72 kB → 313.67 kB gzipped, **+~57 kB gzipped**), CSS +~16 kB.
**Verified:** real `npm run type-check`/`npm run build`, both clean first try — re-confirms the investigation's library choice against actual integration. No dedicated reviewer/security pass (presentation-only, no backend/SQL surface).
**Live-verified, honestly split between checkable and not, per this brief's own kickoff-line instruction:** rebuilt/redeployed (a real image rebuild was required — new `package.json` dependency, `node_modules` isn't bind-mounted); served-module `curl` confirmed the new code is real and wired; real API calls confirmed correct data for an existing 2-step workflow (`b063-live-verify`, including its real `input_mapping`) and a temporary real 5-step workflow made specifically for AC4's "many steps" case (deleted afterward); a real spot-check `PATCH` confirmed zero regression to B-065's edit flow. **AC4's "zero steps" case is defensively coded (`EmptyState`) but not exercisable live** — `validateWorkflowSteps` has required ≥1 step since B-058, so a truly empty workflow isn't reachable through normal API use; disclosed, not glossed over. **AC1/AC2 are genuinely visual claims ("renders correctly," "no visual/CSS conflict... side by side") this environment's standing no-browser-automation limitation cannot verify directly** — data and served-code correctness are both confirmed live, the actual rendered pixels were not seen by this session; stated plainly rather than overclaimed, the user is invited to look themselves.
**Known limitations:** no editing/interactivity (Brief 2+); no connection-validation logic yet (nothing editable, nothing to validate); layout not persisted; cards remain the primary/only editor.

**Interfaces — B-067 (2026-08-13), Workflow Canvas Brief 2: interactivity + draw-time connection validation:** click a node to configure it (`StepConfigPanel`, B-065, reused verbatim, zero changes to its own code), add/remove nodes and draw connections locally, real draw-time validation (one-in/one-out + cycle rejection) per the investigation's A.2 finding. Explicitly not the save-time full-graph validation layer (Brief 3) — the investigation was explicit draw-time alone is not sufficient, and this brief never lets a structurally-broken graph reach the backend by simply never persisting structure at all yet.
**A critical finding, re-verified against actual current code before building:** the brief's own assumption that clicking a node "already has real, working persistence" turned out false on inspection — `StepConfigPanel`'s param mutators only ever updated local state in every existing caller; the real network write only ever fired from the *enclosing panel's* main Save submit, which the canvas page doesn't have. **Consequent design fork, resolved and disclosed:** static params have their own independent endpoint (safe to save immediately) — `input_mapping` (extraction) does not, it can only ever be written as part of a full workflow PATCH that also carries structure, which this brief is forbidden from calling. Resolution: static edits persist for real on config-panel close; extraction edits stay local-only, surfaced via the same unsaved-changes banner as structural edits.
**`WorkflowsPage.tsx`:** two more additive `export`s (`StepConfigPanel`, `revalidateExtractionRefs`) — 2 lines changed total, confirmed via `git diff --stat`.
**`WorkflowCanvasPage.tsx` becomes stateful:** `closeConfigPanel` compares a step's static-params snapshot before/after the panel was open, firing a real PUT only if it actually changed. `addNode`/`removeNode` mutate only local state (grep-confirmed zero `apiFetch`/`mutateAsync` calls). `isValidConnection` implements React Flow's own documented pattern exactly (self-loop/one-out/one-in/`getOutgoers`-based cycle rejection), each with a specific toast, not a silent no-op.
**Verified:** real `npm run type-check`/`npm run build`, both clean. No dedicated reviewer/security pass (presentation-only, no backend/SQL surface).
**Live-verified, per this brief's own stated method:** served-module `curl` confirmed the real validation/persistence-split code is deployed. **AC1**: a real static-param PUT against the real `b063-live-verify` workflow persisted (confirmed via `GET`) with the workflow's own `updated_at` completely unaffected (proving the write never touches `workflows`/`workflow_steps`) — reverted afterward. **AC2/AC3**: verified by direct code inspection of the four validation checks plus confirming that's the exact code served — no test suite exists to run automatically, and a literal draw-and-reject browser interaction remains unavailable in this environment. **AC4**: verified by direct code inspection — the three structural-mutation functions grep-confirmed to contain zero backend-call sites, a precise structural proof, not a substitute for watching live network traffic. **AC5**: `git diff --stat` confirms exactly 2 lines changed in `WorkflowsPage.tsx`.
**Known limitations:** no structural persistence yet (Brief 3); no save-time full-graph validation yet (Brief 3 — this brief's draw-time layer alone is not sufficient, by the investigation's own explicit finding); cards remain the only way to persist structure or extraction config.

**Interfaces — B-068 (2026-08-13), Workflow Canvas Brief 3: save-time validation, real structural persistence — closes the epic's own two-layer requirement:** draw-time (B-067) alone can't catch a deletion leaving a broken graph; this brief adds the save-time full-graph validation backstop, then wires the canvas to the same `UpdateWorkflow` PATCH `EditWorkflowPanel`'s cards already use, unmodified. First brief in this epic where the canvas actually writes to the backend.
**`WorkflowsPage.tsx`:** two more additive `export`s (`validateAndConvertRows`, `saveStaticParams`) — confirmed via `git diff --stat`, zero logic touched.
**New `validateGraph(rows, edges)`:** re-derives graph validity fresh from current state, trusting nothing from draw time — requires exactly one start (zero incoming), one end (zero outgoing), no node with more than one edge in or out, and a walk from the start visiting every node exactly once (the only shape the backend's flat, ordered `steps` array can represent). Every rejection names the specific offending step(s) by number, per CONTEXT.md's Workflow Canvas ease-of-use principle — never a generic "invalid graph."
**`handleSave`:** `validateGraph` → reorder `rows` to the derived order → `revalidateExtractionRefs` on the *reordered* array (order changes what counts as "earlier") → the imported `validateAndConvertRows` → any error surfaces as a persistent `saveError` banner (distinct from the transient toast, never auto-clears), save aborted before any network call → on success, the real `mutateAsync` PATCH, then `saveStaticParams`, then local state rebuilt from the confirmed persisted response. The extraction-persistence gap B-067 left open closes itself here with zero new logic: once the graph is correctly ordered, the existing `validateAndConvertRows` builds `input_mapping` from the reordered rows exactly as it always has.
**A real defensive gap found and closed during the mandatory reviewer pass:** the original reachability check (`order.length < rows.length`) only guarded under-coverage — an edge targeting an id not present in `rows` (not reachable via any current UI path, but not structurally prevented by the function's own types either) could let a corrupted `order` slip through as `error: null`, crashing `handleSave` outside its `try/catch`. Hardened: the walk now only ever advances onto a real row id, and the length check is `!==` (impossible to overshoot, not just unlikely to).
**Verified:** real `npm run type-check`/`npm run build`, both clean.
**Reviewer + security subagent pass, mandatory per this brief's own standing rules (unlike B-064–067):** `validateGraph` independently re-traced against every malformed/valid topology in scope, correct except the phantom-edge gap above (now fixed); confirmed no path reaches `mutateAsync` without a validated order first; confirmed `eami-api/internal/api/workflows.go` genuinely unmodified. Traced the real trust boundary in code: the backend deliberately doesn't validate `input_mapping.from_step` ordering/existence at write time, but this is safe by construction — `eami-gateway`'s `resolveParams` is scoped per-run to only that run's already-completed steps, so a forward or cross-org reference simply isn't found and fails the run cleanly rather than resolving to anything. Cross-org step-id adoption independently confirmed impossible via `resolveStepIDs`'s org-scoped existing-steps map. No vulnerability found.
**Live-verified end-to-end, rebuilt fresh, served-module `curl` confirmed, at B-063's original full rigor standard (not the lighter substitute used since B-064):** created a real throwaway 2-step workflow, sent the exact request sequence `handleSave` generates for a real canvas save. **AC1/AC2**: confirmed byte-identical on reload; `validateGraph` extracted verbatim from the served module and *executed* (not just read) against six concrete topologies (valid chain, valid chain in reverse array order, the brief's own "deleted connecting edge" example, an over-connected node, a pure cycle, two competing starts) — every rejection named the correct step(s), every acceptance produced the correct order. Backend-side defense in depth confirmed directly: a structurally incomplete step submitted straight to the PATCH endpoint got a real `400`, stored workflow unaffected. **AC3, full B-063-standard proof**: real extraction configured, saved, `input_mapping` confirmed persisted via `GET`, workflow run for real via a real agent JWT, extracted value traced through the real `workflow_run_steps.result` column in Postgres — step 1's real dispatched value reappeared as step 2's real dispatched param, genuinely extracted at run time. **AC4**: `EditWorkflowPanel`'s and `WorkflowCanvasPage`'s row-init blocks confirmed byte-identical in construction from the same `GetWorkflow` response. **AC5**: `git diff --stat` confirms `WorkflowsPage.tsx` stays to the two new `export`s. Test workflow, its runs, and scratch token files all deleted afterward; shared dev DB confirmed unaffected.
**Known limitations:** cards remain the primary editor (full cutover not yet scoped); no free node dragging/position persistence; still linear-chain-only, matching Thread B's own current backend scope (no branching).

**Epic discontinued — B-069 (2026-08-13), Workflow Canvas removed, code and this entry both kept as history:** real hands-on browser testing (the first time any human, rather than this environment's code-inspection/structural-proof substitute, actually clicked through the canvas) found multiple genuinely broken interactions the B-066/067/068 entries above did not catch — edge/node keyboard-deletion (`onDelete`/`onNodesDelete`/`onEdgesDelete` were never wired to `<ReactFlow>` at all, and `deleteKeyCode` defaulted to `'Backspace'`-only, missing Windows' separate `Delete` key) and step-configuration dropdowns not responding when opened from the canvas (root cause never found despite exhaustive tracing against the actual installed `@xyflow/react`/`@xyflow/system` source, byte-identical prop-wiring confirmed against the proven-working card editor). `WorkflowCanvasPage.tsx` deleted, its route removed, `@xyflow/react` uninstalled for real (bundle size confirmed to return to the exact pre-B-066 baseline: 933.80 kB / 256.72 kB gzipped), the 7 additive `export`s B-066/067/068 added to `WorkflowsPage.tsx` reverted to file-private (none of the underlying functions/types deleted — all still power the card editor). B-065's card editor was never broken, never depended on the canvas, and is the sole editor again — re-verified end-to-end (create, static param, real extraction, save, real run, `workflow_run_steps.result` trace) with zero regression. Full reasoning also recorded in `BACKLOG.md`'s Workflow Canvas epic header. The "Workflow Canvas ease-of-use principle" in `CONTEXT.md` is left in place for any future decision to revisit a visual canvas.

---

## appliance
- **B-053 (2026-08-09): VM appliance base image + first-boot detection.** New `appliance/` directory — the first appliance-packaging deliverable (Model A per ADR-020: a fully self-contained, single-VM package of `docker-compose.prod.yml` exactly as it already exists, no architectural changes).
  - **`appliance/packer/eami-appliance.pkr.hcl`:** Packer `qemu` builder, Debian 12 (bookworm) official generic cloud qcow2 as the base image (chosen over ISO+preseed — far more reliable for unattended builds, matches `scripts/setup.sh`'s own tested-platform list). A NoCloud cloud-init seed (`cd_content`, rendered inline via `templatefile()` with a Packer-managed ephemeral `sshkey` datasource) gives Packer build-time-only SSH access. Files are staged to `/tmp` first (the unprivileged `packer` SSH user can't `scp` directly into a root-owned `/opt` path), then `sudo mv`'d into place, `chown -R root:root`'d, before `provision-base.sh` runs.
  - **`appliance/scripts/provision-base.sh`** (runs once, during the build): installs Docker Engine + compose plugin (official apt repo) and `parted`; installs the two systemd units + their scripts; locks every local account's password (`passwd -l`, all accounts including root); disables `ssh.service` and cloud-init for all future boots; strips the `packer` build account's `authorized_keys`. **Deliberately does NOT fully delete the `packer` account** — an earlier version did (`userdel -f -r`), which a mandatory review pass caught as breaking Packer's own `shutdown_command` (needs a working `sudo -n` for that exact account) — found live: with `shutdown_command` unset, Packer's QEMU builder force-kills the VM the instant the provisioner returns rather than waiting for a graceful shutdown, risking a corrupted/inconsistent captured disk (unflushed writes, un-journaled ext4 state) on every single build. Reverted to the safer trade-off: keep the account structurally present but authenticate-proof (password locked, key stripped, `ssh.service` disabled — no valid credential reaches it), and keep `shutdown_command = "sudo -n shutdown -P now"` as a real, Packer-orchestrated clean shutdown.
  - **`appliance/scripts/eami-data-disk.sh`** (runs at every boot, `Before=docker.service containerd.service`): idempotently partitions/formats/mounts a second virtio disk (`/dev/vdb`, contract documented in `appliance/README.md`) as `/data` — GPT, single ext4 partition, labeled `EAMIDATA`. Refuses to touch a disk carrying ANY unrecognized filesystem/partition signature (`wipefs -n` dry-run probe first) rather than risk silently wiping real data. Points **both** Docker's `data-root` (`/etc/docker/daemon.json`) **and** containerd's separate persistent `root` (`/etc/containerd/config.toml`) at `/data` — this is what actually keeps the 5 named volumes (and every Docker/containerd image layer) off the OS disk, entirely without modifying `docker-compose.prod.yml` itself (Docker/containerd create their storage under whatever root is configured, transparently).
  - **`appliance/scripts/eami-stack.sh`** (runs at every boot, `After=docker.service`): generates `/data/eami/.env` once, only if it doesn't already exist (idempotent — critical for AC6, an OS-image update must never regenerate secrets over an already-initialized database) — ported from `scripts/setup.sh`'s non-interactive secret-generator functions (`openssl rand -hex 32`), covering all 7 secrets `docker-compose.prod.yml` references (including the 3 `write_env()` itself is missing — see B-054, logged not fixed here, since `setup.sh` is read-only scope for this brief). **Deliberately never runs `setup.sh`'s interactive org/admin seeding** — this is what makes AC3 (boots with zero orgs, no crash) true by construction, not by accident. Runs `docker compose -f /opt/eami/docker-compose.prod.yml --env-file /data/eami/.env up -d`, then writes `/data/eami/state=unconfigured` once on first successful bring-up.
  - **Secrets and the first-boot marker live on the data disk (`/data/eami/`), not the OS disk (`/opt/eami/`)** — a deliberate design point, not incidental: `/opt/eami/` is the static application bundle (compose file, migrations, backup scripts), replaced wholesale by every OS image update. If secrets lived there too, an OS update would silently regenerate `POSTGRES_PASSWORD`/`TOOL_CREDENTIALS_ENCRYPTION_KEY` on next boot — the Postgres data files would technically survive (satisfying AC6's literal wording) but become unusable, since a fresh password won't match the already-initialized database's own auth. Keeping secrets on `/data` makes "survives an update" mean "still actually works."
  - **First-boot detection contract (for the future setup-wizard brief):** `/data/eami/state` contains `unconfigured` (written once by this brief) or `configured` (never written by anything in this brief — the wizard's job, once built, is to write it after creating the first org/admin). No API endpoint exposes this yet, deliberately: `application code` is out of this brief's `MUST NOT MODIFY` scope; whatever serves the wizard reads the file directly from the host filesystem.
  - **Real bugs found and fixed via actual QEMU/KVM boot testing** (this machine has genuine hardware-accelerated KVM inside WSL2 — confirmed via `kvm-ok` reporting "KVM acceleration can be used" and a live SeaBIOS boot sequence before relying on it, not assumed): (1) a systemd unit-ordering race — `DefaultDependencies=no` made `eami-data-disk.service` start concurrently with `systemd-udevd` itself, missing `/dev/vdb`'s just-created device node; fixed with proper `After=systemd-udevd.service local-fs.target` plus `ExecStartPre=/usr/bin/udevadm settle`. (2) `parted` isn't installed on the base Debian cloud image by default; added to the apt install list. (3) containerd has its own separate persistent root (`/var/lib/containerd` by default) — docker-ce on Debian talks to the system `containerd.service` over a socket, it does not embed its own; leaving containerd's root on the OS disk while Docker's was correctly redirected meant an OS-image update (fresh containerd state) paired with Docker's persisted `/data/docker` container metadata (referencing containerd snapshots that no longer existed) produced `"RWLayer of container ... is unexpectedly nil"` — Docker's own volumes/images survived the update, but already-created containers couldn't restart; fixed by redirecting containerd's `root` to `/data/containerd` too. (4) `mount -a` isn't itself idempotent against systemd's own fstab-generated `data.mount` unit racing to mount `/data` automatically on any boot after the first (once the fstab entry is permanent) — fixed with a `mountpoint -q` guard, further refined to a bounded poll loop only on the reuse path (a fresh format skips the poll entirely, since nothing else races to mount a filesystem that didn't exist moments ago).
  - **Two mandatory review passes both ran and found real, distinct issues, applied before shipping — not rubber-stamped:** first pass (general-purpose subagent) found the build-time SSH backdoor wasn't actually closed (password-locking blocks password auth only, does nothing to `authorized_keys`) — fixed by stripping the key. A follow-up pass on that same fix found it had gone further than needed and broken something else: full account deletion (the fix's first version) silently broke Packer's `shutdown_command`, with the corruption risk described above — reverted to the safer strip-credentials-don't-delete-account trade-off. That same follow-up pass also flagged `blkid --label` searching every block device system-wide rather than the specific `/dev/vdb1` partition (a stray `EAMIDATA` label on an unrelated disk could silently skip formatting the real data disk and mount the wrong device) — fixed with `blkid -o value -s LABEL "${DATA_DISK}1"`; no `Restart=` on the two boot-critical oneshot units (a transient failure would permanently strand a console-less appliance) — considered, added, then reverted again after it didn't resolve an unrelated boot-timing issue being investigated at the time and added unproven complexity; and file ownership after Packer's `mv` staging (fixed with an explicit `chown -R root:root`). A security review (manual, run inline after the automated skill's diff-auto-detection didn't apply to all-new untracked files) found no additional issues: ephemeral build-time-only credentials (the private half never written to the image), no secrets logged, `openssl rand -hex 32` for every generated secret, `.env` written with `umask 077` before content (no world-readable window).
  - **Emergency access:** SSH fully disabled by default, permanently — no password or key-based login of any kind on a normally booted appliance. Documented recovery path is hypervisor/physical console access via GRUB (deliberately left unprotected — no GRUB password) — the standard break-glass model for hardened appliances (matches Device42 and similar): console/physical access is inherently privileged, so gating it behind a separate credential adds friction without adding real security. A console-triggered temporary-SSH-enable convenience script was considered and explicitly declined (founder decision, 2026-08-08 — smallest attack surface over convenience, kept this brief's scope contained).
  - **First-boot auth-gap threat modeling** (required by this brief's own standing rules): traced whether an attacker could race a legitimate operator to claim the first org/admin over the network, given the appliance boots with immediately-network-reachable services and an empty `orgs` table. **Not currently exploitable** — no self-service signup/register HTTP route exists anywhere in `eami-api` today (every admin account so far has been created via direct SQL, confirmed via repo-wide investigation earlier this session, never a network-reachable path), so there is no mechanism to create an org over the network at all yet, hence no race to have. Flagged explicitly in `appliance/README.md` as something the **next brief** (the setup wizard) must design against from the start, not solved here since the mechanism that would need race-safety doesn't exist yet.
  - **Verified via real QEMU/KVM boot testing, across multiple clean rebuild-and-reboot cycles:** AC1 (stack comes up automatically — `eami-api`'s `/health`, `eami-gateway`'s `/healthz`, `eami-ui`'s `/` all returned healthy responses), AC2 (indirect but strong: `eami-api`/`eami-gateway` only start after `migrate`'s `condition: service_completed_successfully`, per `docker-compose.prod.yml`'s existing, unmodified gating from B-051 — their healthy responses are only possible if migration succeeded), AC3 (starts healthy with zero orgs, by construction — no seeding code path exists to race), AC4 (`/data/eami/state` confirmed exactly `unconfigured` via direct host-side disk inspection — `debugfs`, not a login shell, since SSH is disabled by design), AC5 (SSH disabled confirmed two ways: `ssh.service` absent from `/etc/systemd/system/multi-user.target.wants/`, and `/etc/shadow` showing both `root` and the build account's password hash prefixed `!`/`!*` — locked) were all directly, repeatedly confirmed. **AC6's core requirement — rebuild the OS disk, reboot with the existing data disk, the stack comes back up healthy — was verified successfully**, including a clean health-check pass (`{"status":"ok"}`/`"ok"`) following a real OS rebuild+reboot with the corrected (post-review-fix) build.
  - **One thing explicitly not claimed as verified, disclosed rather than glossed over:** a stricter secondary check — byte-for-byte comparing `/data/eami/.env`'s generated secrets before vs. after an OS rebuild via host-side `debugfs` disk inspection — could not be completed reliably in this environment. Repeated attempts hit nested-virtualization instability (QEMU-inside-WSL2, on an already heavily-loaded host after an extended session), compounded by several confirmed self-inflicted tooling bugs discovered along the way: `pgrep -f` matching its own wait-loop's command line (which literally contained the search pattern as a string argument) causing a 20+-minute false-stuck wait; `pgrep -x` silently never matching a process name longer than 15 characters (`qemu-system-x86_64`); impatient `kill -9`s racing host-side `qemu-img convert`/`dd` disk extractions and corrupting the resulting read-only copies. Per the user's explicit direction, this specific extra check was stopped rather than pursued further — the core AC6 requirement above is verified; this additional byte-identical comparison is not.
  - **Known limitations, disclosed not hidden:** `.ova` conversion not attempted (flagged as fast-follow, per the brief's own stated allowance). Update-mechanism polish (a CLI/UI to drive "replace disk 1" instead of doing it by hand at the hypervisor level) is separate, later work. Flatcar explicitly deferred (founder decision, ADR-020's own context) — Debian minimal only, this round. `GATEWAY_UI_BASE_URL` in the generated `.env` defaults to `http://<primary-IP-at-first-boot>` (best-effort `hostname -I`) — used only for constructing links in Slack approval notifications, not required for the stack to start. `eami-data-disk.sh`'s `daemon.json`/`containerd config.toml` writes are full overwrites of just the one key each needs, not a merge into package defaults — deliberate, given this appliance's narrow single-purpose config surface, not an oversight. Full writeup in `appliance/README.md`; `BACKLOG.md`'s B-053 entry has the condensed version.

- **B-055 (2026-08-10): `eami-stack.sh` gains console setup-token generation, closing B-053's own flagged gap.** Full design/verification writeup is in the `eami-api` section above (`internal/api/bootstrap.go`'s handlers are the bulk of this work) — this entry covers the appliance-side half only. After `docker compose ... up -d` (by which point `migrate` has completed, per its existing `depends_on: condition: service_completed_successfully` gating), `eami-stack.sh` now queries `SELECT count(*) FROM orgs` directly via `docker compose exec postgres psql` (same pattern `scripts/setup.sh`'s `seed_database` already uses). If zero: deletes any prior unconsumed token (only one live token at a time), generates a fresh `openssl rand -hex 32` token, stores its SHA-256 hash in the new `setup_tokens` table with a 30-minute expiry, and prints the raw token to stdout — which `eami-stack.service`'s existing `StandardOutput=journal+console` (set in B-053, for exactly this reason) already routes to the VM console. If nonzero (already configured): no token is generated, and `/data/eami/state` is best-effort-flipped to `configured` if it still says otherwise.
  **`/data/eami/state`'s role changed from B-053's original design, and that change is documented, not silent:** B-053's README assumed the wizard's backend would read this file directly. It can't — `docker-compose.prod.yml` stays frozen for this brief too, and nothing bind-mounts `/data/eami` into any container. The file is now informational-only; the real gate `eami-api` checks on every request is the `orgs` table itself. `appliance/README.md`'s "First-boot detection contract" section is rewritten to state this plainly, and its "no self-service signup path" known-limitation entry is marked closed with a pointer to the new design.
  No systemd unit or `eami-data-disk.service` changes were needed — this is entirely inside `eami-stack.sh`'s existing script, which already ran at every boot.

---

## Cross-cutting / shared
- **Postgres backup & disaster recovery (B-029, 2026-07-25):** no backup mechanism existed at all before this — `docker compose down -v` or disk loss was unrecoverable, a real gap for a product whose core value is an immutable audit trail. `scripts/reseed.sql` (a demo-data convenience script) was headed "Re-seed org and admin user after `docker compose down -v`," easy to mistake for real recovery — it only recreates one org/user, not any prior data.
  - **Mechanism:** new `eami-backup` service in both `docker-compose.yml`/`.prod.yml`, image `postgres:16-alpine` (has both `pg_dump` for PG16 and Alpine's busybox `crond`). Investigated `pg_cron` first (already running in the `postgres` container, used for `audit_log` partitioning — `schema/migrations/007`) but rejected it: `pg_cron` jobs execute as SQL inside the Postgres backend and can't shell out to run an external client program like `pg_dump` to write a file — that needs a real separate client process. New `scripts/backup-db.sh` (POSIX `sh` — the container has no bash) runs `pg_dump -Fc` to a `.tmp` file, atomically `mv`s it into place only on success, logs a timestamped line either way, exits non-zero with a clear `ERROR` line on failure (never silent), and prunes down to `BACKUP_RETENTION_COUNT` (default 14) newest dumps, also sweeping any orphaned `.tmp` files from a previously killed run. New `scripts/backup-entrypoint.sh` installs a real crontab (`BACKUP_SCHEDULE`, default `0 3 * * *`) and `exec`s `crond -f -l 2`, handling two dockerized-cron gotchas that would otherwise make this silently non-functional: `crond` doesn't pass the container's env through to jobs (captured into a `chmod 600` sourceable file instead, values POSIX-single-quote-escaped so a literal `'` in a hand-typed password can't corrupt the file), and `crond` job output doesn't reach `docker logs` by default (redirected into the container's own PID 1 fds, `/proc/1/fd/1`/`2`). Validates `BACKUP_SCHEDULE` has exactly 5 fields before installing it (falls back to the default + logs an `ERROR` rather than silently never firing again on a typo), and runs one backup immediately at container start so a fresh deployment has a baseline backup within seconds rather than waiting up to a full schedule interval.
  - **Storage:** new named volume `postgres_backups`, deliberately separate from `postgres_data` so losing/recreating the Postgres data volume doesn't also destroy the backups — mounted read-only into the `postgres` service itself too, so restore commands can reference `/backups/<file>` directly with no manual file-copying between containers.
  - **Fits the deployment model:** defined in both compose files, so `docker compose up -d` (including via `scripts/setup.sh`'s `start_stack()`) brings it up automatically — no appliance-specific first-boot logic needed for the eventual VM-appliance story (investigated `setup.sh`'s structure per this task's brief; only change there is a one-line mention in `print_summary()` plus `.env.example` documenting the two optional env vars — no new required secrets).
  - **`RECOVERY.md` (new):** what's backed up vs. not (offsite/cloud storage explicitly flagged as the most important follow-up — a lost/compromised host currently loses backups too, not just live data; PITR/WAL archiving and automated restore also explicitly out of v1 scope, per the brief), how to verify backups are running, and a step-by-step restore procedure — the one that was actually tested (see below), not a theoretical one. `scripts/reseed.sql`'s header rewritten to stop implying it's a recovery tool and point at `RECOVERY.md` instead, with a comparison table.
  - **Real end-to-end test performed 2026-07-25 (isolated `docker compose -p eami-b029-test` project — zero impact to the real local dev stack, its `.env`, or its `eaim_*` volumes, all confirmed still present and untouched afterward):**
    1. Brought up a fresh isolated `postgres` + `eami-backup`. Confirmed via `docker logs` that the automatic first-boot backup ran and produced a real 148KB `.dump` file.
    2. Temporarily set `BACKUP_SCHEDULE=* * * * *`, waited past a real minute boundary, and confirmed a **second** backup fired via cron (not manually) with its output genuinely reaching `docker compose logs` — proving the `/proc/1/fd` redirection fix works live, not "should work in theory."
    3. Manually triggered two more backups to push past `BACKUP_RETENTION_COUNT=3` and confirmed pruning deleted exactly the oldest file, keeping the 3 newest.
    4. Simulated a backup failure (bad credentials) and confirmed a clear `ERROR` log line, non-zero exit code, and — critically — no partial/corrupt `.tmp`-turned-`.dump` file left behind.
    5. **Disaster-recovery drill:** inserted a real, identifiable test org row (`slug='b029-restore-test'`), took a backup capturing it, then `docker volume rm`'d `postgres_data` entirely (simulating total disk loss) — confirmed `postgres_backups` survived, being a separate volume. Brought up a completely fresh `postgres` (confirmed 0 orgs present — genuinely empty, not stale). Restored.
    6. **First restore attempt used `pg_restore --clean` and failed partway** (7 errors) — TimescaleDB hypertables don't support the `ALTER TABLE ... ONLY DROP CONSTRAINT` statements `--clean` generates against a database where `docker-entrypoint-initdb.d/schema.sql` had already run on the fresh container's own boot. A real bug this test caught, not a theoretical concern. Fixed by wiping via `DROP SCHEMA public CASCADE; CREATE SCHEMA public; GRANT ALL ON SCHEMA public TO eami_app;` before a plain `pg_restore` (no `--clean`) — re-tested, **zero errors**. Verified: all 73 tables present (including all 20 `audit_log` monthly partitions), all 6 extensions restored (`pgvector`, `TimescaleDB` 2.28.3, `timescaledb_toolkit`, `pgcrypto`, `uuid-ossp`, `plpgsql`), TimescaleDB hypertable catalog metadata intact, and the exact test org row present with the identical UUID, name, slug, and `created_at` timestamp. This tested procedure — not the naive one — is what `RECOVERY.md` documents, with the failure and its fix called out explicitly so nobody repeats it.
    7. All test containers/volumes torn down after each phase; final `docker volume ls`/`docker ps -a` confirmed zero leftover state and the pre-existing real `eaim_*` volumes untouched throughout.
  - **Reviewer pass (general-purpose subagent) caught 4 real issues before commit, all fixed and re-verified live (not just patched and assumed fixed):** (1) `pg_dump --no-owner --no-privileges` are documented no-ops for custom-format (`-Fc`) archives — only meaningful at `pg_restore` time, which already had them; removed from the dump side. (2) The generated cron env-sourcing file interpolated `DATABASE_URL` inside single quotes with no escaping — fixed with a POSIX single-quote-escape helper, re-verified live end-to-end with a `POSTGRES_PASSWORD` containing a literal `'`, confirming both the escaped file content and a still-successful backup. (3) No `BACKUP_SCHEDULE` validation before writing the crontab — added a 5-field check with a logged fallback. (4) Orphaned `.tmp` files from a hard-killed run were never cleaned up — added a sweep at the start of each run. A 5th flagged concern (a possible race between `postgres`'s `service_healthy` condition and `schema.sql` finishing, which could make the automatic first backup capture an empty schema) was investigated empirically rather than dismissed or assumed: traced the real container startup log and confirmed `timescale/timescaledb-ha:pg16` follows the standard docker-library/postgres entrypoint contract — an internal bootstrap server applies `schema.sql` and fully shuts down *before* the real, healthcheck-visible TCP server ever starts — so the race does not exist for this image; confirmed directly by querying `information_schema.tables` at the exact moment `pg_isready` first succeeded on a fresh volume.
  - **Verification:** no Go files touched (backup/restore is shell + compose + docs), so no `go build/vet/test` applies. `docker compose config` clean on both compose files throughout. All claims above are from the real live test in this session, not static review — Docker Desktop was not running at the start of this session; started it and confirmed the daemon was live before beginning, specifically so this task's "must be a REAL end-to-end test" requirement could be honored rather than deferred.
  - **Known limitation, by design (v1 scope, not a gap):** backups are local-disk-only. If the whole host is lost (not just the `postgres_data` volume), backups are lost too — offsite/cloud storage is the natural next step, explicitly flagged in `RECOVERY.md`'s "Future work" rather than built this round.
  - **New `.gitattributes` (repo-root, didn't exist before)** pins `scripts/backup-db.sh`/`scripts/backup-entrypoint.sh` to `eol=lf` specifically — these two new POSIX-`sh` scripts are bind-mounted from the host into the Alpine `eami-backup` container, and this repo's Windows checkouts default to `core.autocrlf=true` (confirmed via `git config`), which would otherwise inject CRLF into the working-tree copies on a future checkout and silently break them — the exact bug class `BACKLOG.md` B-021 already tracks repo-wide (unfixed). Scoped to just these two new files, not a fix for the other pre-existing scripts B-021 covers; confirmed applied via `git check-attr`.
- **Config hygiene cleanup (B-028/B-030/B-031, 2026-07-25):** three small, low-risk doc/script fixes discovered during the B-025/026/027 security work — none of them live vulnerabilities, all closed in one session, no application code touched.
  - **B-028:** `eami-api/eami-api.yaml` (`database.dsn`, `service_key`) and `eami-gateway/eami-gateway.yaml` (`postgres_dsn`, `api.service_key`) still shipped the literal `"changeme"` as example config — misleading now that B-025's `validate()` rejects that exact value at startup. All 4 replaced with `""` plus a short comment naming the real env-var override (`API_DB_HOST/NAME/USER/PASSWORD`, `API_SERVICE_KEY`, `GATEWAY_DB_HOST/NAME/USER/PASSWORD`, `GATEWAY_API_SERVICE_KEY`) and noting `validate()` rejects empty/placeholder — mirrors `config.go`'s own `defaults()` comments ("no default ServiceKey/DSN shipped on purpose"), not a new convention. Confirmed via `docker-compose.yml`/`.prod.yml` that neither service's Dockerfile/default invocation actually depends on the YAML's literal values: both services always get their real DSN/service-key from env vars in the compose files, which `config.go`'s `Load()` unconditionally overlays onto whatever the YAML provided — so blanking the YAML defaults cannot change any running container's behavior.
  - **B-030:** `scripts/seed-db.sh` and `scripts/create-audit-partition.sh` both had `DB_URL="${DATABASE_URL:-postgresql://eami_app:devpassword@localhost:5432/eami}"` — the same silently-guessable-default pattern B-025 already closed in `docker-compose.yml`'s `POSTGRES_PASSWORD`. Both scripts now check `[[ -z "${DATABASE_URL:-}" ]]` first and `exit 1` with an actionable message ("DATABASE_URL must be set — see .env.example. Refusing to fall back to a guessable default connection string.") before ever constructing a connection string — same fail-closed contract as B-025's Go-side `validate()`. Each script's header comment updated to match (previously claimed a default/override relationship that no longer exists). `bash -n` syntax-checked clean on both.
  - **B-031:** `API_JWT_SECRET` — generated by `scripts/setup.sh`'s `gen_jwt_secret()`, documented in `.env.example` as an "HS256 fallback," wired through both compose files as an env var — but a repo-wide grep for `JWT_SECRET`/`JwtSecret`/`JWTSecret` confirmed zero Go code anywhere reads it; `eami-api/internal/auth`'s `Service` signs/verifies exclusively via RS256. Removed entirely, per this task's stated preference for full removal over a reserved-with-comment approach — confirmed first that nothing in `setup.sh`'s own flow (`start_stack`, `seed_database`, `print_summary`) depends on the variable's value beyond writing it to `.env`. Deleted: `gen_jwt_secret()` itself, its Step-5 generation call, `write_env`'s `api_jwt_secret` parameter and the `API_JWT_SECRET=...` heredoc line (remaining positional parameters renumbered/shifted at both the function signature and its one call site), `.env.example`'s whole `API_JWT_SECRET` block, and one line each from `docker-compose.yml`/`docker-compose.prod.yml`. `bash -n` syntax-checked `setup.sh` clean after the parameter-shift; re-grepped the full repo afterward and confirmed zero remaining references outside this session's own doc/log entries (`BUILT.md`/`BACKLOG.md`/`CONTEXT.md` — history, not live config).
  - **Verification:** no Go files were touched by this task (confirmed via `git diff --stat` against the 8 changed files, all YAML/shell/compose/env), so no `go build`/`go vet`/`go test` run applies. `docker compose config` (which parses and resolves the compose YAML, including all env-var substitution, without needing a running daemon) was run against both `docker-compose.yml` and `docker-compose.prod.yml` with a full set of required env vars supplied on the command line — both resolved cleanly (exit 0), and the resolved `eami-api` service block in each was inspected directly to confirm `API_JWT_SECRET` no longer appears while every other environment value (`API_SERVICE_KEY`, `API_DB_PASSWORD`, etc.) is unchanged. The Docker daemon itself was not running in this session (confirmed via `docker info`), so a live `docker compose up` re-verification of the running stack was not possible here — `docker compose config`'s clean resolution is the evidence produced for this task's AC #4, not a substitute for a live check if one is wanted later.
- **`POSTGRES_PASSWORD` fail-closed fix (B-025, 2026-07-24):** `docker-compose.yml` had `${POSTGRES_PASSWORD:-devpassword}` in all 3 places it's consumed (`postgres`, `eami-gateway`'s `GATEWAY_DB_PASSWORD`, `eami-api`'s `API_DB_PASSWORD`) — an unset env var meant Postgres silently started with a guessable password. `docker-compose.prod.yml` had no textual fallback (bare `${POSTGRES_PASSWORD}`), so an unset var there passed an empty string through to the official Postgres image, which does refuse to start without a real password — but as a Postgres-container crash-loop, not a clean EAMI-level error. Both files' 3 occurrences (6 total) now use compose's native `${POSTGRES_PASSWORD:?POSTGRES_PASSWORD must be set — see .env.example}` syntax, which aborts `docker compose up`/`run` before any container is created if unset — confirmed fail-closed both by reading compose's documented `:?` semantics and by live-testing `docker compose config` with the var unset. `.env.example`'s `POSTGRES_PASSWORD=changeme` and `DATABASE_URL=postgresql://eami_app:changeme@...` are now blank with instructive comments (matching how `GATEWAY_EPISODE_READ_SERVICE_KEY` was already documented); `API_SERVICE_KEY`/`GATEWAY_API_SERVICE_KEY` were already blank, just gained a clarifying "both services refuse to start if this is unset or a placeholder" comment. See the `eami-api`/`eami-gateway` sections above for the app-level `validate()` half of this fix (the same known-bad values are now rejected at the Go level too, not just relying on the compose/Postgres-image layer) and for the live verification against the currently-running local stack's real secrets.
- **`docker-compose.yml` build-context fix (2026-07-22):** `eami-ui` service had `build.context: ./eami-ui` while `eami-ui/Dockerfile` copies `api/openapi.yaml` (a repo-root path, needed for `generate-client`) — `docker compose up --build` failed with `COPY api/openapi.yaml /api/openapi.yaml: not found`. Fixed to `context: .` / `dockerfile: eami-ui/Dockerfile`. `eami-api`'s service was never affected — its Dockerfile has no path outside its own `./eami-api` context, so it wasn't a valid template to copy from. Verified with `docker compose build eami-ui` — clean. `docker-compose.prod.yml` unaffected (pulls a prebuilt `ghcr.io` image, no build context).
- **Docker is confirmed available on this machine as of 2026-07-22** (`docker build`/`docker compose build` both ran successfully this session) — corrects the "no Docker in this environment" notes elsewhere in this doc and in `BACKLOG.md` B-004, which were accurate when written but are now stale for this point specifically. Full-stack `docker compose up` end-to-end run still not performed.
- **Schema:** `schema/schema.sql` + `schema/migrations/001`–`007` (sequential, no gaps). Table references in Go store code cross-checked clean except `notification_channels` (dead, see above).
- **Multi-tenancy:** enforced at the application layer — every store query takes an explicit `org_id` param (e.g. `WHERE org_id = $1`), not Postgres RLS (RLS is used only for `audit_log` append-only enforcement per ADR-007). ADR-013 (org-per-schema vs. RLS) is still listed Pending in `DECISIONS.md`. Correctness currently depends on every handler remembering to pass `org_id` — no defense-in-depth. Not verified exhaustively this session.
- **Secrets/auth posture (from `tasks/TASK-051-security-findings.md`, all HIGH findings closed):** bcrypt cost 12 (TASK-066), JWT RS256 with explicit algorithm allowlist, constant-time API-key comparison, SHA-256-hashed API keys, `crypto/rand` throughout, JWT revocation persisted + issuer-validated (TASK-062/063), audit-log DB-error propagation fixed (TASK-064), audit chain verify endpoint added (TASK-065, `GET /v1/audit/verify`).
- **Toolchain gap:** this development machine has no `go` or `node`/`npm` installed. No build or test command in this doc was actually executed — all status above is from static source review. Treat every "STABLE" tag as "reads correct" rather than "verified green." **No longer accurate as of B-047 below** — `go` (1.26.5) is installed and on a documented path per `CLAUDE.md`'s toolchain note, and Docker is confirmed working; real `go build`/`vet`/`test` and real `npm`-via-Docker runs are now routine for this repo, not the exception.

- **B-047 (2026-08-06): security dependency updates + container hardening.** Closed 5 confirmed-reachable Go CVEs, one chi deprecation with header-spoofing implications, two containers running as root, floating base-image tags on all 4 service Dockerfiles, and one Dockerfile comment that actively misrepresented its own container's security posture. Every finding was re-verified against current code before fixing (not trusted from the audit brief as-is) — one line number had drifted (`router.go:127`→`:116`, code unchanged) and two of the brief's own assumptions turned out wrong, both surfaced and resolved with the user before building rather than silently worked around.
  - **CVE fixes (all verified via `govulncheck`, 0 reachable vulnerabilities post-bump, across all 5 modules):** `eami-gateway`: `jackc/pgx/v5` v5.6.0→v5.9.2 (GO-2026-5004/CVE-2026-33815/33816, SQL injection + memory-safety), `golang-jwt/jwt/v5` v5.2.1→v5.2.2 (GO-2025-3553/CVE-2025-30204, unbounded-allocation DoS in `ParseUnverified`), `golang.org/x/text`→v0.39.0 (GO-2026-5970, infinite loop — not picked up by plain `go mod tidy`, since pgx's own `go.mod` only floors it at v0.29.0; required an explicit `go get golang.org/x/text@v0.39.0`). `eami-api`: same pgx (v5.7.2→v5.9.2)/jwt/x/text fixes, plus `go-chi/chi/v5` v5.1.0→v5.3.0 (RealIP's spoofing-class CVEs, see below). `eami-agent`: `golang.org/x/sys` v0.21.0→v0.44.0 (GO-2026-5024/CVE-2026-39824, Windows `NewNTUnicodeString` integer overflow — confirmed present but unimported by any current code path, bumped anyway per the brief's own "cheap, removes latent risk" reasoning).
  - **Cascading discovery, not in the original brief:** pgx v5.9.2 and x/text v0.39.0 both declare `go 1.25.0` in their own `go.mod` — confirmed by fetching each version's real `go.mod` from the module proxy, not assumed. This forced `go.work` and both affected modules' `go` directives from `1.24`→`1.25.0` (the workspace-mode tooling auto-synced `go.work` during the `go get`/`go mod tidy` runs), which in turn forced `eami-api`/`eami-gateway`'s Dockerfile builder stage off `golang:1.24-alpine` (verified necessary, not just prudent: `eami-gateway`'s Dockerfile sets `GOTOOLCHAIN=local`, so an unbumped builder image would have hard-failed rather than auto-downloading a newer toolchain) and CI's 4 `go-version: '1.24'` pins in `.github/workflows/build.yml`. Both of the latter were out of this task's original `MAY MODIFY` list — surfaced to the user explicitly before touching either (Dockerfile bump self-evidently required by the CVE fix already approved; CI pin change required a deliberate, user-confirmed exception to `CLAUDE.md`'s "never touch CI config" hard rule) rather than silently done or silently skipped.
  - **chi `RealIP`→`ClientIPFromRemoteAddr`** (`eami-api/internal/api/router.go`): chi deprecated `RealIP` in v5.3.0 for mutating `r.RemoteAddr` and unconditionally trusting `X-Forwarded-For`/`X-Real-IP` with no configurable trust boundary (GHSA-3fxj-6jh8-hvhx and siblings — real IP spoofing, not just a style deprecation). **Verified functionally inert before touching it**: a repo-wide grep for `RemoteAddr`/`ClientIP`/`X-Forwarded-For` inside `eami-api` found zero consumers of the resolved client IP anywhere in production code — nothing reads it today, so the swap changes no observable behavior, only closes the deprecated/spoofable code path for whenever something eventually does. Chose `ClientIPFromRemoteAddr` (trust only the raw TCP source, not any header) over a header-trust variant because `eami-api` is reachable two ways simultaneously — via `eami-ui`'s nginx proxy (which does set `X-Forwarded-For`/`X-Real-IP`, confirmed in `eami-ui/nginx.conf`) and directly on its own published Docker port — so there is no single trusted ingress that would justify trusting a caller-supplied header. New `router_realip_test.go`, 2 tests, proving directly (not just by code-reading) that a forged `X-Forwarded-For: 10.0.0.1`/`X-Real-IP: 10.0.0.2` on a request with `RemoteAddr = "203.0.113.7:54321"` still resolves `middleware.GetClientIP` to `203.0.113.7`, and a header-less request resolves correctly too.
  - **Non-root containers (`eami-api`, `eami-gateway`, matching `eami-collector`'s existing correct su-exec-based pattern):** added `USER 65532:65532` (distroless's own nonroot convention UID, numeric so it needs no `/etc/passwd`/`/etc/group` lookup — required for `eami-gateway`'s `FROM scratch` runtime stage, which has neither file) to both. **A real, non-obvious regression found by this task's own mandatory code-review pass, not caught by the initial live-verification run:** both `eami-api` and `eami-gateway` self-generate and persist an RSA JWT signing key to disk on first boot (`auth.loadOrGenerateKey`/`identity.loadOrGenerateKey`, both pre-existing from B-026/B-002-era work, unchanged by this task) — `eami-api` to `API_JWT_KEY_PATH` (`/certs/api.key`, the `api_certs` named volume) and `eami-gateway` to `GATEWAY_JWT_KEY_PATH` (`/certs/gateway.key`, `gateway_certs`) when those env vars are set (both compose files set them), or to `eami-gateway`'s hardcoded fallback `/var/lib/eami-gateway/gateway.key` (`config.go`'s `validate()`) when `GATEWAY_JWT_KEY_PATH` is unset. A brand-new named or anonymous Docker volume mounted over a path with no pre-existing content in the image defaults to root:root ownership — which UID 65532 can't write to. Fixed for all three paths by pre-creating and `--chown=65532:65532`-ing empty directories in each Dockerfile's builder stage (which has a real shell/`mkdir`, unlike the `scratch`/distroless runtime stages) and `COPY`ing them into the runtime image — a fresh volume then inherits that baked-in ownership on first population, standard practice for non-root `scratch`/distroless containers with a runtime-writable path. The `/certs` half of this was caught and fixed during the task's own live-verification pass (both containers crash-looped on first rebuild with `permission denied` reading the just-created key file); **the `/var/lib/eami-gateway` fallback-path half was missed by that same live pass — both compose files set `GATEWAY_JWT_KEY_PATH`, so it was never exercised — and was only caught by the separate, mandatory code-review pass**, which reasoned through the config fallback chain rather than only checking what was already running. Fixed and **re-verified live against the actual failure scenario**, not just re-read: a throwaway `docker run` of the rebuilt image with `GATEWAY_JWT_KEY_PATH` deliberately unset and a fresh anonymous volume (the exact "any other deployment method" case the finding describes) produced `"identity: RSA keypair written" path=/var/lib/eami-gateway/gateway.key` and a clean boot; the real compose-managed containers were then rebuilt and redeployed with the same fix and confirmed still healthy via `docker top` (both processes run as UID 65532, not root) and a full live E2E pass (below).
  - **One-time migration note for this machine's pre-existing dev volumes** (would apply to any real upgrade of an already-running deployment, not just this dev box): the `api_certs`/`gateway_certs` named volumes already existed from prior sessions with root-owned key files inside — baking ownership into a *new* image only affects volume population on genuine first-use, not an already-initialized volume. Both were `chown -R 65532:65532`'d directly (via a throwaway `alpine` container mounting each volume) as a one-time fix; documented here since a real production upgrade to this image would need the identical step, not just a fresh rebuild.
  - **Digest-pinned base images, all 4 service Dockerfiles** (real digests fetched via `docker pull`+`docker inspect`, not hand-typed): `eami-api`/`eami-gateway` builders bumped `golang:1.24-alpine`→`golang:1.25-alpine` (required by the go.mod bump above) and pinned by digest; `eami-api`'s `gcr.io/distroless/static-debian12` runtime and `eami-gateway`'s builder-stage `ca-certificates` source pinned by digest (`scratch` itself has no manifest/tag to pin — nothing floats there by construction); `eami-collector`'s `golang:1.24-alpine`/`alpine:3.19` pinned by digest with **no version bump** (not exposed to any in-scope CVE, kept minimal-diff per `CLAUDE.md`'s no-unrequested-scope-creep rule); `eami-ui`'s `node:20-alpine` (both `development`/`builder` stages) and `nginx:alpine` pinned by digest.
  - **`eami-ui/Dockerfile`'s false nginx comment, corrected, not just deleted:** the old comment claimed nginx "runs as the default nginx user (not root) ... via the `USER nginx` directive in the base image configuration." **Verified empirically wrong, not just by inspection**: `docker inspect nginx:alpine --format='{{.Config.User}}'` returns empty (no `USER` set at all) and `docker run --rm nginx:alpine sh -c id` shows `uid=0(root)`. The real mechanism (also verified directly, via `docker run --rm nginx:alpine cat /etc/nginx/nginx.conf`): the base image's own `nginx.conf` sets `user nginx;`, which only the *worker* processes — the ones that actually accept connections and serve files — read and drop to; the master process stays root by design, since binding port 80 requires root privilege, and this repo's `eami-ui/nginx.conf` (which only replaces the `server{}` block, not the base `nginx.conf`) doesn't touch that directive. Comment rewritten to state this accurately; no runtime behavior change (no `USER` directive added — that would break port 80 binding).
  - **`react-router-dom`/`react-router` and `vite` deliberately left unfixed, logged to `BACKLOG.md` (B-048, B-049):** both were assumed patchable in the task brief's own contracts, but investigation found neither has a non-major-version fix available. `react-router-dom`'s own GHSA-jjmj-jmhj-qwj2 advisory lists its patched version as literally "None" in the 6.x line (only `react-router` v7.13.0 fixes it — the installed 6.30.4 is already the newest 6.x release, confirmed via the npm registry). `vite`'s two CVEs (GHSA-4w7w-66w2-5vf9, GHSA-v6wh-96g9-6wx3) are fixed starting at 6.4.2/6.4.3 respectively — the installed 5.4.21 is already the newest 5.x release (confirmed via the npm registry) — a major bump beyond even the 8.2.0 `npm audit fix` itself suggested. Both decisions made explicitly with the user rather than assumed: consistent with the brief's own "Vite major-version migration" carve-out, react-router-dom got the identical treatment rather than being force-fit into a dependency-bump session as a one-off exception.
  - **`postcss` bumped 8.5.15→8.5.18** (patch-level, already inside `package.json`'s existing `^8.4.45` range — `npm install postcss@8.5.18` updated the declared range to `^8.5.18`) via the established Docker `node:20-alpine` builder-stage trick.
  - **`x/crypto`'s 17 known advisories, logged to `BACKLOG.md` (B-050), not fixed** — out of scope per the brief. `govulncheck` confirms all 17 are unreachable by `eami-api`'s actual call graph (only `bcrypt` is imported; none of the 17 affect it).
  - **Test coverage:** 2 new tests (`router_realip_test.go`, described above). `go build`/`go vet`/`go test ./...` clean across all 5 Go modules (`eami-agent`, `eami-api`, `eami-collector`, `eami-gateway`, `eami-policy`), including `eami-api`/`eami-gateway`'s existing real-Postgres integration suites — no regressions from the pgx/jwt/chi bumps. `govulncheck` clean (0 reachable vulnerabilities) across all 5 modules. Real `npm run generate-client && npm run type-check && npm run build` clean for `eami-ui` via the Docker builder-stage trick.
  - **Reviewer + security subagent passes, both mandatory, both ran as real independent sub-tasks against the actual diff** (not skipped, not simulated): **security pass** returned zero findings at ≥7 confidence after specifically tracing the RealIP swap's inertness, the `/certs` ownership-inheritance mechanism, the `VOLUME`/config-fallback interaction, DSN `sslmode` handling, and JWT algorithm-confusion guards (all unchanged, confirmed not newly affected). **Code-review pass** found the one real HIGH finding described above (`/var/lib/eami-gateway` fallback path) — fixed and re-verified live before commit — plus one LOW informational note (the `ClientIPFromRemoteAddr` swap has zero consumers today, matching the security pass's own "functionally inert" finding; not a defect, just worth knowing if IP-based audit logging is ever built later without checking this first).
  - **Live-verified end-to-end against the real `docker-compose` stack, rebuilt fresh (all 4 changed service images, `--no-cache`):** all services healthy; `docker top` confirmed `eami-api`/`eami-gateway` processes run as UID 65532 (non-root) — the actual process table, not just the Dockerfile's declared intent. Full real E2E flow through the rebuilt stack: logged in as a throwaway admin (created via direct SQL insert against the existing dev org — no signup/register route exists, matching prior sessions' established precedent), created a real `escalate`-action policy via the real `POST /v1/gateway/policies` API, issued a real agent JWT via `POST /v1/gateway/tokens`, opened a real MCP SSE session, posted a real `tool_call` — confirmed a genuine `approval_requests` row landed `pending`, approved it via the real `POST /v1/approvals/{id}/decide` API, confirmed the gateway resumed the held call and attempted the downstream dispatch (the final "unsupported protocol scheme" error is expected — no real downstream configured for this synthetic tool name, not a regression). Also confirmed directly: a forged `X-Forwarded-For`/`X-Real-IP` against `eami-api` produces a normal `200`, consistent with the in-process unit tests. All throwaway fixtures (admin user, policy, approval request) cleaned up afterward — confirmed via a final query that only the original `dev@example.com` seed user remains, zero leftover policies/approvals.
  - **CI verification — pushed (`bb2663e`), then watched the real run rather than trusting the `GOTOOLCHAIN=auto` theory, per explicit user instruction. Honest current status: not yet confirmed green, blocked on an external cause, not this task's code.** Run `31118536388`: `Docker — eami-gateway` and `Docker — eami-ui` both completed successfully (proving the go1.25 builder-image bump and the digest pins build cleanly in CI, not just locally). `Go — eami-agent (darwin/arm64)` failed twice — both times at "Getting action download info," before `actions/checkout` or any of this task's code ever ran, with `Error: Service Unavailable`/`Internal Server Error`. Cross-checked against the immediately prior run (commit `839e8ae`, the pre-B-047 baseline): the identical job, using the identical `actions/setup-go@v5` step, succeeded cleanly there — ruling out the go-version pin bump as the cause. Confirmed via `githubstatus.com`: GitHub Actions was in a **platform-wide major outage** (started 15:22 UTC, `qcvjkzcs7j74`, "runners being assigned jobs that are no longer valid") that began *before* this push and was still unresolved after two retries of the failed job. Per explicit user instruction, stopped actively polling once this was confirmed external rather than keep spending CI cycles against a declared incident — **to be rechecked once GitHub's status page shows the incident resolved**, not assumed green. `eami-gateway`/`eami-ui`'s clean CI passes plus this session's own exhaustive local `go build`/`vet`/`test`/`govulncheck` + live `docker-compose` verification are the load-bearing evidence for this task's correctness; the still-pending CI run is corroborating, not the only evidence.
  - **Known limitations, disclosed not hidden:** `eami-collector`'s `golang.org/x/sys` (indirect, via the `modernc.org/sqlite` driver chain) was left unbumped — not named in the task brief's contract, and not a direct import. This machine's pre-existing dev `api_certs`/`gateway_certs` volumes needed a manual one-time `chown` (documented above) — a fresh deployment wouldn't need this step, but an existing production deployment upgrading to this image would.
- **B-051 (2026-08-07): real database migration runner.** Closes the VM-appliance investigation's C7 finding: no mechanism existed to apply a schema change to an already-running, already-seeded database — `schema.sql` only ever ran once, via Postgres's own `docker-entrypoint-initdb.d`, on a genuinely empty data directory. `schema/migrations/001-010` were historical documentation of `schema.sql`'s evolution, never individually runnable.
  - **Tool choice: golang-migrate via its official `migrate/migrate` Docker image** (digest-pinned `v4.19.1@sha256:cc4ad8e1...`), not embedded as a Go library in any of the 5 existing service modules — zero new Go dependency added anywhere, matches this repo's established pattern of using official upstream images directly (`postgres:16-alpine`, `nginx:alpine`) rather than wrapping tools in custom binaries.
  - **Reconciliation, investigated not assumed:** read all 10 legacy migration files before deciding. Only `005_token_usage.sql`, `007_audit_partitions.sql`, and `008_paste_events.sql` are actually idempotency-guarded (`IF NOT EXISTS` throughout) — `001_init.sql`, `002`, `003`, `004`, `009`, and `010` all use bare `CREATE TABLE`/`ALTER TABLE ADD COLUMN`/`CREATE INDEX`/`CREATE TRIGGER` that error on any re-run (contradicting the task brief's own assumption that 007/010 were the guarded examples — 010 is actually unguarded, 005/007/008 are the real ones). Given a table-level diff between `schema.sql` and `001_init.sql` confirmed `schema.sql` is the accurate cumulative result, chose a **fresh baseline** over retrofitting 6 files with guards they never had: new `schema/migrations-v2/000001_baseline.up.sql` captures `schema.sql`'s current content verbatim, hand-rewritten with idempotency guards throughout (`CREATE TABLE/INDEX IF NOT EXISTS`, `DROP TRIGGER IF EXISTS` + recreate and `DROP POLICY IF EXISTS` + recreate since Postgres has no `CREATE TRIGGER`/`POLICY IF NOT EXISTS` form, `ON CONFLICT DO NOTHING` for the `model_pricing` seed, `add_retention_policy(..., if_not_exists => TRUE)`). `000001_baseline.down.sql` is a deliberate no-op (dropping every table is never the right response to a failed migration) — pairs with B-029's existing `backup-db.sh`/`RECOVERY.md` mechanism as the real rollback story instead. `schema/migrations/001-010` left in place, untouched, with a new `schema/migrations/README.md` marking them historical/non-runnable rather than silently ambiguous.
  - **`000002_add_orgs_table_comment`** — a deliberately trivial, genuinely harmless (`COMMENT ON TABLE orgs IS '...'`, pure metadata, zero structural/data impact, no application code reads table comments) second migration, proving the mechanism actually works end-to-end without making a real schema change — explicitly out of this brief's own scope. Flagged explicitly to the user as the chosen approach before building, per their own instruction to disclose rather than silently decide.
  - **`docker-compose.yml`/`.prod.yml`:** removed the old `./schema/schema.sql:/docker-entrypoint-initdb.d/01-schema.sql:ro` mount entirely — the new `migrate` service is now the *only* path, fresh installs and existing installs upgrading both go through the identical `migrate up` invocation, no fork. `migrate` runs once, applies pending migrations, exits; `eami-api`/`eami-gateway` gained `depends_on: migrate: condition: service_completed_successfully`, gating them from ever starting against a not-yet-migrated schema.
  - **New `scripts/migrate.sh`** — thin wrapper around `docker compose run --rm migrate ...` (reuses the exact same image/mount already defined in the compose files, not a second divergent definition) for the real-world "apply an update to an already-up stack" case. Supports `up`/`version`/`force VERSION` (force stamps a version without running SQL — for marking an already-correct database as caught up, e.g. this session's own dev database — requires interactive `y/N` confirmation, fails closed on non-interactive/empty input).
  - **A real MEDIUM security finding, found by the mandatory security review and fixed before commit:** `POSTGRES_PASSWORD` interpolated unescaped into a `postgres://` URL (in the new `migrate` service's `-database` flag, and in `scripts/migrate.sh`) can misparse or, on some golang-migrate versions, leak into error output if the password contains base64-reserved characters (`+`/`/`/`=`) — **not theoretical**: this session's own real dev `POSTGRES_PASSWORD` already contains a `+`, confirmed via direct inspection. Root cause fixed at the source: `.env.example`'s `POSTGRES_PASSWORD` generation guidance changed from `openssl rand -base64 24` to `openssl rand -hex 32`, matching `setup.sh`'s own generator (which already filters to alphanumeric-only) and every other secret in the same file. Defense-in-depth: `scripts/migrate.sh` gained a `urlencode()` bash function, applied before any DSN construction — re-verified live against the real `+`-containing dev password (`scripts/migrate.sh version` correctly returned `2`). The automatic-at-boot `docker-compose.yml`/`.prod.yml` `migrate` service path left as a documented residual — `migrate/migrate`'s minimal Alpine image has a shell but no reliable way to URL-encode from pure compose YAML without a fragile inline one-liner, and the risk is already substantially closed by the `.env.example` fix; same risk class already exists, unfixed, in `eami-api`/`eami-gateway`'s own pre-existing DSN construction (`config.go`), out of this brief's scope to touch.
  - **New standalone Go module `schema/migrationtest/`** — deliberately **not** added to `go.work`: doing so broke `eami-gateway`'s Docker build (that Dockerfile `COPY`s each workspace member's `go.mod`/`go.sum` individually into the builder stage, and `go mod download` fails if `go.work` references a path that doesn't exist on disk inside the container) — caught live during this session's own verification, not just reasoned about. Run via `GOWORK=off` (Go auto-detects the parent `go.work` for anything nested under the repo tree regardless of `use()` membership). 4 real-Postgres integration tests (`migrate_test.go`), each creating and dropping its own throwaway database (`eami_app` confirmed to have `CREATEDB` before relying on it, not assumed) rather than reusing one shared always-current schema the way this repo's other `_pg_test.go` files do: `TestMigrate_FreshDatabase_MatchesExpectedSchema`, `TestMigrate_ExistingSeededDatabase_AppliesNewMigrationWithoutDataLoss`, `TestMigrate_RunTwice_NoErrorNoDuplication`, `TestMigrate_FreshPathAndIncrementalPath_ProduceIdenticalFinalSchema`.
  - **Reviewer + security subagent passes, both mandatory, both ran as real independent sub-tasks** (the security review's first attempt hit a transient API connection error mid-run — retried cleanly, not silently skipped). **Security pass** found the one real MEDIUM finding above (fixed); traced and cleared: no injection risk in `scripts/migrate.sh`'s `force`/argument handling, no realistic un-migrated-startup window (Compose's `depends_on` correctly gates normal `up` invocations; bypassing it requires `--no-deps` or a raw `docker run`, which fails loudly with missing-relation errors, not silently), no dynamic-SQL injection risk in the baseline's partition-creation `DO` block (static literals only, no `EXECUTE`/string concatenation). **Code-review pass**, after mechanically diffing `000001_baseline.up.sql` against `schema.sql` (not eyeballing) and confirming zero content discrepancies, found 3 real issues, all fixed: `schema/migrationtest/go.mod` had pinned `golang.org/x/text v0.31.0`, reintroducing the exact GO-2026-5970 CVE B-047 (the immediately preceding session) had just bumped every other module to `v0.39.0` to fix — bumped, re-verified clean via `govulncheck`; the new test module had zero CI wiring — investigated and found this is actually a repo-wide gap (no Go module's tests run in CI at all, for any of the 5 existing services either), logged honestly as its own item, **B-052**, rather than narrowly wiring up just the new module; throwaway-database cleanup in `migrate_test.go` swallowed its own reconnection failure via `t.Logf` instead of `t.Errorf`, meaning a transient cleanup failure could leak a throwaway database without failing the test — fixed.
  - **Test coverage:** 4 new real-Postgres integration tests (`schema/migrationtest/migrate_test.go`, described above), plus manual verification beyond the automated suite: the baseline migration applied to a truly fresh Postgres (table count matched `schema.sql` exactly — 48 tables + `schema_migrations`); the raw SQL file re-run directly via `psql`, bypassing `migrate`'s own version tracking entirely, confirmed every single statement is a genuine idempotent no-op (`NOTICE: already exists, skipping` throughout, `model_pricing` row count unchanged). `go build`/`go vet` clean for `schema/migrationtest` (`GOWORK=off`) and all 5 existing workspace modules (confirmed unaffected by the `go.work` edit); `go test ./...` re-confirmed clean for `eami-api` and `eami-gateway` (no regression from the compose-file changes).
  - **Live-verified end-to-end, beyond the automated tests:** this session's real, already-running dev database (real `dev@example.com` admin user, real prior B-04x-series data) was stamped at baseline version 1 — but only after first confirming via direct SQL that its actual current schema genuinely matched the baseline (48 tables, both B-044/B-046's `action_paths`/`tool_server_ids` columns already present, no `schema_migrations` table yet) — then had migration 2 applied for real via `scripts/migrate.sh up`: the `orgs` table comment landed, `dev@example.com` and all existing data verified untouched afterward, a second `up` run correctly reported `no change`. Separately, a fully isolated fresh `docker compose -p eami-b051-test` project (genuinely empty Postgres volume, real rebuilt `eami-api`/`eami-gateway` images) confirmed via actual container state transitions — not just compose config inspection — that `migrate` completes and exits 0 (both migrations applied: `1/u baseline`, `2/u add_orgs_table_comment`) strictly before `eami-gateway`/`eami-api` ever reach the `Starting` state, proving AC #5's gating requirement holds for real. The isolated test project and all its volumes were fully torn down afterward (`docker compose -p eami-b051-test down -v`); the real dev stack's `postgres` container (briefly stopped to free port 5432 for the isolated test) was restarted and confirmed healthy with all data intact, and `eami-api`/`eami-gateway`'s own `/health`/`/healthz` endpoints confirmed `200` afterward.
  - **Known limitations, disclosed not hidden:** the automatic-at-boot `migrate` service's password-URL-encoding residual (documented above, both compose files); B-052 (CI test wiring, repo-wide) queued rather than fixed here — out of this brief's stated scope (mechanism only, no schema changes, and CI wiring is a materially separate, larger effort than this brief's own migration-mechanism focus).
  - **CI: confirmed green, not just pushed.** Pushed as `984605f`. GitHub's platform-wide Actions outage (the cause of B-047's still-pending CI status) had resolved by the time this commit's run started — confirmed via `githubstatus.com` ("All Systems Operational") before trusting the result. Run `31204946077` completed with `conclusion: success` across every job. Also closed the loop on B-047's own still-pending run: attempted to re-run the originally-failed `Go — eami-agent (darwin/arm64)` job now that the outage was confirmed resolved, but GitHub rejected it ("Only jobs from the current attempt can be re-run" — the re-run window on that now-superseded commit had expired). Not pursued further: several newer commits (including this one) have since built and passed clean CI, which is the practically relevant confirmation.
- **B-052 / B-016 (2026-08-08): `go test` runs for real in CI for the first time, across all 6 Go modules, gated on a real Postgres — and the gate caught a real pre-existing bug on its very first run.**
  - **Confirmed the gap for real before building:** read `.github/workflows/build.yml` in full — 364 lines, builds/pushes 4 Docker images and cross-compiles/vets `eami-agent` binaries, but `go test` appeared zero times anywhere, for any of the 6 modules.
  - **New `test` job** (`.github/workflows/build.yml`): matrix over the 5 `go.work` modules (`eami-agent`, `eami-collector`, `eami-policy`, `eami-api`, `eami-gateway`), each with its own `timescale/timescaledb-ha:pg16` service container — chosen deliberately over the stock `postgres` image because the schema requires the `pgvector`/`timescaledb` extensions the stock image lacks, and it's the same image `docker-compose.yml` already uses. `schema/migrations-v2` is applied via the pinned `golang-migrate` CLI (`go install -tags postgres .../migrate/v4/cmd/migrate@v4.19.1`, matching the version pinned in `docker-compose.yml`'s `migrate` service) before tests run, mirroring B-051's real production migration mechanism rather than a second, divergent schema-loading path. **User decision, explicitly asked rather than assumed:** all 5 legs get a Postgres service uniformly, even the 3 (`eami-agent`/`eami-collector`/`eami-policy`) with zero real-Postgres tests today — rejected the conditional-per-module alternative because the maintenance cost of remembering to add it back the day a 3rd module gains a real-Postgres test outweighs the marginal CI-time savings.
  - **New `test-migrationtest` job**: `schema/migrationtest` (deliberately outside `go.work`, per its own `go.mod`/`go.work`'s comment), run with `GOWORK=off` against its own Postgres service — no pre-migration step needed, since its tests manage their own throwaway per-test databases via `CREATE DATABASE`/`CREATEDB`.
  - **`build-images` gained `needs: [test, test-migrationtest]` — a deliberate, user-confirmed design fork, not the default choice.** The alternative (tests running fully in parallel with image builds, red CI without blocking a push) was presented explicitly; the user chose gating specifically because B-051's update mechanism now assumes published GHCR images are trustworthy — a broken test must never let a broken image reach the registry, even at the cost of losing `max(build,test)` parallelism in favor of sequential `build+test` time. Real cost measured, not assumed: ~1m40s baseline (pre-B-052 run `31205156691`) → ~2m54s–3m12s post-change across 3 separate clean runs — judged acceptable given `push: true` only fires on `main`/tags.
  - **The gate caught a real bug on its very first real run, closing B-016 (queued since 2026-07-22, discovered during B-002 Brief 2's first-ever real `go test` pass in this repo):** `eami-api/internal/api/finops.go`'s `FinOpsSummary`/`FinOpsTimeSeries` both called `s.queries.DB()` with **no nil-guard**, unlike every other handler in the package (`agents.go`, `policies.go`, `approvals.go`, `auth.go`'s own `"if s.queries != nil"` convention, `tools.go`'s `toolQueries()` seam). `finops_test.go`'s `newFinOpsTestEnv` deliberately builds a `Server` with `s.queries == nil` (a validation-path-only unit-test fixture); any request that passes validation reached the unguarded call and nil-pointer-panicked — silently recovered into an opaque 500 by chi's `Recoverer`, masked for who knows how long by the tests' own weak `!= http.StatusBadRequest` assertions (they never checked for `500` specifically). Confirmed with real evidence before fixing anything, not from a third-party log summary a review tool produced: reproduced the exact panic locally with a full stack trace (`finops.go:269`, `TestFinOpsTimeSeries_ValidGranularities/hour`) via `go test -count=1`.
  - **A real process failure, caught and corrected in-session rather than shipped:** an initial "all 6 modules verified locally" pass had actually been a **false negative** — Go's test-result cache doesn't invalidate on environment-variable changes without `-count=1`, so `go test ./...` silently reused a stale, pre-fix cached "ok" for `eami-api` across a `TEST_DATABASE_URL` change. Discovered by deliberately re-running with `-count=1` and getting a different, real answer. Both CI `go test` invocations subsequently gained `-count=1` too, closing the identical risk inside the gate itself — the exact class of gap CI was built to catch, almost present in the CI wiring itself.
  - **Fix:** both handlers gained `if s.queries == nil { writeError(500, "internal_error", "finops store is not configured"); return }`, matching the package's own established pattern exactly — deliberately **not** a mock-DBTX/fake-store fix (the initial lead, sourced from an unverified third-party log-summary tool, was explicitly rejected by the user before being acted on, since a fake store risks masking other real gaps behind fabricated success responses). Two new tests, `TestFinOps{Summary,TimeSeries}_NoStoreConfigured_ReturnsCleanError`, assert the exact clean 500 — mirroring `tools_test.go`'s existing `TestToolHandlers_NoStoreConfigured_ReturnsCleanError` precedent, closing B-016's own "strengthen assertions" acceptance criterion, not just the panic.
  - **Mandatory code-review pass (general-purpose subagent, run as a real independent sub-task against the actual diff) found a real HIGH finding the original brief missed:** the identical unguarded `s.queries.DB()` pattern also existed in `eami-api/internal/api/paste_events.go:314` (`PasteEventsTimeSeries`) — the file's own doc comment even cross-references `FinOpsTimeSeries` as a structural sibling, yet only two of the three raw-`DB()` call sites in the package had been guarded. Never caught by existing tests because `paste_events_test.go`/`paste_events_read_test.go` always require a real Postgres connection or skip cleanly — none builds a nil-`s.queries` server the way `finops_test.go` deliberately does. **Verified directly before fixing** (read the actual line, confirmed no existing test exercises the nil path), not taken on the review agent's word. Fixed identically; new `TestPasteEventsRead_TimeSeries_NoStoreConfigured_ReturnsCleanError` (a self-contained black-box test needing no real database at all, proven by running it in isolation with no Postgres available). Two smaller findings from the same review also applied: CI's `go test` steps gained `-count=1` (above), and a `finops_test.go` comment overclaiming the new tests check "status+body" was corrected to "status" (they only assert the status code). One finding — 3 of 5 matrix legs standing up an unused Postgres service — was **not** applied: already an explicit, user-confirmed design decision (see above), not a bug the review surfaced fresh.
  - **AC1 (the gate actually blocks on failure) proven twice, not once:** organically, when the real B-016 bug failed `Test — eami-api` on run `31251999449` and `Docker — ${{ matrix.service }}` was correctly skipped as a result (no staged breakage needed — a genuine bug did the job); and deliberately, per the brief's own explicit instruction, via a one-line assertion flip in `eami-policy/policy_test.go` (`2e7e284`), pushed, confirmed red (`Test — eami-policy` failed, `Docker` skipped, run `31253303734`), then reverted (`8042bcc`) and confirmed green again (run `31253438387`).
  - **Security review:** no findings. The hardcoded CI-only Postgres password is an ephemeral, service-container-scoped, non-secret placeholder (not reachable outside the job's network namespace); `golang-migrate` is installed at a pinned version tag, not `@latest`; the `finops.go`/`paste_events.go` guards are purely defensive early-returns introducing no new attack surface or data flow.
  - **Verified 2026-08-08 with a real toolchain, repeatedly, after discovering the cache gotcha above:** `go build`/`go vet` clean and `go test ./... -count=1` run fresh (no stale cache) across all 6 modules (`eami-agent`, `eami-collector`, `eami-policy`, `eami-api`, `eami-gateway`, `schema/migrationtest`) against a real Docker `timescale/timescaledb-ha:pg16` container matching the CI service config exactly (same image, same `golang-migrate` version, same port) — zero failures, zero flakiness across repeated runs (`eami-api/internal/api` alone re-run 3x fresh with no regression).
  - **Live-verified via real CI runs, not just local reproduction, per this session's standing "confirm actual CI status via githubstatus.com before trusting any result" discipline:** 5 separate pushes to `master`, each polled to completion (`a6eab3d` fail → `416db03` fix, pass → `2e7e284` deliberate break, fail (gate confirmed) → `8042bcc` revert, pass → `1bffdd4` paste_events fix + CI hardening, pass). `githubstatus.com` confirmed "All Systems Operational" before trusting every single result.
  - **Known limitation, disclosed not hidden:** no branch protection exists on this repo today, so a red run doesn't currently block a merge to `master` by GitHub's own enforcement — the gate's real teeth today are (a) `build-images`' `needs:` dependency, which does concretely block image pushes to GHCR on a failing `test`/`test-migrationtest`, and (b) the visible red commit status. Configuring branch protection itself is a separate, smaller follow-up, not done here (out of this brief's stated scope).
- **B-054 (2026-08-11): `scripts/setup.sh` now generates all 3 secrets `docker-compose.yml`/`.prod.yml` reference but `write_env()` never wrote.** Gap found and logged during B-053 (2026-08-08) while porting `setup.sh`'s secret-generation pattern into `appliance/scripts/eami-stack.sh`; left unfixed there deliberately since `setup.sh` was out of B-053's scope. Re-confirmed still present by reading `write_env()` directly before starting this task.
  - **Fix, `scripts/setup.sh` only:** `main()`'s "Generating secrets" step now also calls the existing `generate_secret` (`openssl rand -hex 32`) for `GATEWAY_EPISODE_READ_SERVICE_KEY`, `GATEWAY_TOKEN_REVOKE_SERVICE_KEY`, and `TOOL_CREDENTIALS_ENCRYPTION_KEY` — identical entropy/method to the pre-existing `SERVICE_KEY`/`COLLECTOR_API_KEY`, not a new generator. `write_env()` gained 3 new positional parameters (`$8`–`$10`) and writes all 3 into the `.env`'s `eami-gateway` section (a single entry each is correct — both compose files reference these var names identically from both the `eami-gateway` and `eami-api` service blocks, unlike `SERVICE_KEY` which needs two differently-named variables for the same value). Confirmed via `eami-api/internal/toolcreds/toolcreds.go` that the cipher requires exactly a hex-encoded 32-byte key — `generate_secret`'s existing output format already matches, no new validation logic needed.
  - **Investigated whether this should become shared logic with `appliance/scripts/eami-stack.sh` (which already generates all 3 independently via its own `gen_hex_secret()`) rather than a third duplication — decided against it:** extracting shared logic would require editing `eami-stack.sh`, outside this task's `MAY MODIFY` scope, and duplicating small security-relevant secret-generation snippets across `setup.sh`/`eami-stack.sh` already matches this repo's own established B-025 precedent (duplicate over shared infrastructure for this class of helper). `eami-stack.sh` itself is untouched.
  - **Live-verified, twice:** (1) extracted `write_env()` and its generator dependencies into an isolated harness (main()'s prompt/stack-boot steps not exercised) and confirmed a fresh `.env` contains all 3 new keys, each exactly 64 hex characters (32 bytes) — matching every other `generate_secret`-produced value in the same file. (2) Booted a fully isolated throwaway stack (`docker compose -p eami-b054-test`, fresh Postgres volume, real rebuilt images) with that generated `.env`: both `eami-gateway` and `eami-api` started cleanly with no restart loop and no missing-key errors (`docker compose logs` grepped clean for `must be set|required|fatal|panic|failed`, only routine Postgres first-boot noise present). Went beyond config-acceptance to prove the features are actually functional, not just that the vars are non-empty: `GET /v1/gateway/episodes` returned `200` with the real generated `GATEWAY_EPISODE_READ_SERVICE_KEY` and `401` with a wrong one; `POST /v1/gateway/tokens/{jti}/revoke` returned `400` (past auth, into normal business-logic rejection of a fake jti) with the real `GATEWAY_TOKEN_REVOKE_SERVICE_KEY` and `401` with a wrong one; gateway's own startup log showed `"tool router ready","credentials_configured":true`, confirming `TOOL_CREDENTIALS_ENCRYPTION_KEY` was accepted by the cipher. Throwaway stack and volumes fully torn down afterward (`docker compose -p eami-b054-test down -v`), confirmed via `docker volume ls`/`docker ps -a`.
  - **Incidental discovery during live verification, not caused by this task's changes, disclosed and resolved rather than left broken:** Docker Desktop was not running at the start of this session; starting it surfaced that the real local dev stack's `eaim-postgres-1` had been left in a stale/crashed state from an earlier session (its container `FinishedAt` timestamp and exit code 255 line up with the daemon restart, not with anything in this task) — `eami-ui` auto-restarted fine via its own `restart: unless-stopped` policy (no DB dependency), but `eami-api`/`eami-gateway` failed on auto-restart because Docker's own restart mechanism doesn't respect `docker-compose.yml`'s `depends_on`/`service_healthy` ordering the way `docker compose up` does. Restored via `docker compose up -d` (proper dependency ordering) plus `--force-recreate` on `eami-api`/`eami-gateway` (their auto-restarted containers had a stale/unreachable embedded-DNS network attachment even after Postgres came up healthy) — confirmed the real dev stack fully healthy again afterward, `dev@example.com`'s data and the `postgres_data` volume untouched throughout (never removed, only the already-dead process was restarted).
  - **No application code, `docker-compose*.yml`, or `.env.example` touched** — `scripts/setup.sh` is the only file changed, per this task's scope.
- **B-056 (2026-08-11): perf-seed tests no longer leak synthetic orgs into the shared dev database.** Logged during B-055 (2026-08-10, incidentally, while live-verifying that brief against the real dev DB and finding 30 leaked `paste-events-perf-*` rows dating to 2026-07-25) but misattributed at filing time to `paste_events_read_test.go:362`'s `seedPasteEventsPerfData` — investigation found that function does **not** leak (tested in isolation, zero leak every run; it already correctly used `t.Cleanup(pool.Close)`).
  - **Real root cause, found by direct empirical testing, not guessed:** `paste_events_test.go`'s `TestPasteEvents_ReportingQuery_UsesIndex_AtRealisticVolume` (B-032, 2026-07-25 — matching both the leaked orgs' exact naming, no `-read-` infix, and the leak's own dated history) has `defer pool.Close()` (a plain Go defer, scoped to the test function) alongside a `t.Cleanup`-registered `DELETE FROM orgs` for its two seeded orgs. `defer` unwinds when the test function itself returns; `t.Cleanup` callbacks are a *separate, later* teardown phase that only runs after the function body — including its own defers — has already fully completed. So the registered DELETE always ran against an already-closed pool, erroring immediately, with the error silently discarded (`_, _ = pool.Exec(...)`). Proven directly, not theorized: ran the test in total isolation (`-run`, single test, no timeout, no interruption) — clean `PASS`, exit 0 — and it still leaked both orgs (100,200 child rows each: 200 `endpoints` + 100,000 `paste_events`) every single time; the identical `DELETE` run manually via `psql` succeeded instantly with a clean cascade to zero remaining child rows, ruling out any FK/cascade explanation. A repo-wide grep confirmed no other real-Postgres test file in the module mixes `defer pool.Close()` with a `t.Cleanup`-registered row delete — an isolated, one-function bug.
  - **Fix, scoped as a rewrite rather than a one-line ordering patch, per explicit user direction:** both the buggy test and its non-buggy sibling (`seedPasteEventsPerfData`, converted too — seeding 200,000 synthetic rows into the shared dev DB at all is the underlying problem this item actually named, not just the one ordering bug) now create their own throwaway database per run, reusing `bootstrap_test.go`'s already-established `bootstrapTestPgConn`/`newThrowawayDB`/`applyMigrations` helpers directly (same `api_test` package, zero duplication) instead of `pasteEventsTestDSN`'s shared-dev-DB connection. The whole throwaway database — and everything seeded into it — is dropped by `newThrowawayDB`'s own `t.Cleanup`; the per-org `t.Cleanup` DELETE calls are gone entirely, removing the whole class of future ordering mistakes, not just this instance.
  - **Live-verified against all 3 scenarios the fix needed to hold under, including the two explicitly requested beyond a normal pass:** (1) normal run — all 3 affected tests pass, shared dev DB unchanged (still only `Dev Org`), zero leftover throwaway databases. (2) **simulated failed run** — a `t.Fatal` temporarily injected right after seeding (reverted immediately after): test fails as expected, cleanup still fires — zero leak in either the shared DB or a leftover throwaway database, confirming Go's `t.Fatal`→`runtime.Goexit` semantics still run registered cleanups. (3) **simulated interrupted run** — compiled the test binary directly and hard-killed it via `taskkill /F` mid-flight, twice (once during migration setup, once with one org's full 100,200 rows already inserted): in both cases the shared dev database's `orgs` table stayed untouched — the one remaining residual is the throwaway database itself being left behind, which no cleanup mechanism in any language can survive after a hard kill. This is the same already-accepted residual `bootstrap_test.go`'s own throwaway-DB pattern has always carried (not a new gap introduced here); both leaked throwaway DBs were uniquely named (`bootstraptest_<pid>_<rand>`), trivially identifiable, inspected, and manually dropped.
  - **Verification:** `go build ./...`/`go vet ./...` clean; `go test ./... -count=1` clean across the full `eami-api` module (including the full pre-existing suite, not just the 3 affected tests) — no regressions, comparable runtime to the pre-fix version (~23s for all 3 perf tests together, both before and after). Real dev database confirmed to still contain only `Dev Org` throughout and after this session's work.
  - **No application code touched** — `eami-api/internal/api/paste_events_test.go` and `paste_events_read_test.go` are the only files changed.
- **AI Provider Connector — Thread A Model 1 (2026-08-11): a new `ai_provider` `gateway_tools` type, dispatching to an external AI provider (Claude first) as a named tool through the real gateway dispatch path, exactly like a `rest_api` tool** (`claude/messages`, reusing MCP's existing async 202-then-SSE dispatch unchanged, non-streaming per this brief's own scope). This is the multi-provider adapter pattern from the Thread A investigation, Model 1: Claude is the first complete implementation of a real `Adapter` interface, not a special case bolted on. Also unblocks prompt redaction/tokenization architecturally (the gateway had no visibility into an agent's outbound LLM call before this) — that subsystem itself is not built here.
  - **Schema** (`schema/migrations-v2/000004_ai_provider_tools.up.sql`): `gateway_tools.type` CHECK widened to include `'ai_provider'`; new `provider TEXT` (nullable, deliberately no CHECK enum — validated at the Go layer against a small allowlist instead, so a second provider is one entry + one adapter file, not another migration); new `audit_mode TEXT NOT NULL DEFAULT 'structural_metadata_only' CHECK (IN ('full','structural_metadata_only'))` — a string enum (not boolean) so a future `'redacted'`/tokenized mode is a value addition later, deliberately not accepted yet since nothing implements it.
  - **New `eami-gateway/internal/aiprovider` package:** `Adapter` interface (`Provider() string`, `Dispatch(ctx, Credentials, Request) (Response, error)`); `Router` (`Resolve` — org+name+type-scoped SQL, mirrors `toolrouter.Resolve`'s discipline exactly; `Dispatch` — decrypts via the reused `toolrouter.Cipher`, looks up the registry, calls the adapter); `ClaudeAdapter` (real `x-api-key`+`anthropic-version` headers — not the `Authorization: Bearer` `toolrouter.Forward` assumes for REST tools, since Claude's own auth scheme genuinely differs; POSTs the agent-supplied, already-Claude-shaped params to `https://api.anthropic.com/v1/messages`); a disclosed duplicate of `toolrouter`'s `safeDialContext` SSRF guard (toolrouter is read-only scope this brief; same B-025/B-044 duplicate-rather-than-touch-frozen-code precedent). `Credentials{APIKey}` decoded from the same stored-JSON shape B-022 already uses, not a new credential shape. No request/response normalization across providers attempted (confirmed unrealistic without real per-provider translation work in the Thread A investigation, Part A) — the adapter abstracts *dispatch*, not request shape.
  - **`cmd/gateway/main.go` wiring, additive only** (B-044/B-046's `rest_api` dispatch logic itself untouched): a new `resolveAIProviderTool` (mirrors `resolveDynamicTool`'s fail-open contract), only queried when `resolveDynamicTool` didn't already match (code-review finding — the initial version paid a second DB round trip for every `mcp`/`database`-type dispatch too, not just genuinely-unregistered names; the fix means an already-registered `rest_api` tool still pays exactly one query, same as before this brief — a small residual extra query for `mcp`/`database`-type calls specifically was judged not worth further restructuring and is disclosed here, not silently accepted). Reuses `pc.ToolServerID` (Part 0's own finding: policy evaluation needed zero changes). One redaction point, applied once before the deny/escalate/allow switch: `auditEntry.Parameters = nil` when the resolved connector's `audit_mode != "full"` — scoped strictly to the `audit_log` write per AC5's literal wording; `approval_requests`/episodes keep showing full parameters unchanged (a human reviewer needs full visibility to decide, confirmed as correct existing behavior, not a bug, in the Thread A investigation Part 0 §6-7).
  - **`eami-api`:** `tools.sql.go`/`models.go` extended (not the frozen B-044/B-046 dispatch logic) with `Provider`/`AuditMode` on `CreateToolParams`/`UpdateToolParams`/`GatewayTool`; `CreateTool` defaults `AuditMode` to `"structural_metadata_only"` **at the store layer**, not only in the handler — a more robust guarantee (holds for any caller, not just ones that remember to set it; caught a real gap where two pre-existing tests constructing `CreateToolParams` directly would otherwise have violated the new CHECK constraint). `tools.go`'s `CreateTool`/`UpdateTool` validate `provider` against the allowlist and reject it outright for non-`ai_provider` types (code-review finding: an earlier version silently accepted+persisted `provider` on any type); validate `type=ai_provider` requires `auth_type="api_key"` at creation (the only shape the adapter understands, catching a misconfiguration at creation instead of on every real dispatch); `UpdateTool` fetches the target row's actual current type (reusing the existing `GetToolForTest` read, no new query) before honoring a `provider`/`audit_mode` PATCH, closing the identical gap on the update path. `tool_connectivity.go` gained an honest `ai_provider` case (matches the `mcp` case's own precedent: reports "not implemented" rather than fabricating a "connected" — testing it for real would spend provider API credits on every click and duplicate the adapter's own wire-format knowledge in a second file).
  - **`eami-ui`:** `ToolsPage.tsx`/`useTools.ts` — `ai_provider` added to the type union (`Omit<Tool,'type'> & {type: ToolType}`, since `type` is a generated-schema literal union `openapi.yaml` doesn't document yet, matching B-046's own undocumented-field precedent); a Provider dropdown (structured as a real list — `claude` today, a second provider is one more `<option>`, matching AC2's "ready for a second provider without UI rework") and an Audit-mode dropdown, defaulted to `structural_metadata_only`; Auth type forced to and locked at `api_key` when Type is `ai_provider` (code-review finding: the two selects were previously independent, so a user could leave a stale unrelated auth type selected with no visible sign until a confusing 400 on submit); `TypeBadge` gained a color for the new type (was falling through to the generic gray "unrecognized type" styling).
  - **Scope expansion 1, explicitly authorized mid-brief, matching B-039's own `mcp/handler.go` precedent for "a real gap found during this task's own investigation that makes an acceptance criterion unsatisfiable without the fix":** live verification found `approval.Router`'s resume logic (`resolve()`'s "approved" case) was hardcoded to the static `fwd *proxy.Proxy`, with **zero** awareness of `toolRouter`/`aiProviderRouter` — meaning an approved escalation for **any** dynamically-routed tool, `rest_api` (B-044) included, never actually reached the destination it was escalated for; it silently hit the static proxy instead. Not a new bug — B-044's own session had already found and explicitly disclosed this exact gap for `rest_api` without fixing it (`CONTEXT.md`: "`internal/approval/router.go` are completely untouched, so approved escalations keep using the static proxy exactly as before"). Fixed with a new `dispatchApproved` method: resolves the tool via `aiProviderRouter` then `toolRouter` (mirroring `main.go`'s own resolution order), falling back to the static proxy only if neither matches — closing the gap for both types at once, scoped to the resume dispatch path only, `Submit`/`Hold`/the LISTEN-NOTIFY mechanism itself untouched. `New()` gained two new nil-safe params; all 4 existing call sites (1 production, 3 test) updated to pass them (or `nil, nil`, preserving prior behavior exactly for tests that don't exercise resume dispatch).
  - **Scope expansion 2, also explicitly authorized, found by this session's own mandatory security review of expansion 1 — judged more serious than the routing bug it followed, since it's a bypassable safety guarantee, not a broken feature:** re-resolving by `(org_id, name)` at resume time (expansion 1's own design) meant a lower-privileged `admin`/`operator` role — which cannot itself approve or deny an escalation, and which the human approver has no visibility into having acted — could edit the escalated connector's `base_url`/`credentials`/`provider` (or, found by a **second** security review pass on THIS fix, `action_paths` alone, base_url/credentials untouched) during the hold window, silently redirecting the approved call to a different destination than the one actually reviewed. The approver-facing UI/API shows only agent/tool/action/justification/risk — never `base_url`, `provider`, or a credential fingerprint — so there was no way for a human to detect this from the approval screen itself.
    - **Fix:** at escalation time (`main.go`, already resolving the tool before policy evaluation), the resolved connector's ID and `approval.ComputeConfigHash(toolType, baseURLOrProvider, credentialsEncrypted, actionPathsJSON)` — SHA-256 over type + base_url-or-provider + the **encrypted** credential bytes (never plaintext; needs no cipher/key, and re-encryption's fresh GCM nonce means any credential touch, even resaving the identical secret, always changes the hash — confirmed by inspecting `toolcreds.Cipher.Encrypt`'s real nonce generation, not assumed) + a canonical (`encoding/json.Marshal`, always sorted-key) serialization of `action_paths` — are pinned into new `Request.ResolvedToolID`/`ResolvedConfigHash` fields, persisted by `Submit()` into two new `approval_requests` columns (`schema/migrations-v2/000005_approval_resolved_config.up.sql`). At resume, `dispatchApproved` fetches the connector **by ID** (a new `fetchResolvedConnector`, raw SQL, org-scoped — never by name, closing the delete-and-recreate-under-the-same-name variant too, since a different row has a different ID), recomputes the hash, and **fails closed** — refuses to dispatch, returns a clear error, records a new `resume_outcome` (`'dispatched'|'config_changed'|'connector_deleted'|'static_fallback'`, closing the audit gap: what actually executed at resume time is now recorded, not just the original pre-hold escalation entry) — if the connector is gone or the hash differs. `resolved_tool_id`'s FK uses `ON DELETE SET NULL` (deliberately different from this table's other FK columns like `policy_id`/`agent_id`, which have none — found live by this fix's own test suite, which tried to delete a referenced tool and got blocked by a naive FK; a historical approval record must never prevent an admin from deleting a tool).
    - **The `action_paths` gap was found live, not assumed:** this fix's own mandatory security review (run twice — once on the routing fix, once specifically re-checking the config-pinning fix) and an independent code-review pass **both independently converged on the identical finding**: the first version of `ComputeConfigHash` covered only `base_url`/credentials, missing that `action_paths` alone (unrelated to `base_url`/credentials) fully determines a `rest_api` tool's real per-action destination/method (`toolrouter.Forward`). Fixed in the same pass, with its own dedicated live-simulation test.
  - **Test coverage:** `eami-gateway/internal/aiprovider` (new package) — `Resolve` (real Postgres: found/not-found/wrong-type/cross-org), `Dispatch` (decrypt-and-route wiring, no real DB needed), `ClaudeAdapter` (real headers, real round trip via `httptest`, credential-never-in-error-message, response-size-cap, SSRF guard against the real dialer). `main_ai_provider_pg_test.go` — `resolveAIProviderTool` mirroring `resolveDynamicTool`'s existing test shape exactly (found/unregistered/wrong-type/cross-org/empty-input), plus a real-Postgres proof the fail-safe `audit_mode` default holds through the full insert-then-resolve path. `eami-api` — `tools_ai_provider_test.go`, provider/audit_mode validation on both `CreateTool`/`UpdateTool` (valid, unknown-provider-rejected, missing-provider-rejected, wrong-auth-type-rejected, wrong-type-rejected, invalid-audit-mode-rejected, omitted-leaves-unchanged) plus response-shape round-trip. `eami-gateway/internal/approval` — `router_dispatch_test.go`: the resume-routing fix (ai_provider/rest_api both resume to their real resolved destination, unregistered-tool/nil-routers still fall back exactly as before) **and** the TOCTOU fix's fail-closed behavior proven via **real mid-hold Postgres mutations, not mocks**: a real `UPDATE ... SET credentials_encrypted`, a real `UPDATE ... SET base_url`, a real `UPDATE ... SET action_paths`, and a real `DELETE` against the actual `gateway_tools` row while an approval sits `pending`, each followed by a real `dispatchApproved` call proving the resume is refused and the correct `resume_outcome` is recorded — this is what "actually change a connector mid-hold and confirm the resume rejects it, don't just reason about it" meant at the automated-test level, in addition to the live proof below. `go build`/`go vet`/`go test ./... -count=1` clean across all three affected Go modules (`eami-gateway`, `eami-api`, `schema/migrationtest`) throughout, including the full pre-existing suites — no regressions at any point. Real `npm run type-check`/`npm run build` clean for `eami-ui`.
  - **Reviewer + security subagent passes: three full rounds, mandatory per the standing rule (new credential type, new external-network dispatch surface, real API keys to a third party), each finding real, distinct issues before the next was clean.** Round 1 (base feature): code review found 4 issues (a `schema.sql`/sqlc concern investigated and found overstated — `tools.sql.go` was never actually sqlc-generated, no `tools.sql` query source exists — disclosed not fixed; 2 real `CreateTool`/`UpdateTool` type-validation gaps, fixed; 1 minor extra-query inefficiency for `mcp`/`database` dispatches, disclosed); security review found nothing (traced credential handling, the SSRF-guard duplicate, org-scoping, input validation, and the audit-redaction logic — confirmed all correct as designed). Round 2 (resume-routing fix, scope expansion 1): security review — explicitly mandated to check destination-integrity — found the real TOCTOU gap described above (8/10 confidence, full exploit trace); code review found 3 more issues (2 real `provider`/`audit_mode` type-consistency gaps on `CreateTool`/`UpdateTool`, fixed; 1 UI polish gap, fixed). Round 3 (config-pinning fix, scope expansion 2): security review and code review **both independently found the identical `action_paths` gap** described above, fixed in the same pass; security review confirmed org-scoping/no-internal-TOCTOU/no-misroute/credential-hash-safety all hold given the fix, and found nothing further against its explicit "can any role still redirect an approved action" mandate.
  - **Live-verified end-to-end against a real, isolated `docker-compose` stack (`-p eami-aiprovider-test`, fresh Postgres, real rebuilt images), with a real Anthropic API key — a real one-off operational mistake caught and corrected mid-verification, disclosed not glossed over:** an early rebuild omitted the `-p` project flag, silently rebuilding a *different* project's image tag and leaving the throwaway stack's actual running image stale for several verification steps (surfaced when a pinned `resolved_tool_id` kept coming back NULL despite the DB-level query working correctly in isolation) — re-diagnosed from first principles (confirmed the exact SQL directly via `psql`, ruling out a query bug, before suspecting the build) and corrected by rebuilding with the correct `-p` flag for all three images. Once corrected: **AC1** — a real agent `tool_call` for `claude/messages` on a directly-allowed connector reached the real Claude API and returned a real response (`"AI_PROVIDER_TEST_OK"`). **AC2** — provider/credentials/audit_mode configured per-connector via the real `eami-api` CRUD, response never contains credentials. **AC3** — a real `tool_names:["claude"]` escalate policy correctly escalated the call (real `approval_requests` row, `status=pending`). **AC4, the full standard, not partial** — submitted, visible, approved via the real admin decide API, **and the original call genuinely resumed**: a second full cycle after the fix returned a real Claude response (`"RESUME_FIX_VERIFIED"`) over the live SSE stream, with `resume_outcome='dispatched'` recorded. **The fail-closed behavior itself, live, not just via Go tests** — a third cycle: escalate, then a real `PATCH` (the exact attacker/operator-role action) swapping the connector's credentials while the request sat pending, then approve — the resume was genuinely refused, `resume_outcome='config_changed'` recorded, matching the automated test suite exactly. **AC5** — `audit_log.parameters` confirmed `null` for the default-mode connector's call and fully populated for the explicit `full`-mode connector's call, in the real database. **AC6** — grepped all three services' real logs for the actual API key's distinctive suffix across the entire session: zero matches. Every throwaway resource (org, agent, 2 connectors, policy, 3 approval requests, the isolated stack's containers/volumes/network, the helper container, the on-disk API key file) confirmed torn down/deleted afterward; the real dev stack confirmed untouched throughout (still only `Dev Org`).
  - **Out of scope, as specified:** streaming (Model 2), Gemini/OpenAI adapters themselves (the interface supports adding them; not built), redaction/tokenization (architecturally unblocked, not built), transparent SDK passthrough (Model 2).
- **B-059 (2026-08-12): Multi-Hop Workflows, Brief 2 — execution engine, per-hop TOCTOU pinning, static per-step parameters.** Makes a B-058-defined workflow (Brief 1: definitions only, zero execution) actually run: each step dispatched through the exact same, completely unmodified `dispatch()`/policy/audit path a standalone MCP call uses. Output→input mapping between steps and durable escalation pause/resume are both explicitly deferred to later, unscoped Thread B briefs.
  - **New `eami-gateway/internal/workflow` package:** `resolve.go` (`Resolve(ctx, pool, orgID, workflowID)` — reads `workflows`/`workflow_steps`/`workflow_step_params` directly via the gateway's own pool, mirroring `toolrouter.Resolve`'s established pattern of gateway-side reads of eami-api-owned tables in this shared-Postgres deployment model; returns `ErrDanglingStep` before any step executes if one references a deleted connector — B-058's `ON DELETE SET NULL` state); `connector.go` (a by-ID `gateway_tools` read, deliberately duplicated from `approval/router.go`'s unexported `fetchResolvedConnector` rather than imported, matching this repo's own established B-044/B-025 duplicate-small-security-helper precedent); `executor.go` (`Executor.Run` — the per-step loop); `http.go` (`POST /v1/gateway/workflows/{workflowId}/run`, agent-JWT authenticated via the same `identity.Manager.Validate`+`registry.LookupByName` pattern `mcp.Handler.parseBearer` already uses, synchronous — the HTTP response blocks until the run finishes).
  - **Per-hop TOCTOU pinning is what calling the existing `dispatch()` closure once per step already gives, unmodified, for free — this brief's own central design question, resolved by inspection rather than new mechanism.** `dispatch()` (`cmd/gateway/main.go`) already does its own complete resolve→policy→TOCTOU-pin(if escalating)→dispatch cycle inside one call; calling it N times in sequence, once per step, means each step independently re-resolves and re-pins its own connector immediately before its own policy evaluation — there is no shared "resolve everything upfront" phase for a later step's connector edit to hide behind undetected. Upfront pinning (resolving every step's connector once at workflow start) was considered and rejected: it's strictly weaker, since a later step's connector could still be edited during an earlier step's escalation hold without upfront pinning ever re-verifying that at the actual dispatch moment — at which point it's just per-hop pinning again, with a wasted, misleading earlier snapshot.
  - **Escalation mid-chain reuses `Hold()` unmodified, automatically, as this brief's explicit interim behavior** (a real durable pause/resume mechanism is later-brief scope): because `dispatch()` itself is called verbatim per step, a step whose policy escalates transparently blocks inside `Submit()`+`Hold()` exactly as a standalone MCP call would, with zero special-casing in the new execution code. The whole run — and the HTTP request that triggered it — blocks for up to `holdTimeout`; no durable persistence survives a process restart mid-hold.
  - **`Executor.runStep`'s per-step contract:** resolves the step's connector by ID (informational only — for `workflow_run_steps`' own audit record, via the exact same `approval.ComputeConfigHash` B-057 already exports); runs an independent, informational pre-dispatch `policy.Evaluate` call (recorded as `projected_decision`, purely so the audit trail can show "this step escalated" even though `dispatch()`'s own return value alone can't distinguish that from "always allowed" — never used to skip or alter what `dispatch()` itself enforces); calls the real, unmodified `dispatch()`; classifies the real outcome into `allowed` / `denied` (an `*mcp.PolicyDeniedError`) / `blocked` (any other dispatch error — escalation denied/timed out, or a downstream failure), with the raw error always preserved in `error_detail` regardless of the coarser bucket. The first non-`allowed` step stops the run — no partial-failure recovery or step-skipping, explicit v1 scope.
  - **New schema (`schema/migrations-v2/000007_workflow_execution.up.sql`):** `workflow_step_params` (1:1 with `workflow_steps`, a deliberately *separate* table rather than a new `workflow_steps` column or reuse of the already-present `input_mapping` — ~~that column stays untouched, reserved for the later output→input-mapping brief~~ **closed by B-063, see immediately below**; conflating "admin-typed static constant" with "value derived from a prior step's response" would collide two concepts that will likely need to coexist per step once mapping ships); `workflow_runs`/`workflow_run_steps` (the execution audit trail, separate from B-058's static definition) — `workflow_id`/`workflow_step_id`/`resolved_tool_id` all `ON DELETE SET NULL`, mirroring `approval_requests`' established precedent throughout: a run is a historical record, and deleting the definition or connector it ran against must never be blocked by that history.
  - **`eami-api`: new `internal/api/workflow_step_params.go` + `internal/store/workflow_step_params.sql.go`** (new files — `workflows.go`/`workflows.sql.go` from B-058 are completely untouched, per that brief's own boundary carried forward into this one). `PUT`/`GET /v1/gateway/workflow-steps/{stepId}/params`, same role gating as `workflows`' own write/read groups. `UpsertWorkflowStepParams` is one atomic `INSERT...SELECT...WHERE EXISTS` statement combining the org-ownership guard (`workflow_steps` → `workflows.org_id`, since `workflow_step_params` has no `org_id` column of its own, matching `workflow_steps`' identical no-`org_id` shape) with the write itself — no separate check-then-write TOCTOU window. **Known minor limitation, disclosed not fixed:** a literal JSON `null` body passes the handler's "must be an object" validation (unmarshals into a nil map without error) and degrades gracefully to "no params set," rather than being explicitly rejected like an array/scalar body is (confirmed those ARE rejected, via a passing test) — low severity, no security impact, not blocking.
  - **Test dispatch harness — a real, disclosed simplification, not a mock:** `dispatch()` itself is an unexported closure local to `main.go`'s `run()` — not importable into `internal/workflow`'s tests, and refactoring it to be testable would itself be exactly the kind of change to B-057's logic this brief must not make. `eami-gateway/internal/workflow/testenv_test.go` reconstructs dispatch()'s real shape (real `policy.Evaluator`, real `audit.Writer`, real `episode.Recorder`, real `approval.Router` with real `Submit`/`Hold`/`LISTEN`/`NOTIFY`, real `aiprovider.Router`) using only exported constructors — the same real pipeline `main.go` itself wires, just reassembled at the test-file level. Test connectors are all `type=ai_provider` dispatched via a fake `Adapter` (mirroring `approval/router_dispatch_test.go`'s own `fakeProviderAdapter` pattern), not `rest_api` via `toolrouter.Forward` — found live, not assumed: the first test version used `rest_api` tools pointed at a local `httptest.Server`, and `toolrouter`'s real, correct SSRF guard (B-023-class) rejected every call as loopback, exactly the same reason `router_dispatch_test.go`'s own "genuinely dispatches and succeeds" proofs already use `ai_provider` instead of `rest_api`.
  - **Test coverage:** `eami-gateway/internal/workflow` — 10 real-Postgres integration tests covering all 6 acceptance criteria: full multi-step execution with real per-step static params (AC1); a middle step's policy denial stopping the run before a later step's `workflow_run_steps` row is ever created (AC2); escalate→approve→resume and escalate→deny→stop, both via the real `Hold()`/`pg_notify` mechanism with a background goroutine + `waitForPendingApproval` polling helper (AC3); two distinct TOCTOU scenarios kept deliberately separate (AC4) — a non-escalating connector edit between steps is *correctly* picked up by the next step (proves no accidental upfront-caching bug, not a vulnerability, since nothing was ever "reviewed" against a stale snapshot for an allow-path step) versus the real attack, an escalating step's own connector edited specifically during its hold window, which correctly fails closed via B-057's completely unmodified `dispatchApproved` mechanism; `workflow_run_steps` audit-trail accuracy inspected directly against real inserted rows (AC5); no output→input mapping, proven by a distinctive-marker-leak check across two real fake-adapter calls (AC6). Plus `eami-api/internal/api/workflow_step_params_test.go` — 3 real-Postgres/real-HTTP tests (round-trip incl. full-replace-not-merge, cross-org rejection via the atomic upsert guard, non-object-body rejection). **Verified 2026-08-12 with a real toolchain: `go build`/`go vet`/`go test ./... -count=1` clean across both `eami-gateway` and `eami-api`, full pre-existing suites included, no regressions.**
  - **A real test-authoring bug reproduced a THIRD time this session (the identical B-056-class `t.Cleanup`-registered-DELETE-after-a-plain-`defer pool.Close()` ordering bug B-058's own session already found and fixed twice) — found and fixed again, in the new `eami-api` params test file:** confirmed via a real leaked-org check against the shared dev database, fixed identically (`t.Cleanup(func(){ pool.Close() })` registered before any org-seeding call so LIFO ordering runs the real DELETE first), re-verified clean, dev database confirmed back to just `Dev Org`.
  - **Reviewer + security passes: the automated code-review subagent failed on a platform session-limit outage mid-task (not a finding — the identical class of tooling failure B-046's own session already hit and disclosed) — substituted with a direct manual review, disclosed here rather than glossed over.** That manual pass found and fixed one real bug, a direct analog of the exact issue B-039 already found and fixed for the MCP async path: `workflow/http.go`'s `HandleRun` originally passed `r.Context()` straight into `Executor.Run` — since this handler is synchronous (unlike MCP's `ServeMessages`, which returns immediately after a 202 and dispatches in a detached goroutine), the request context stays live for the whole run in the normal case, but a client disconnect mid-`Hold()` (reproduced live, organically, during this session's own testing via a killed `curl` client) cancels that context, and every subsequent DB write depending on it — including the final run-status `UPDATE` — then silently fails, leaving `workflow_runs.status` stuck at `'running'` forever. Fixed identically to B-039's own precedent: `context.WithoutCancel(r.Context())`. Security review (this session, a direct adversarial trace-through specifically aimed at the brief's own stated bar — "the same rigor as B-057's original adversarial proof"): confirmed the workflow runner's own pre-dispatch connector read and config-hash computation are purely informational and structurally cannot be substituted into or weaken `dispatch()`'s own completely independent enforcement (the runner never branches on its own read, only records it); confirmed cross-org scoping holds throughout `Resolve`/`HandleRun` (workflow resolution is always scoped to the authenticated agent's own resolved org via `registry.LookupByName`, never client input); zero exploitable findings.
  - **Live-verified end-to-end against the real `docker-compose` stack, rebuilt fresh both before and after the context-cancellation fix:** a real 2-step workflow (`Test` connector, allow → `manual_verify_tool` connector, matches a real pre-existing escalate policy) executed via a real agent JWT against `POST /v1/gateway/workflows/{id}/run` — step 1 genuinely dispatched to a real external endpoint (`postman-echo.com`) carrying its own real static params (set via the new `PUT .../params` endpoint, round-tripped through a real `GET`); step 2 genuinely blocked the HTTP request on a real pending `approval_requests` row (confirmed mid-flight via a direct query). **The real TOCTOU attack, not simulated:** while step 2's approval sat pending, its connector's `base_url` was edited via a real `PATCH /v1/gateway/tools/{id}` call to an attacker-controlled-looking URL, then approved via the real `POST /v1/approvals/{id}/decide` — resume correctly refused (`resume_outcome='config_changed'`, step `outcome='blocked'` with a clear error message, run `status='failed'`), proving B-057's completely unmodified mechanism engages correctly for an escalation that originated from inside a workflow run, not just a standalone call. A second run, connector restored and no tampering, completed successfully end-to-end (`status='completed'`, both steps `outcome='allowed'`, step 2's real dispatch carrying its own distinct static params, visibly different from step 1's — confirming AC6 live as well as by test). **The context-cancellation bug reproduced and fixed live, before/after:** killed the triggering `curl` mid-hold, confirmed (pre-fix) the run stuck at `'running'` forever even after the approval was later decided; after the fix, repeated the identical kill-mid-hold scenario and confirmed the run correctly reached `'completed'` once decided, with the original client long gone. All gateway/API container logs confirmed clean of errors/panics throughout; shared dev database confirmed to contain only `Dev Org` before, during, and after this session's work.
  - **Dependencies:** B-058 (workflow definitions, read via a new gateway-side `Resolve`, never modified), B-057 (TOCTOU pinning mechanism — `ComputeConfigHash`, `dispatchApproved` — reused via the unmodified `dispatch()` closure, not reimplemented), B-039 (`Hold()` blocking mechanism, reused unmodified). **Out of scope, as specified:** output→input mapping, durable pause/resume for escalation, chain-aware approval UI (the approver sees the same single-step card as any other escalation today), any UI for triggering/viewing workflow runs (live verification used direct API calls throughout), branching/conditional logic.
- **B-063 (2026-08-13): Multi-Hop Workflows, Brief 3 — output→input mapping.** A later step's parameter can now be extracted from an EARLIER step's real recorded result within the same run (the Freshservice JSON-Parser-node pattern) — the piece that makes Thread B a real data pipeline, not a sequence of unconnected calls. Re-verified the MCP Discovery investigation's D9 finding directly against current schema before building: **zero new tables, zero new columns, zero new migration** — `workflow_steps.input_mapping JSONB` (B-058) and `workflow_run_steps.result JSONB` (B-059) already existed for exactly this purpose. Expression library: `github.com/tidwall/gjson` — pure read-only JSON-path querying, no eval/code-execution capability.
  - **CRITICAL FINDING, surfaced in the plan before any code was written, changed the file scope from what the brief itself assumed:** `workflow_steps.id` was NOT actually stable across a save — `eami-api/internal/api/workflows.go`'s `UpdateWorkflow` unconditionally deleted every step row and reinserted fresh ones (brand-new UUIDs, `input_mapping` never even passed through `InsertWorkflowStep`) whenever a request included a `steps` array, which the current UI does on every edit-panel submit, including an unrelated rename. Extraction wiring set up today would have been **wiped by the very next save**, not just misdirected by a reorder (the brief's own AC2 framing) — worse than anticipated, and not achievable within the brief's stated `MUST NOT MODIFY: B-058's core CRUD` boundary as literally written. **Presented to the user as a genuine scope-boundary fork rather than decided unilaterally** (same category as B-044/B-059's own presented forks) — approved: a minimal, additive touch preserving 100% of existing behavior for any caller that doesn't use the new field.
  - **`eami-gateway/internal/workflow`:** `types.go` — `ExtractionRef{FromStep, Path}`, `Step.InputMapping`. `resolve.go` — now also reads `input_mapping`. New `params.go` — `resolveParams(step, priorResults map[uuid.UUID]StepResult)` merges static params with extracted values; **extraction overwrites a static value for the same key — the documented, deliberate precedence rule**; a `from_step` absent from `priorResults` (covering "future step," "different workflow," and "different run" identically, since all three are simply absent from the map) or an unresolved `gjson` path both fail the WHOLE step cleanly, no partial substitution. `executor.go` — `Run` accumulates `priorResults` per-run, purely in-memory (never a DB read-back — the entire mechanism AC4's cross-run/cross-workflow isolation rests on); `resolveParams` runs after connector resolution and before dispatch, recording `outcome='blocked'` with a clear `error_detail` on failure exactly like the existing dangling-connector path — **dispatch is never called**.
  - **`eami-api`:** `WorkflowStepDTO` gained `ID`/`InputMapping` (new `ExtractionRefDTO`); `InsertWorkflowStep` takes a caller-assigned `id` (was DB `DEFAULT`) plus `inputMapping []byte`; new `resolveStepIDs` reuses a submitted step's id only if it matches a currently-existing step of THAT workflow (`ListWorkflowSteps` scoped by `workflow_id`, itself already org-verified via `GetWorkflow` earlier in the handler) — anything else mints fresh, identical to today's behavior for a caller that's never sent this field.
  - **A second real bug found live during this brief's own verification (not part of the originally approved plan, fixed the same session):** `workflow_step_params.workflow_step_id` has `ON DELETE CASCADE` — so even with step-id reuse working, the delete-then-reinsert still cascade-deleted a step's *static* params the instant its row was deleted, even though the same id got reinserted moments later. Reproduced live: an unrelated sibling-step edit silently zeroed a step's static params to `{}`. Fixed by capturing each reused step's current params (existing B-059 `GetWorkflowStepParams`) before the delete, re-writing them (existing B-059 `UpsertWorkflowStepParams`) inside the same transaction right after the reinsert — no new SQL.
  - **Reviewer + security passes, both mandatory, both clean.** Code review found and both got fixed: `go.mod` had `tidwall/gjson` mis-marked `// indirect` (`go mod tidy` not run, cosmetic); the same real step id echoed on two different submitted steps in one request would hit `workflow_steps`' primary-key uniqueness (23505, a code `isForeignKeyViolation` doesn't recognize) and leak a raw Postgres error as a 500 — fixed with an explicit duplicate-id check in `validateWorkflowSteps`. Security review, matching B-042's own rigor for the AC4 isolation claim: independently re-derived that `resolveParams` has zero DB calls anywhere in its path, that `priorResults` is a fresh function-local map per `Run()` call (no cross-run/cross-org aliasing possible), and that a step genuinely cannot reference itself or a not-yet-executed step (traced the exact ordering: a step's own `priorResults` entry is written only after that step's own `resolveParams` call already completed); independently re-traced `resolveStepIDs`' SQL scoping and confirmed airtight. Zero exploitable findings.
  - **Test coverage:** `eami-gateway/internal/workflow/params_test.go` (7 pure unit tests: static-only unaffected, nested-path extraction, unresolved-reference failure covering future-step/cross-workflow/cross-run identically, non-"allowed" prior step, path-not-found, precedence, no param-map aliasing). `mapping_test.go` (3 real-Postgres end-to-end tests: a real extracted value genuinely received by a real downstream call (AC1); a real path-not-found failure confirmed `outcome='blocked'` at the DB level with the downstream adapter never called (AC3); a REAL second workflow's real step id substituted as `from_step`, confirmed to never resolve (AC4, real data)). `eami-api/internal/api/workflows_test.go` gained 5 tests: `id` survives a pure reorder (AC2); `input_mapping` survives an unrelated field edit; an echoed id from a *different* workflow is never reused (real HTTP round trip); the duplicate-echoed-id regression guard; static params survive a reused-step-id sibling edit (the second bug's regression guard). **Verified 2026-08-13 with a real toolchain: `go build`/`go vet`/`go test ./... -count=1` clean across the full `eami-gateway` and `eami-api` modules.**
  - **Live-verified end-to-end against the real `docker-compose` stack, rebuilt fresh (multiple times, as review/verification findings were fixed):** a real 2-step workflow (`b046-live-verify`/`echo_post`, real `postman-echo.com` dispatch) with step 1's real static `lookup_key` param and step 2's real `input_mapping` extracting it — run via a real agent JWT against `POST /v1/gateway/workflows/{id}/run`: step 2's real downstream HTTP request genuinely carried the exact value extracted from step 1's real response, traced through the real `workflow_run_steps.result` column directly via `psql`. Separately confirmed the params-carry-forward fix live (step 1's params intact after an unrelated step-2 edit) and AC3 live (a broken path failed the run cleanly, `outcome='blocked'`, a precise error naming the exact param/path/step). Test workflow deleted afterward; shared dev DB confirmed unaffected.
  - **Dependencies:** B-058 (`input_mapping` column, extended not modified), B-059 (execution engine/`workflow_step_params`, both reused), the MCP Discovery investigation (D9). **Out of scope, as specified:** any UI for defining extraction expressions (a real variable-picker UX is a separate, later polish brief — this brief is the backend mechanism only); branching/conditional logic; the visual canvas redesign.
